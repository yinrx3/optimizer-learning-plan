# 综述精读：优化方法的演化全景

> 本文是 `processing/2604.12968v1.pdf` 的专门精读笔记，不涉及其他材料。
> 所有公式、分类、实验数字均取自原文，标注了出处章节与表号，可直接回原文核对。

---

## 1. 这份综述是什么

| 项目 | 内容 |
| --- | --- |
| **标题** | Evolution of Optimization Methods: Algorithms, Scenarios, and Evaluations |
| **arXiv** | arXiv:2604.12968v1 [cs.LG]，2026-04-14 |
| **本地文件** | `processing/2604.12968v1.pdf`（72 页） |
| **作者** | Tong Zhang, Jiangning Zhang, Zhucun Xue, Juntao Jiang, Yicheng Xu, Chengming Xu, Teng Hu, Xingyu Xie, Xiaobin Hu, Yabiao Wang, Yong Liu, Shuicheng Yan |
| **单位** | 浙江大学 APRIL Lab（第一单位）、复旦、上海交大、新加坡国立 |
| **项目主页** | `https://github.com/APRIL-AIGC/Awesome-Optimizer` |

**它填的是什么洞。** 原文在 Introduction 里点名批评了既有综述的三点不足：

1. **覆盖面窄** —— 几乎只讲一阶（FO）和二阶（SO），忽略零阶（ZO）与"场景导向"框架；而后两者恰恰是大模型显存受限训练、分布式学习、隐私保护的关键。
2. **缺统一、严格的数学分类** —— 导致算法归类零散不一致，看不出方法之间的内在联系与演化逻辑。
3. **缺标准化大规模实证基准** —— 文献结论互相冲突、不可复现，工程上无从选型。

所以这份综述的三项贡献是：**统一分类学**（Sec. 2）、**场景导向分析**（Sec. 3.5–3.8）、**标准化基准**（Sec. 4，23 个优化器）。

> 🎯 **定位判断：** 这是一份"地图"，不是一份"教材"。它的价值在于给你坐标系和文献索引，公式推导全部一笔带过、引用原始论文。所以读法必须是**跳读 + 回原论文**，逐行硬啃会浪费大量时间。它最适合你的用法是：**先建立坐标系 → 再挑 3–5 个点深入**。

---

## 2. 三条主线：为什么"收敛速度"不再是唯一瓶颈

原文 Introduction 的核心论点是：**优化算法的瓶颈已经从数学转移到了物理与系统约束**。它把这一转变归结为三堵墙：

| 墙 | 物理来源 | 优化器侧的应对 | 原文位置 |
| --- | --- | --- | --- |
| **显存墙**（memory wall） | 反向传播要存激活与优化器状态 | ZO 免反向传播；低秩/量化/minimal-state 设计 | Sec. 3.4, 3.7 |
| **通信墙**（communication wall） | 去中心化网络中的梯度同步 | 梯度压缩/量化、本地更新、去中心化通信 | Sec. 3.5 |
| **隐私墙**（privacy wall） | 敏感数据不能直接上传 | 差分隐私：梯度裁剪 + 高斯噪声 | Sec. 3.6 |

原文的结论句很直白：现代优化器的演化是**"对这一可行性危机（feasibility crisis）的统一但碎片化的回应"**——传统上被视为纯算法设计的问题，现在变成了**高度受约束的、系统感知的工程解**。

> 💡 **核心洞察 ①：优化算法的研究重心发生了位移。**
> 1950–2015 年，优化器的评价轴基本只有一根：**收敛速率**（凸情形下的 $O(1/T)$、$O(1/T^2)$，非凸情形下找 $\epsilon$-稳定点的迭代复杂度）。但在这份综述的叙事里，当你把模型放大到千亿参数、把数据放进联邦网络、把隐私合规写进合同，**每步迭代的算术成本、跨设备通信字节数、以及隐私预算 $\epsilon$ 的消耗，都变成了与收敛速率同等甚至更硬的约束**。
> 这意味着一个数学上"更优"的算法（比如真正的自然梯度）可能因为每步要算矩阵逆而完全不可用。$\Rightarrow$ **评价一个优化器时，先问它优化的是哪个目标函数——是收敛速率，还是"在某个资源预算下的有效收敛速率"。**

> 💡 **核心洞察 ②：三堵墙不是并列的，而是有优先级的。**
> 显存墙是最硬的一堵：它决定了一个算法**能不能跑起来**，而不是跑得快不快。这解释了为什么 ZO 方法会在沉寂多年后"重新兴起"（reemerge）——它用 $q$ 次前向查询换掉一次反向传播，代价是梯度估计方差激增，但换来的是显存可行性。$\Rightarrow$ **当可行性成为瓶颈时，"理论上更差"的算法可以因为"能跑"而胜出。** 这是数学系学生最容易忽略的一层。

---

## 3. 全文骨架（原文目录，直接抄录）

这份综述的**目录本身就是最有价值的一页**——它把整个领域切成了一张完整的格子。建议先把这张表背下来，再读正文。

| 章节 | 标题 | 页 | 一句话内容 |
| --- | --- | --- | --- |
| **1** | Introduction | 1 | 三堵墙、三项贡献、23 个优化器的核心发现 |
| **2** | Background | 3 | 问题设定、评价指标、大模型优化的特殊性、历史路线图 |
| 2.1 | Optimization Problem Setup | 3 | 形式化问题定义 |
| 2.2 | Deep Learning Optimization | 3 | 深度学习优化的非凸/高维/随机特性 |
| 2.3 | Evaluation Metrics | 4 | 用什么指标衡量"好" |
| 2.4 | Optimization for Large Models | 4 | 大模型带来的新约束 |
| 2.5 | History and Roadmap | 4 | 优化器演化的时间线（含机构与发表venue） |
| **3** | Optimization Algorithms | 5 | **全文主干的分类学** |
| 3.1 | Unified Mathematical Perspective | 6 | **四算子统一框架（本文最有价值的一节）** |
| 3.2 | First-Order Algorithms | 7 | 一阶方法，8 个子主题 |
| 3.2.1 | Accelerating Convergence Rate | 8 | 动量、Nesterov、前瞻式梯度 |
| 3.2.2 | Adaptive Step-Size Control | 10 | AdaGrad→Adam→LAMB→LARS 家族 |
| 3.2.3 | Variance Adaptation | 14 | 梯度方差/结构信息的利用 |
| 3.2.4 | Enhancing Training Stability | 15 | 梯度裁剪、异常值抑制 |
| 3.2.5 | Learning Rate Scheduling | 16 | 调度策略与 schedule-free |
| 3.2.6 | Enhancing Generalization Ability | 18 | 平坦极小值、SAM 家族 |
| 3.2.7 | Hybrid Methods | 19 | 组合策略 |
| 3.2.8 | Towards LLMs Training | 21 | 面向大模型的四维重构 |
| 3.3 | Second-Order Algorithms | 22 | 二阶方法，4 个子主题 |
| 3.3.1 | Hessian Approximation & Estimation | 23 | 块对角、子采样、Nyström |
| 3.3.2 | Fisher Information Matrix Applications | 24 | 自然梯度、K-FAC |
| 3.3.3 | Quasi-Newton Methods | 25 | BFGS 家族、L-BFGS |
| 3.3.4 | Second-Order Moment Fusion | 26 | 二阶信息与动量融合 |
| 3.4 | Zeroth-Order Algorithms | 27 | 零阶方法，6 个子主题 |
| 3.4.1 | Adaptive Methods | 27 | ZO 自适应步长 |
| 3.4.2 | Perturbation Optimization | 29 | 扰动方向与稀疏扰动 |
| 3.4.3 | Zeroth-First Order Hybrid | 30 | 混合阶数 |
| 3.4.4 | Memory-Efficient Methods | 30 | 显存高效 ZO |
| 3.4.5 | Variance Reduction | 32 | 控制变量法 |
| 3.4.6 | Distributed Zero-Order Optimization | 32 | 分布式 ZO |
| 3.5 | Distributed Optimization | 33 | 分布式，6 个子主题 |
| 3.5.1 | Gradient Compression & Quantization | 34 | 压缩与量化 |
| 3.5.2 | Local Update Strategies | 35 | 本地多步更新 |
| 3.5.3 | Decentralized Communication | 36 | 去中心化拓扑 |
| 3.5.4 | Federated Learning Optimization | 37 | 联邦学习 |
| 3.5.5 | Communication Scheduling & Threshold | 39 | 通信调度与阈值 |
| 3.5.6 | Distributed Hybrid Optimization | 39 | 分布式混合 |
| 3.6 | Privacy-Preserving Optimization | 40 | 隐私保护，5 个子主题 |
| 3.6.1 | Differential Privacy Optimization | 41 | DP 优化 |
| 3.6.2 | Gradient Noise Injection | 42 | 梯度噪声注入 |
| 3.6.3 | Privacy-Utility Tradeoff | 42 | 隐私-效用权衡 |
| 3.6.4 | Federated Privacy Enhancement | 42 | 联邦隐私增强 |
| 3.6.5 | Privacy-Aware Gradient Clipping | 43 | 隐私感知裁剪 |
| 3.7 | Memory-Efficient Optimization | 43 | 显存高效，6 个子主题 |
| 3.7.1 | Low-Memory Optimizer Design | 44 | 低显存优化器设计 |
| 3.7.2 | Low-Rank Algorithms | 44 | 低秩算法 |
| 3.7.3 | Optimizer State Compression | 45 | 优化器状态压缩 |
| 3.7.4 | Low-Rank Gradient Storage | 45 | 低秩梯度存储 |
| 3.7.5 | Stateless Optimization | 45 | 无状态优化 |
| 3.7.6 | Memory-Efficient for Large Models | 46 | 大模型显存高效 |
| 3.8 | Tailored Optimization Approaches | 46 | 定制化方法 |
| 3.8.1 | Auto-Designed Optimizers | 46 | 自动搜索优化器（含 Lion 的发现过程） |
| 3.8.2 | Robust Optimization | 47 | 鲁棒优化 |
| **4** | Experiments | 47 | **23 个优化器的标准化基准** |
| 4.1 | Datasets | 48 | ImageNet-1K + WikiText-103 |
| 4.2 | Metrics and Settings | 48 | Top-1 / PPL，训练配方 |
| 4.3 | Analysis | 49 | 六个维度的实证分析 |
| 4.3.1 | Learning Rate Sensitivity on ViT-S | 49 | 学习率敏感性 |
| 4.3.2 | Cross-Architecture Generalization | 51 | 跨架构泛化 |
| 4.3.3 | Long-term Training Scalability | 52 | 长期训练可扩展性 |
| 4.3.4 | Correlation of Optimizers | 53 | 优化器轨迹相关性 |
| 4.3.5 | Comprehensive Optimizer Evaluation | 54 | 综合星级评价 |
| 4.3.6 | Computational cost disclosure | 54 | 算力成本披露 |
| **5** | Future Prospect | 55 | 未来方向 |
| **6** | Conclusion | 57 | 总结 |

---

## 4. 全文精华：四算子统一框架（Sec. 3.1）

**如果这份综述你只读一页，读 Sec. 3.1。** 它把几乎所有优化器写成同一个离散动力系统：

$$\tilde g_t = \mathcal{E}(f, \theta_t, \xi_t), \qquad \hat g_t = \mathcal{T}_{\text{scenario}}(\tilde g_t),$$
$$m_t = \phi(m_{t-1}, \hat g_t),$$
$$\theta_{t+1} = P_\Theta\!\left(\theta_t - \eta_t M_t^{-1} m_t - \eta_t \lambda \theta_t\right)$$

（以上四行合起来即原文 **Eq. (7)**）

其中 $\xi_t$ 是随机数据批，$\eta_t$ 是步长，$\lambda$ 是权重衰减。

### 四个算子各自的含义

| 算子 | 名称 | 作用 | 一阶实例 | 二阶实例 | ZO / 场景实例 |
| --- | --- | --- | --- | --- | --- |
| $\mathcal{E}(\cdot)$ | **梯度估计器** | 决定"目标景观信息如何获取" | 标准随机梯度 $g_t$ | 用子采样构造曲率代理 | 有限差分：$\tilde g_t^{ZO} = \frac{1}{q}\sum_{i=1}^{q}\frac{f(\theta_t+\mu u_i)-f(\theta_t-\mu u_i)}{2\mu}u_i$，$u_i\sim\mathcal N(0,I)$ |
| $\mathcal{T}_{\text{scenario}}(\cdot)$ | **场景变换** | 把环境约束直接注入梯度流 | 恒等映射 | 恒等映射 | 隐私：$\mathrm{Clip}(\tilde g_t, C) + \mathcal N(0,\sigma^2C^2I)$；分布式：$\mathcal C(\tilde g_t)$（有损压缩/量化/稀疏化） |
| $\phi(\cdot)$ | **动量** | 时间维度的信息累积 | 一阶动量 EMA | 曲率信息融合进动量序列 | — |
| $M_t^{-1}$ | **预条件子** | 在病态空间中扭曲梯度以加速 | 对角矩阵，如 Adam 的 $M_t=\mathrm{diag}(\sqrt{v_t}+\epsilon)$；或矩阵级算子范数下的结构归一化，如 Muon 的谱正交化 | $M_t = \hat H_t + \gamma I$，$\hat H_t\in\{\nabla^2 f(\theta_t), F_t\}$ 为 Hessian 或 FIM 的近似，$\gamma$ 为阻尼 | 单边预条件（如 ASGO 用低秩梯度分解构造 $M_t$） |
| $P_\Theta$ | **结构投影** | 把更新映回可行参数集 | — | — | 低秩/稀疏约束下的投影 |

### 两个必须记住的分类判据

原文给出了一个**反直觉但极重要的判据**：

> **使用经验 Fisher 信息矩阵（EFIM）的方法，被归入"一阶"而非"二阶"。**

理由是：EFIM **本质上捕捉的是梯度方差，而不是真正的二阶曲率**（原文引用 Kunstner et al. 2019 支持这一点）。所以：

- **一阶**：$M_t$ 仅由一阶信息导出——梯度历史统计量，或梯度矩阵自身的几何结构。
- **二阶**：$M_t$ 显式包含高阶几何，形如 $M_t = \hat H_t + \gamma I$。

> 💡 **核心洞察 ③："二阶"是一个关于*信息来源*的标签，不是关于*矩阵形状*的标签。**
> 这是初学者最容易搞混的地方。Shampoo 用的预条件子是 Kronecker 分解的矩阵（形状上很"二阶"），但因为它的因子来自**经验 Fisher**，按本文判据它属于一阶；而 K-FAC 虽然也用 Kronecker 分解，但它近似的是**真正的 Fisher**，所以属于二阶。$\Rightarrow$ **不要看矩阵长什么样，要看这个矩阵是从哪个统计量估出来的。** 这个判据也直接给出了一条研究线索（见下）。

### 框架顺手暴露出的一个理论缺口

原文自己给出了一个由框架推出的研究方向，值得单独记下来：

> Kronecker 分解类方法（如 Shampoo）计算高效，但它们依赖**经验** Fisher，这限制了 $M_t$ 只能捕捉梯度方差而非真正的几何曲率。**补救思路是重新设计 $\mathcal E$ 与 $M_t$ 的耦合方式**——例如把"显存高效的梯度估计器"（用 model-distribution sampling 去逼近**真** Fisher）与"结构化/局部化预条件子"组合起来。

> 🎯 **这是全文最像"可做的题目"的一段。** 它把 Sec. 3.1（框架）、Sec. 3.3（二阶）、Sec. 3.7（显存高效）三块串成了一个明确的 gap：**真 Fisher 的显存高效估计 × 结构预条件子**。如果你将来要找选题，这句话就是起点。

---

## 5. 算法地图：五条支线

### 5.1 一阶方法（Sec. 3.2，8 个子主题）

原文的组织逻辑不是按时间，而是按**"它想解决什么问题"**。这个切法比按时间线好记得多：

| 子节 | 要解决的问题 | 代表算法 | 关键机制 |
| --- | --- | --- | --- |
| 3.2.1 加速收敛 | 动量框架在复杂景观中的固有局限 | Momentum, NAG, **adaNAPG**, **SGDO** | 在"前瞻"位置（$\theta_t + \gamma v_{t-1}$）算梯度，修正动量过冲；SGDO 直接在**未来权重**处计算梯度来改进更新方向 |
| 3.2.2 自适应步长 | 减少手工调参成本 | AdaGrad, RMSprop, Adam, **LAMB**, **LARS** | 通用框架 $\theta_{t+1} = \theta_t - \eta_t\,\phi(g_{1:t})/\psi(g_{1:t})$，$\phi$ 为一阶动量、$\psi$ 为二阶预条件子；LAMB/LARS 做逐层自适应以约束步长 |
| 3.2.3 方差适应 | 把参数拉成向量会丢掉结构信息 | Kron, LAPROP, Shampoo | 用矩阵/张量结构做预条件；单度量预条件子以降低协方差矩阵的存储与求逆开销 |
| 3.2.4 训练稳定性 | 梯度爆炸与方差失衡 | 各类裁剪方法 | 用**历史动量**动态决定局部裁剪阈值，与既有优化器（Adam、AdaBelief）兼容 |
| 3.2.5 学习率调度 | 收敛速度与稳定性的权衡 | Schedule-Free, **AdamMC** | AdamMC 归一化自适应优化器的一阶动量，得到更短更光滑的轨迹 |
| 3.2.6 泛化能力 | 收敛到尖锐极小值 | SAM 家族, **SSAM**, **F-SAM**, GAM | 用极小极大形式主动寻找平坦极小值；SSAM 对内部上升步做梯度范数重归一化；F-SAM 用历史随机梯度的 EMA 过滤全梯度分量，用残差噪声替代精确扰动的计算开销 |
| 3.2.7 混合方法 | 单一策略无法兼顾多个目标 | 各类组合 | 在收敛速度、稳定性、泛化、资源效率之间取舍 |
| 3.2.8 面向 LLM 训练 | 硬件显存瓶颈与计算低效 | 低秩/分块/混合精度类 | 从**梯度变换、降维、分块计算、参数更新**四个维度重构优化动态；低秩方法的优势是每步复杂度有严格上界，代价是丢弃低秩信息带来的近似误差 |

> 📌 **3.2.2 的通用框架值得单独记：** $\theta_{t+1} = \theta_t - \eta_t\,\phi(g_{1:t})/\psi(g_{1:t})$。几乎所有"自适应学习率"方法都是给 $\phi$ 和 $\psi$ 选不同的函数。你自己写一个优化器时，第一件事就是问：我的 $\phi$ 和 $\psi$ 是什么？

### 5.2 二阶方法（Sec. 3.3，4 个子主题）

| 子节 | 主题 | 代表算法 | 权衡 |
| --- | --- | --- | --- |
| 3.3.1 | Hessian 近似与估计 | 块 Hessian 近似, **SketchySGD** | 用**结构化块对角**或**随机子采样低秩代理**换取效率；激进简化会在高度非凸景观上显著掉点 |
| 3.3.2 | Fisher 信息矩阵的应用 | 自然梯度, **K-FAC**, **AdaFisher** | 自然梯度是理论基石；K-FAC 用激活梯度协方差做 Kronecker 分解，使矩阵求逆可行 |
| 3.3.3 | 拟牛顿法 | BFGS, S-BFGS, **mL-BFGS**, Q-Newton, **FUSE-PV** | 通过迭代更新动态逼近曲率，避免显式 Hessian；FUSE-PV 把 L-BFGS 与 mini-batch 计算适配 |
| 3.3.4 | 二阶矩融合 | **Sophia**, **AdaHessian** | 把局部曲率信息融进动量序列，统一定义为 $\theta_{t+1} = \theta_t - \eta P_t^{-1} m_t$ |

> ⚠️ **本文自己承认的核心缺口（3.3.4 末）：** 现有研究缺乏一种**根据实时噪声水平动态校准"曲率整合程度"的自适应机制**，来保证融合的最优性。也就是说，"该用多少二阶信息"这件事目前还是超参数，没有原则性的答案。$\Rightarrow$ 这是又一个可直接切入的开放问题。

### 5.3 零阶方法（Sec. 3.4，6 个子主题）

ZO 的根基是**有限差分**，用前向函数求值换取免反向传播。原文给的估计式（与统一框架一致）：

$$\tilde g_t^{ZO} = \frac{1}{q}\sum_{i=1}^{q}\frac{f(\theta_t+\mu u_i)-f(\theta_t-\mu u_i)}{2\mu}\,u_i,\qquad u_i \sim \mathcal N(0,I)$$

其中 $\mu$ 是平滑半径（smoothing radius），$q$ 是查询数。

| 子节 | 主题 | 代表算法 | 要点 |
| --- | --- | --- | --- |
| 3.4.1 | 自适应方法 | MeZO, ZO-AdaMM, ZO-AdaMU | 把一阶的自适应机制搬到 ZO 域，缩小与 FO 的性能差距 |
| 3.4.2 | 扰动优化 | FZOO, QZO | **稀疏扰动**的优势是在严格显存约束下适配超大模型；代价是硬阈值或逐层选择可能丢掉关键的跨层梯度信息 |
| 3.4.3 | 零阶-一阶混合 | — | 构造同时最小化显存占用与梯度方差的估计器；显著加速理论收敛，但维护校正项带来额外计算成本 |
| 3.4.4 | 显存高效方法 | LOZO, TeZO, LeZO, **KerZOO** | LOZO 用低秩适配；KerZOO 是基于核函数的 ZO 框架，用核平滑缓解低维投影问题 |
| 3.4.5 | 方差缩减 | **MeZO-SVRG**, **VAMO** | 用控制变量（control variates）结合历史梯度信息抑制估计方差，给出更严格的收敛速度保证 |
| 3.4.6 | 分布式 ZO | — | ZO 与分布式结合 |

### 5.4 分布式优化（Sec. 3.5，6 个子主题）

| 子节 | 主题 | 核心权衡 |
| --- | --- | --- |
| 3.5.1 | 梯度压缩与量化 | 压缩率 vs 收敛性；保留严格理论界的内部补偿显存 |
| 3.5.2 | 本地更新策略 | 允许各设备本地多步更新以减少同步；本地步数↑ ⇒ 通信↓ 但客户端漂移↑ |
| 3.5.3 | 去中心化通信 | 无中心节点的拓扑一致性 |
| 3.5.4 | 联邦学习优化 | 数据异构（non-IID）下的收敛 |
| 3.5.5 | 通信调度与阈值 | 什么时候通信（触发式）而非每次都通信 |
| 3.5.6 | 分布式混合优化 | 压缩 + 本地更新 + 自适应等的组合 |

### 5.5 隐私保护（Sec. 3.6）、显存高效（Sec. 3.7）、定制化（Sec. 3.8）

隐私保护的核心就是统一框架里的场景变换：

$$\mathcal T(\tilde g_t) = \mathrm{Clip}(\tilde g_t, C) + \mathcal N(0, \sigma^2 C^2 I)$$

即**范数有界 + 高斯噪声注入**，对应 5 个子主题：DP 优化、梯度噪声注入、隐私-效用权衡、联邦隐私增强、隐私感知裁剪。

显存高效（Sec. 3.7）的 6 个子主题：低显存优化器设计、低秩算法、优化器状态压缩、低秩梯度存储、**无状态优化（stateless）**、大模型显存高效。

- **无状态优化**的思想很干净：**彻底消除对历史数据累积的依赖**，更新方向直接由瞬时计算和参数的内在属性导出。
- **tpSGD** 走得更远，它重新定义训练范式本身：用**随机标签投影**生成本地目标，实现逐层训练。

定制化方法（Sec. 3.8）：

- **3.8.1 自动设计优化器**：把算法发现形式化为**无限稀疏空间中的程序搜索**，用演化技术高效探索——**Lion 就是这么被发现的**（符号动量更新，降低显存使用）。
- **3.8.2 鲁棒优化**：应对系统性的不规则性，包括随机梯度噪声、硬件量化误差、数据分布漂移。

---

## 6. 实验部分（Sec. 4）：23 个优化器的标准化基准

### 6.1 设置

| 维度 | 内容 |
| --- | --- |
| **视觉任务** | ImageNet-1K（约 128 万训练图 / 5 万验证图 / 1000 类） |
| **视觉架构** | ResNet-50（CNN 代表）+ ViT-Small（Transformer 代表） |
| **语言任务** | 从零训练 **60M 参数 Llama**，数据集 WikiText-103 |
| **语言指标** | 困惑度 PPL |
| **语言设置** | batch size 256，序列长度 512，token 总数按 Chinchilla scaling 确定 |
| **视觉设置** | DeiT 设置（ViT-S/现代 CNN）、A2/A3 设置（CNN，匹配 ViT 的收敛速度） |
| **学习率网格** | 基础 LR 的 0.1× / 0.2× / 1× / 5× / 10× 五档 |
| **训练轮数** | 100 epoch 与 300 epoch（考察长期可扩展性） |
| **评价方式** | Tab. 7 用**五星评价**，横跨五项准则，星数反映相对性能百分位 |

> 📌 **一个诚实性细节值得学：** 原文明确说明 60M 的配置**不是为了复现完整规模 LLM 的涌现行为**，而是作为现代自回归 Transformer 的**架构代理（architectural proxy）**，在保留 Llama 核心架构特征的同时保持计算可行。$\Rightarrow$ 写论文时这种"我这个实验能说明什么、不能说明什么"的自我限定，是审稿人非常看重的。

> 📌 **另一个细节：** 有一批优化器（SGD、Shazeer & Stern 2018、Taniguchi et al. 2024、Zhuang et al. 2020、Li 2017、You et al. 2017、Zhang et al. 2019、Defazio & Jelassi 2022、AdamP/SGDP 等）在 **ResNet + BCE loss** 下会不稳定或不收敛，因此在这些情况下改用 **CE loss**。$\Rightarrow$ 这本身就是一条实证信息：**这些优化器对损失函数形式敏感。**

### 6.2 核心发现

**发现一：SGD 在 LLM 上灾难性崩塌。**

原文用词是 "catastrophic training collapse on the highly anisotropic loss landscapes of LLMs"（Fig. 9），并由此断言**自适应缩放是绝对必要的**。

**发现二：标准自适应方法（Adam 家族）对学习率极度敏感。**

5× 或 10× 放大通常直接导致不收敛或性能断崖式下跌。而 Muon 和 MARS 在极端学习率缩放下仍保持**低且稳定的 PPL（约 12–14）**。

**发现三：Muon 与 MARS 的跨架构泛化最强。**

Muon 的跨架构泛化源于**矩阵级正交化**的设计——它不依赖标量方差历史，而是在矩阵层面处理结构信息。MARS 则靠梯度校正。两者在 ResNet 与 Llama 上都表现优秀。

**发现四：不同优化器各有"舒适区"。**

| 现象 | 优化器 |
| --- | --- |
| 只偏好保守学习率，鲁棒性有限 | RMSprop |
| 在**小学习率**下就遇到收敛困难，反而**需要较大 LR** | AdaBelief, LAMB, LARS, NovoGrad |
| 低倍缩放（0.1×–1.0×）方差极小，但大倍缩放容易崩 | Kron, Adan |
| 峰值精度落后（64.36%）但对大学习率异常鲁棒，即使 10× 仍可用 | SGD |
| 靠 sign 更新把更新量锁在 $\{-1,0,+1\}$，对梯度爆炸/消失天然免疫 | Lion, MADGRAD |

> 💡 **核心洞察 ④：SGD 的"鲁棒"和 Adam 的"脆弱"是同一个数学事实的两面。**
> Adam 的更新是 $\Delta\theta = -\eta\,\hat m_t/(\sqrt{\hat v_t}+\epsilon)$。**当某个参数的梯度极小时，$\sqrt{\hat v_t}$ 也极小，这个除法会产生一个巨大的乘数**；再叠加 10× 的基础学习率，该参数的实际步长就变得天文数字般巨大，瞬间摧毁权重。
> 而 SGD 的更新就是 $-\eta g_t$——即使 $\eta$ 放大 10 倍，步长仍然**与梯度成正比**，线性放大，不会失控。
> Lion/MADGRAD 则更彻底：**sign 更新把每维步长锁死为 $\eta$，与梯度幅值无关**。
> $\Rightarrow$ **这三者构成一条清晰的谱：步长完全依赖梯度幅值（SGD）→ 步长依赖梯度幅值的*比值*（Adam）→ 步长与梯度幅值完全解耦（Lion/sign-SGD）。** 稳定性从左到右递增，但表达力（对景观各向异性的适应能力）也同时在变。**这是理解全部自适应优化器的中心轴线。**

**发现五：长期训练的可扩展性分化（100 → 300 epoch）。**

- 用**复杂预条件、正交化或解耦权重衰减**的方法（各类高级方法），在**前 100 epoch 内**就快速穿越优化轨迹到达高质量解，后续训练**收益递减**，精度变化被限制在 **+2.622%** 以内。
- **反例是 RMSprop**：在 ResNet-50 上从 100 到 300 epoch 出现**性能回退**。原文解释为：**依赖未校正的平方梯度 EMA** 在长时间训练周期下的结构性脆弱。

**发现六：架构对轨迹的影响有时超过优化器本身。**

原文的实证相关矩阵显示：在 ViT 实验中，相关矩阵表现出**压倒性的同质性**，因为 ViT 的训练配方高度受限（constrained training regime）；而在 ResNet 上分化更明显。

> 💡 **核心洞察 ⑤：优化器排名不是优化器的属性，而是"优化器 × 架构 × 配方"三元组的属性。**
> 这是本文实验部分最实用的一条结论。"哪个优化器最好"是一个**不适应的问题（ill-posed question）**。正确的问法是："在 ResNet-50 + BCE + 这个 LR 网格下，哪个优化器最好？"以及"这个排名迁移到 ViT-S 上还成立吗？"
> 对做理论的人而言，这解释了一个长期困扰：**为什么关于优化器优劣的理论预测经常和实验不符**——因为理论分析通常针对一般的 $L$-光滑、$\mu$-强凸目标，而实验里架构带来的各向异性结构可能比算法的设计差异更dominant。**理论要起作用，就必须把架构结构（如注意力机制的谱特性）写进假设里。**

### 6.3 算力成本（Tab. 8）

> ⚠️ **下表按原文逐格转录。注意 ResNet-50 行的"超参搜索"列在原文中是 `-`（未拆分给出），因此该行只有一个约 288 小时的数字，不要误以为搜索与评估各占 288 小时。**

| 基准任务 | 硬件 | 超参搜索（5 LR × 23 优化器） | 最终评估（最优 LR × 23） | 总 GPU 小时 |
| --- | --- | --- | --- | --- |
| ResNet-50 (ImageNet-1K) | A100 | - | 约 288 小时（100/300 epoch） | 约 288 |
| ViT-S (ImageNet-1K) | A100 | 约 544 小时（100 epoch） | 约 213 小时（300 epoch） | 约 757 |
| LLaMA-60M (WikiText-103) | A100 | 约 28 小时（Chinchilla scaling） | — | 约 28 |
| **合计** | — | — | **≈ 45 天单卡 A100** | **≈ 1073 小时** |

> 📌 **这组数字本身就是一个论点。** 原文说：严格的网格搜索协议带来了**指数级的计算开销爆炸**，而约 1073 GPU 小时的庞大成本"凸显了现代优化器基准测试中的根本性计算壁垒"。$\Rightarrow$ 言下之意：**这个领域缺乏廉价的公平比较手段**，这既是问题，也是机会（比如"能否用代理基准预测大模型上的优化器排名？"）。

---

## 7. 未来方向（Sec. 5）

原文把当前挑战归纳为几类，再由此推方向：

**挑战：**

| 挑战 | 内容 |
| --- | --- |
| **显存与计算开销** | 模型到十亿参数级后，稠密优化器状态成为主要显存瓶颈；结构化近似试图平衡精度与显存，但**矩阵求逆与结构开销显著降低全局效率**；某些方法理论上迭代更少，但**每步计算延迟抵消了收益** |
| **噪声放大与估计方差** | 在深度注意力网络的各向异性景观上，mini-batch 训练的随机噪声在**曲率估计过程中被严重放大**，降低更新稳定性；随机扰动方法同样受影响 |
| 非凸景观中的发散 | 见 Shazeer & Stern 2018 相关工作 |

**方向：**

1. **自动生成架构专用的优化器** —— 让模型无需人工干预就能在复杂景观中导航。
2. **预条件与正交化** —— 既然 Kron、Muon 这类方法与自适应标量家族的**损失穿越轨迹本质不同**，未来应推进**结构化矩阵更新**：用结构梯度统计做预条件，或用矩阵正交化，突破简单对角缩放的表达瓶颈。
3. **动态精度缩放** —— 缓解显存压力。

> 🎯 **注意方向 2 的措辞。** 原文不是说"Muon 更好"，而是说这两类方法的**轨迹在本质上是不同的**（fundamentally different loss traversal trajectories）。这是一个更强的、也更有数学味的论断：**它不是"谁优谁劣"的问题，而是"它们在不同的几何里做最速下降"的问题。** 这正好呼应你在课程 §4.3 学到的"优化器 = 在选定范数下的最速下降"——$\ell_2$ 范数给 SGD，$\ell_\infty$ 给 sign-SGD，谱范数给 Muon。**同一件事的两种语言。**

---

## 8. 必须掌握的公式（12 条）

| # | 公式 | 出处 | 为什么必须会 |
| --- | --- | --- | --- |
| 1 | $\tilde g_t^{ZO} = \frac1q\sum_i \frac{f(\theta_t+\mu u_i)-f(\theta_t-\mu u_i)}{2\mu}u_i$ | Eq.(7) 附近 | ZO 的定义式；写出它才能谈方差 |
| 2 | $\hat g_t = \mathrm{Clip}(\tilde g_t, C) + \mathcal N(0,\sigma^2C^2I)$ | Sec. 3.1 | DP-SGD 的全部数学内容 |
| 3 | $\hat g_t = \mathcal C(\tilde g_t)$ | Sec. 3.1 | 分布式压缩的抽象形式 |
| 4 | $M_t = \mathrm{diag}(\sqrt{v_t}+\epsilon)$ | Sec. 3.1 | Adam 的预条件子；注意它属**一阶** |
| 5 | $M_t = \hat H_t + \gamma I$ | Sec. 3.1 | 二阶预条件子的一般形式；$\gamma$ 是阻尼 |
| 6 | $\theta_{t+1} = \theta_t - \eta_t\,\phi(g_{1:t})/\psi(g_{1:t})$ | Sec. 3.2.2 | 自适应学习率的通用分解 |
| 7 | $\theta_{t+1} = \theta_t - \eta P_t^{-1}m_t$ | Sec. 3.3.4 | 二阶矩融合的统一形式 |
| 8 | $\theta_{t+1} = \theta_t - \eta_t M_t^{-1}m_t - \eta_t\lambda\theta_t$ | Eq.(7) | 解耦权重衰减 |
| 9 | $\theta_{t+1} = P_\Theta(\cdot)$ | Eq.(7) | 结构投影，低秩/稀疏约束的来源 |
| 10 | $\theta_t + \gamma v_{t-1}$ | Sec. 3.2.1 | "前瞻"位置；NAG 的精髓 |
| 11 | $G_t = G_{t-1} + g_t^2$ → $\mathbb E[g^2]_t$ | Sec. 2.5 | AdaGrad → RMSprop 的替换动作 |
| 12 | sign 更新 $\mathrm{sign}(c_t)$ | Sec. 4.3.1 | 步长与梯度幅值解耦的机制 |

---

## 9. 和其它材料的关系

这份综述的价值一半在它自己，一半在它是**索引**。下面是你本地/清单里能接上的点：

| 综述里的位置 | 接什么 | 怎么接 |
| --- | --- | --- |
| Sec. 3.1 四算子框架 | 课程 `lectures/Chapter4/variants.html` §4.3 | **框架 ↔ 范数视角**：本文说"$M_t$ 是结构归一化"，课程说"优化器 = 选定范数下的最速下降"。两者是同一件事的代数语言与几何语言，**必须能互相翻译** |
| Sec. 3.2.3 / 3.3.2 结构预条件 | Muon、Shampoo 原论文 | 综述只给一句结论，机制要回原文补。课程 §4.3 的两个作业题（实现 Muon/Shampoo、讨论 NS step）正好是这条线的动手入口 |
| Sec. 3.1 "EFIM 属一阶" | Bernstein & Newhouse, *Old Optimizer, New Norm* (arXiv:2409.20325) | 那份工作正是从**范数/最速下降**的角度重新解释"老的优化器"，与本判据互为印证 |
| Sec. 3.7 显存高效 + Sec. 3.3 二阶 | 你清单里的 Newton Matching (`maybeRead/2609.05727v1.pdf`) | 综述承认二阶方法的瓶颈是**矩阵求逆与每步延迟**，Newton Matching 恰是在这条线上做文章 |
| Sec. 5 方向 2（正交化/结构化更新） | 苏剑林《Muon 赏析》《从谱范数梯度到新式权重衰减的思考》 | 综述给方向，苏文给可视化的机制推导 |
| Sec. 4.3.1 LR 敏感性 | 苏剑林《重新思考学习率与 Batch Size》系列、`ScalingOpt` 的相关笔记 | 综述给现象（Adam 5×/10× 崩），苏文给 ELR / 权重范数增长的解释 |
| Sec. 5 + 三堵墙 | 导师方向（ELR/GradLoc、双时间尺度优化） | GradLoc 的 LayerClip 正是"逐层梯度异质性"的**缓解手段**，而综述 Sec. 4.3.4 指出架构影响有时超过算法本身——两者指向同一个未解问题 |

---

## 10. 逐节讲解（原文 Sec 1 到 Sec 6 全覆盖）

> 本节按原文顺序逐节展开，覆盖 Sec 1 到 Sec 6 的全部小节与 P1–P58 的正文页。
> 每一节的结构统一为：**这一节在讲什么 → 原文的关键论断与公式 → 为什么这样设计 → 它的代价或缺口**。
> 页码标注 `p3` 指 PDF 物理页码，可直接翻原文对照。
> 标 🌟 的是必须精读的节；其余可当字典查阅。

> 以下按原文顺序逐节展开。每一节的结构是：**这一节在讲什么 → 原文的关键论断与公式 → 为什么这样设计 → 它的代价或缺口**。
> 页码标注形式为 `p3`，指 PDF 物理页码，可直接翻原文对照。

---

#### Sec 1 Introduction（p1–2）——问题意识

这是全篇的论点章。它要说服你接受一个判断：**优化算法的瓶颈已经从数学转移到了物理与系统约束**。

**原文的论证链条**是这样的：深度学习历史上由基础优化算法驱动——SGD（Robbins & Monro 1951）建立了基本训练框架，Adam（Kingma 2015）家族后来成为各类任务的标准。但现代基础模型的规模"从根本上改变了这个优化范式"。在大语言模型训练中，这些传统主力暴露出严重局限。**原文的措辞是：首要瓶颈"不再仅仅是理论收敛速率，而是训练的物理与系统可行性"（the physical and systemic feasibility of training）**——由此提出三堵墙：

1. **显存墙（memory wall）**：反向传播强加的
2. **通信墙（communication wall）**：去中心化网络中的
3. **隐私墙（privacy wall）**：敏感数据的

然后是本篇的核心论断：**近期优化算法的多样化，是对这场"可行性危机"（feasibility crisis）的统一但碎片化的回应。** 原文用"re-architected"（被重新架构）这个词来描述传统数学原语的命运——一阶主力、二阶曲率感知方法、以及"近期复兴的显存高效零阶技术"，都在被重新架构。而通过演化为"场景导向范式"（scenario-oriented paradigms），现代优化器"正在超越纯算法设计，成为高度受约束的、系统感知的工程解"。

**四个研究领域的划分**（原文明确列举）：

| # | 领域 | 定义（原文表述） |
| --- | --- | --- |
| 1 | **FO 一阶** | 依赖一阶梯度及其导出统计量，以低计算开销获得优越收敛，**严格避免显式逼近真实二阶曲率** |
| 2 | **SO 二阶** | 显式构造并纳入真实曲率信息（Hessian 或真 FIM）来预条件更新方向，旨在**突破一阶算法的根本性能极限** |
| 3 | **ZO 零阶** | 通过函数求值逼近梯度方向，以**避免反向传播的巨大显存负担** |
| 4 | **场景导向** | 一个统摄性应用框架，专注于为分布式通信、差分隐私等专门用例**重新工程化这些基础原语** |

> 📌 **注意第 1 条里"严格避免（strictly avoiding）"这个措辞。** 这句话是理解全文分类学的钥匙，也是 Sec 3.1 那个反直觉判据（经验 Fisher 算一阶）的伏笔。一阶与二阶的分界线不是"用了多复杂的矩阵"，而是**有没有显式逼近真实曲率**。

接着，原文批评既有综述的三点不足（这是它为自己定位的地方）：

1. **覆盖面窄**：几乎只讲传统 FO 和 SO，忽略 ZO 与场景导向框架——而后两者"现在对显存高效的 LLM 训练、分布式学习、隐私保护应用是必需的"。
2. **缺统一且数学严格的分类学**：导致算法归类零散、不一致，**遮蔽了不同方法之间的内在联系与演化逻辑**。
3. **缺标准化大规模实证基准**：导致文献结论互相冲突、不可复现，"使研究者和工程师在实践场景中缺乏可靠的优化器选型指导"。

**三项贡献**（原文编号）：

1. **Unified Taxonomy 统一分类学**（Sec. 2）：统一了零散的概念定义，建立严格的数学分类学，追溯基础优化原语从 FO 到 SO、ZO 的演化。
2. **Scenario-Oriented Analysis 场景导向分析**（Sec. 3）：展示这些基础算法如何被重新架构为场景导向范式，以应对分布式通信壁垒与严格差分隐私约束等物理瓶颈；**这一分析凸显了"从纯算法设计到系统感知工程解"的转变——在理论保证与实践约束之间取得平衡**。
3. **Standardized Evaluation 标准化评估**（Sec. 4）：一个严格控制、**把算法性能与大规模工程优化分离开来**的评估框架，在包括 CNN 与 Transformer 的多种架构代理上评测 **23 个不同优化器**，系统性地隔离并检验学习率敏感性、长期训练可扩展性、跨架构泛化。

> 💡 **第 3 项贡献里"separates algorithmic performance from large-scale engineering optimizations"这句话值得单独注意。** 它的意思是：为了让比较公平，实验刻意**不**叠加各种工程加速技巧（并行、混合精度、通信优化），只让优化器本身的差异显现出来。这是一个方法学选择，也是有代价的——见 Sec 4.3.4 关于"ViT 训练配方高度受限导致相关性同质化"的讨论。

**Survey scope**：本篇聚焦四大范式（FO、SO、ZO、场景导向）。原文承认一个取样偏差：鉴于文献体量（含已发表论文与预印本），**"我们主要纳入有代表性和有影响力的工作"**，而非穷尽。

**Survey pipeline**：Sec. 2 背景 → Sec. 3 按梯度信息使用方式与应用场景分类综述 → Sec. 4 代表性优化器的实验评估 → Sec. 5 批判性审视剩余挑战与未来方向 → Sec. 6 总结。最后一句是"我们紧密跟进本项目的最新进展"，指向那个 GitHub 仓库。

#### 三张图（Fig. 1–3）

| 图 | 内容 | 怎么读它 |
| --- | --- | --- |
| **Fig. 1**（p2） | **优化方法的演化树**：以经典基础方法为根，分出一阶/二阶/零阶三个分支；**节点大小反映引用影响力**，不同簇图示从基础梯度更新到高级场景定制框架的进展 | 这是全文的视觉目录。建议打印出来贴在桌前，读 Sec 3 时随时对照自己在树的哪个位置 |
| **Fig. 2**（p4） | **优化方法数量的定量演化**：堆叠柱状图按 FO/SO/ZO 分类算法，折线追踪年度总数 | 关键读数：**2024 年起出现显著激增**，对应大规模深度学习模型的发展与优化需求。原文特别声明 2026 年统计不完整（只到 4 月），"不应被误解为研究活动下降" |
| **Fig. 3**（p5） | **重要优化算法时间线**：标注算法里程碑、关联研究机构与发表 venue | 这张图信息密度很高，比如 AdaGrad [JMLR] 2011 → Adam [ICLR] 2015 → K-FAC [ICML] 2015 → Shampoo [ICML] 2018 → AdamW [ICLR] 2019 |

> 🎯 **读 Fig. 3 时请特别注意一个细节**：时间线上 **Muon 标的是 `[GitHub]` 而不是会议 venue**（2024）。这不是排版疏忽——它反映了一个真实的领域现象：**若干有影响力的优化器工作最初是通过代码仓库和社区传播而非传统同行评审进入主流的**。Muon 就是典型例子。$\Rightarrow$ 这提醒你：追踪这个领域**不能只盯 arXiv 和会议**，代码仓库与社区讨论是同等重要的一手信息源。这对你建立"追踪清单"有直接的操作含义。

---

#### Sec 2 Background（p3–5）——形式化与坐标

这一节把深度学习优化放进经典优化的语言里，同时指出哪些经典假设在 DNN 上失效。

#### Sec 2.1 Optimization Problem Setup（p3）

**目标函数**：优化的根本目标是找参数 $\theta \in \mathbb R^d$ 使模型 $f(\cdot;\theta)$ 在未见数据上表现最优。假设样本 $z=(x,y)$ 抽自未知真实分布 $P$，目标是最小化**期望风险**：

$$R(\theta) = \mathbb E_{z \sim P}[\ell(f(x;\theta), y)]$$

由于 $P$ 未知，$R(\theta)$ 无法直接计算。依据**经验风险最小化**（Empirical Risk Minimization, Vapnik 1999）原理，用包含 $N$ 个 i.i.d. 样本的训练集 $S=\{z_1,\dots,z_N\}$ 构造替代目标——**经验风险** $J(\theta)$：

$$\min_{\theta \in \mathbb R^d} J(\theta) = \frac{1}{N}\sum_{i=1}^{N} \ell(f(x_i;\theta), y_i)$$

原文在这里加了一句极其重要的话：**当 $N \to \infty$ 时 $J(\theta)$ 收敛到 $R(\theta)$，但在有限样本情形下，对 $J(\theta)$ 的过度优化会导致过拟合。因此优化器的设计不仅要考虑训练损失的最小化，还要考虑泛化间隙（generalization gap），通常通过正则化或隐式偏置来处理。**

> 💡 **核心洞察（Sec 2.1）：优化器的目标函数从来不是训练损失。**
> 这一句话是整篇综述后面所有"泛化"讨论的源头。$\min J(\theta)$ 只是一个**代理问题**；真正想要的是 $\min R(\theta)$。而两者之间的差就是泛化间隙。
> 这解释了为什么 Sec 3.2.6 要专门用一节讲 SAM 家族的"平坦极小值"——**平坦极小值之所以好，不是因为训练损失更低，恰恰是因为它让 $J$ 和 $R$ 的差更小**。
> 对你（数学系背景）来说，这是从"优化问题"跨到"学习问题"的第一道坎：**在纯优化里，目标函数就是一切；在深度学习里，目标函数只是个代理。** 后面所有"隐式偏置""锐度""隐式正则化"的讨论，本质都在处理这个代理误差。

**随机梯度**：计算全梯度 $\nabla J(\theta) = \frac1N \sum_i \nabla \ell(z_i;\theta)$ 对大数据集是计算上不可行的。所以现代优化器普遍采用随机梯度。在第 $t$ 次迭代采样 mini-batch $B_t$：

$$g_t(\theta) = \frac{1}{|B_t|}\sum_{z \in B_t}\nabla \ell(z;\theta)$$

理论分析的标准假设是 $g_t$ 是真实梯度的**无偏估计**，即 $\mathbb E[g_t] = \nabla J(\theta)$，且有**有界方差** $\mathbb E[\lVert g_t - \nabla J\rVert^2] \le \sigma^2$。

> 📌 **把这两个假设记住，它们是后面所有"理论保证失效"讨论的靶子。** 原文在 Sec 3.2.1 和 Sec 3.2.2 末尾反复指出：**这些方法通常假设梯度有界或满足特定光滑性条件，而这些数学假设在现代架构的高度非凸景观中频繁被违反，从而限制了理论保证向实证成功的转化。** 一手是假设，一手是现实——这就是整个领域张力的来源。

**凸性与强凸性**：原文为了给收敛性分析提供直觉，正式定义了这两个概念。

$f$ 是**凸**的，若对所有 $x,y \in \mathbb R^d$ 成立一阶 Taylor 近似给出全局下界：

$$f(y) \ge f(x) + \langle \nabla f(x), y-x \rangle$$

该性质意味着**每个局部极小值必然也是全局极小值**。若 $f$ 二阶可微，凸性等价于 Hessian 半正定：$\nabla^2 f(x) \succeq 0$。

$f$ 是 $\mu$-**强凸**的，若对所有 $x,y$：

$$f(y) \ge f(x) + \langle \nabla f(x), y-x \rangle + \frac{\mu}{2}\lVert y-x\rVert^2$$

该性质**保证唯一全局最优解的存在**。直觉上，强凸意味着函数至少以二次速率增长。对二阶可微函数，这等价于要求 Hessian 的特征值下界为 $\mu$：

$$\nabla^2 f(x) \succeq \mu I$$

**收敛性**（原文给出的经典结论，建议记住这张速率对照）：

| 方法 | 目标类 | 收敛速率 |
| --- | --- | --- |
| GD | 凸 | 次线性 $O(1/T)$ |
| SGD | 凸 | 更慢的次线性 $O(1/\sqrt T)$（因梯度噪声方差） |
| GD | 强凸 | 线性 $O((1-\mu\eta)^T)$，$\eta$ 是学习率 |
| SGD | 强凸 | 到**固定误差半径**的线性收敛，半径取决于梯度噪声方差与 batch size；**要获得精确收敛，速率必须退化为次线性** |

> 💡 **核心洞察（Sec 2.1 收敛表）：为什么 SGD 永远无法精确收敛到最优点。**
> 看最后一行。SGD 在强凸情形下能线性收敛，但收敛到一个**误差球**而不是一个点；球半径由噪声方差 $\sigma^2$ 和 batch size 决定。**这不是算法缺陷，而是数学必然**：只要每步梯度都带 $\sigma^2$ 的随机扰动，参数的稳态分布就不可能塌缩成一个点。
> 要精确收敛，唯一的办法是让学习率**衰减到零**（$\sum \eta_t = \infty$ 但 $\sum \eta_t^2 < \infty$），此时线性速率退化为次线性。
> $\Rightarrow$ **这是一个不可回避的三难：收敛速度 / 最终精度 / 固定学习率，三者只能取二。** 后面 Sec 3.2.5 的学习率调度（schedule-free、余弦退火、warmup）全部是在解这个三难；Sec 3.2.2 的"方差有界保证"也是在这一层做文章。**你在读任何"我们的方法收敛更快"的声明时，第一个该问的问题就是：它比较的是收敛到球内的速度，还是收敛到零的速度？**

#### Sec 2.2 Deep Learning Optimization（p3–4）——经典假设如何失效

原文明确说：**凸理论提供直觉，但深度神经网络（DNN）的损失景观是高度非凸的，呈现出违反标准假设的独特挑战。** 它列了四条：

**① 非凸性（Non-convexity）**。DNN 损失函数包含大量局部极小值。深度学习优化**利用固有的梯度噪声与高维几何来逃离鞍点**，通常收敛到能带来强泛化性能的平坦极小值。

**② 鞍点（Saddle point）**。在高维空间中，**局部极小值相对稀少，而鞍点——在某些方向是局部极小、在另一些方向是局部极大的点——要普遍得多。** 对 GD 这类一阶方法，鞍点导致停滞与收敛缓慢；**SGD 由于随机梯度噪声往往能逃离鞍点，因而对非凸优化更有效**。

> 💡 **核心洞察（Sec 2.2）：噪声是特性，不是缺陷。**
> 把①和②连起来读，会得到一个反直觉的结论：**随机梯度噪声在这里是有用的**。它把 SGD 从"收敛到最近的驻点"变成"更容易找到好的极小值"。
> 这解释了一连串现象：
> - 为什么 full-batch GD 反而常常不如 SGD（GD 会干脆地停在鞍点）；
> - 为什么小 batch 往往泛化更好（更多噪声）；
> - 为什么 Sec 3.2.6 的 SAM 家族要**主动注入扰动**去寻找平坦极小值——它是在把 SGD 的"自发噪声优势"变成显式的算法机制；
> - 为什么 Sec 3.2.4 的梯度裁剪不能剪得太狠（会抹掉这个有用的噪声）。
> $\Rightarrow$ **从"噪声 = 误差"到"噪声 = 探索机制"，是理解深度学习优化的第二次观念转变**（第一次是上面那条"目标函数只是代理"）。

**③ 过参数化区域（Over-parameterization regimes）**。现代深度网络通常**过参数化**：参数数量远超训练样本数。在这个区域，优化过程表现出独特性质。

原文在此引出 **NTK（神经正切核）**理论（Jacot et al. 2018）：**在无限宽度极限下，配合小初始化与梯度流，神经网络的训练动力学可以被核回归近似。** 这为"为什么过参数化网络能收敛到全局最优并泛化良好"提供了理论框架。

> 📌 **NTK 这一条对你尤其重要，因为它是纯数学的工具。** 它的核心思想是：当网络无限宽时，参数 $\theta$ 几乎不动，网络输出作为 $\theta$ 的函数近似线性，于是训练变成在一个**固定的核**（NTK）下做核回归，而核回归是凸问题。
> 这给出一个"良性区域"（lazy training regime）：在那里经典收敛理论**成立**。但真实的深度学习恰恰**不在**这个区域——特征在学习，核在变化。这正是你感兴趣的那个方向（特征学习动力学）的起点：**NTK 描述的是特征不学习的极限，而真实训练的有趣之处全在"特征学习了"这件事上。**
> 原文只用一句话带过 NTK，但这句话是你连接"综述"与"你真正感兴趣的理论"的接口。

**④ 硬件与系统约束**（这一条被放在 Sec 2.3–2.4 展开）。

#### Sec 2.3 Evaluation Metrics（p4）——用什么衡量"好"

原文的立场是：**在大规模训练中，评价指标与模型精度同等关键。** 它给了四类指标：

| 指标类别 | 具体内容 | 原文的关键论述 |
| --- | --- | --- |
| **计算复杂度** | FLOPs（浮点运算数） | **直接与训练时长和硬件成本相关** |
| **训练显存消耗** | 两个来源：**激活值** + **优化器状态** | 激活值是前向传播产生、必须为反向传播的梯度计算保留的中间结果——**其显存占用可通过梯度检查点（gradient checkpointing, Chen et al. 2016）降低**。优化器状态带来可观开销，因为**像 Adam 这样的方法需要存一阶和二阶矩估计** |
| **隐私与安全** | DP-SGD（Abadi et al. 2016） | 通过**梯度裁剪 + 高斯噪声注入**提供隐私保证，**但这通常会降低收敛速度与最终模型精度** |
| （隐含第四类） | 最终任务指标 | Top-1 准确率 / 困惑度 PPL（Sec 4.2 展开） |

> 💡 **核心洞察（Sec 2.3）：优化器的"成本"有三本账，而学术论文通常只报第一本。**
> 三本账是：**算力（FLOPs）、显存（激活 + 优化器状态）、隐私预算**。传统优化理论只关心"需要多少次迭代"，而迭代次数**完全不能**换算成实际训练成本——因为每次迭代的成本在三个维度上差异巨大。
> 具体到 Adam：它需要为**每个参数**存两个标量（$m_t$ 和 $v_t$）。一个 7B 模型，FP32 下参数本身 28GB，Adam 状态再加 56GB——**优化器状态比模型本身还大一倍**。这就是"显存墙"的具体算术。
> $\Rightarrow$ **这条算术是理解 Sec 3.7 全部内容的钥匙。** 当你看到"低秩优化器""状态压缩""无状态优化"时，问的都是同一个问题：**这 2× 参数量的开销，能压到多少？** 而 Sec 3.7.5 的"无状态优化"给出了最激进的答案：压到 0。

#### Sec 2.4 Optimization for Large Models（p4）——硬件感知的五个手段

当参数量向十亿甚至万亿级扩展，优化策略**必须考虑硬件约束**。原文列了五项：

**① 混合精度训练（Mixed precision training）**。用 FP16 或 BF16 替代 FP32，降低显存占用、提升计算效率。**关键区别：FP16 需要损失缩放（loss scaling, Micikevicius et al. 2018）来防止梯度数值下溢，而 BF16 通常不需要额外的损失缩放，因为它的动态范围与 FP32 一致。**

> 📌 这是个很实用的工程知识点，也是一道好面试题：**为什么 BF16 不需要 loss scaling？** 答：因为 BF16 保留了 FP32 的 8 位指数（动态范围一致），只削减了尾数精度；FP16 只有 5 位指数，梯度下溢风险大。

**② 并行策略（Parallelism strategies）**。必须结合**数据并行、张量/模型并行（Shoeybi et al. 2019）、流水线并行（Huang et al. 2019b）**来克服单 GPU 显存限制。

**③ Batch size 与学习率**。增大 batch size 提升并行效率；但为维持收敛，学习率通常必须按特定规则缩放，**例如线性缩放规则 $\eta \propto B$（Goyal et al. 2017）**。

> 🎯 **这一条是你清单里"重新思考学习率与 Batch Size"那组苏剑林博客的直接对应点。** 原文只给了线性缩放规则这一句引用，而**线性缩放规则在大 batch 下会失效**（这正是那组博客要处理的问题，涉及 ELR 与权重范数增长）。综述在这里停在了 2017 年的结论上——这是一个可以顺着往下挖的接口。

**④ 梯度累积（Gradient accumulation）**。当显存约束不允许使用大 batch 时，在多个微步（micro-steps）上累积梯度，然后做一次参数更新，**从而模拟大 batch 训练的效果**。

#### Sec 2.5 History and Roadmap（p4–5）——演化谱系的细读

这一节给出基础方法的完整演化链条。原文的说法是"从公式化视角解剖基础方法的演化逻辑"，以下六步是**必须能背下来的一条线**：

| 步 | 方法 | 原文描述的**相对前者的具体改进** |
| --- | --- | --- |
| 1 | **GD**（Cauchy 1847） | 沿梯度反方向更新参数来最小化目标函数。基准 |
| 2 | **SGD**（Robbins & Monro 1951） | **相对于 GD 的改进是：基于 mini-batch 计算梯度**，显著提升计算效率 |
| 3 | **Momentum**（Polyak 1964） | **引入变量 $v_t$ 累积历史梯度信息**，使参数更新依赖的不只是当前梯度 |
| 4 | **NAG**（Nesterov 1983） | **进一步精化：在"前瞻"位置 $\theta_t + \gamma v_{t-1}$ 计算梯度**，修正动量引起的潜在过冲 |
| 5 | **AdaGrad**（Duchi et al. 2011） | **引入累积项 $G_t = G_{t-1} + g_t^2$ 实现逐参数自适应学习率** |
| 6 | **RMSprop**（Tieleman & Hinton 2012） | **把 AdaGrad 的直接累加替换为指数移动平均 $\mathbb E[g^2]_t$**，有效防止分母无限增长导致的过早学习率衰减 |
| 7 | **Adadelta**（Zeiler 2012） | **消除对手工设定全局学习率 $\eta$ 的依赖**，改用参数更新的均方根 $\sqrt{\mathbb E[\Delta\theta^2]_{t-1}}$ 做自适应调整 |

> 💡 **核心洞察（Sec 2.5）：这七步不是七个独立的发明，而是一条"分母改造史"。**
> 把 AdaGrad → RMSprop → Adadelta 三步连起来看，会发现它们改的都是**同一个位置**——更新式里的分母：
> - AdaGrad：分母是**历史平方梯度的总和** $G_t$。问题：$G_t$ 单调递增 → 学习率单调衰减 → 训练后期几乎停滞。
> - RMSprop：分母改成**指数移动平均** $\mathbb E[g^2]_t$。这样分母不再无界增长，学习率可以维持。**这一步是"修复 AdaGrad 的单调衰减"。**
> - Adadelta：**连分子也改造了**，用 $\sqrt{\mathbb E[\Delta\theta^2]_{t-1}}$ 代替学习率 $\eta$。**这一步是"消除对全局学习率这个超参数的依赖"。**
> $\Rightarrow$ **一阶自适应方法的历史 = 不断给分母（以及分子）换更好的估计量。** 记住这条，再看 Sec 3.2.2 那一大串方法（NosAdam、ADOPT、AdamNX、SET-Adam、HomeAdam、EAdam……），你会发现它们**几乎全部是在这个分母上做文章**：换估计量、去掉某个超参数依赖、加个上界、改个指数。**这不是一堆零散的技巧，而是一个参数空间被系统搜索的过程。**

**Fig. 2 的读数**：原文观察到 **2024 年起算法数量显著激增**，并解释为"对应大规模深度学习模型的出现"。图中柱状统计显示 2024–2025 年是快速增长期。原文特别声明：**2026 年的统计不完整（仅覆盖到 4 月），基于当前上升趋势，2026 年总数预期继续增长，因此当前的部分数字不应被误读为研究活动下降。**

**Tab. 1 统一记号表**（p6）——这张表定义了全文统一的数学记号，建议记住：

| 符号 | 含义 |
| --- | --- |
| $\theta_t$ | 第 $t$ 次迭代的模型参数（权重与偏置） |
| $\eta$ | 学习率，决定参数更新的步长 |
| $\nabla J(\theta_t)$ | 第 $t$ 步目标函数对参数的精确梯度 |
| $g_t$ | 第 $t$ 步目标函数的随机梯度（mini-batch 梯度） |
| $m_t$ | **有偏**一阶矩估计（过去梯度的指数移动平均） |
| $v_t$ | **有偏**二阶原始矩估计（过去平方梯度的指数移动平均） |
| $\beta_1, \beta_2$ | 一阶与二阶矩估计的衰减率 |
| $\epsilon$ | 防止除零的小平滑常数 |

> 📌 **注意"有偏（biased）"这个限定词。** $m_t$ 和 $v_t$ 在初始化时是 0，所以训练初期它们系统性偏小——这就是 Adam 要做**偏差校正**（$\hat m_t = m_t/(1-\beta_1^t)$）的原因。原文在记号表里特意标出 "biased"，是在为 Sec 3.2.2 的"偏差校正规则适应"那一类方法（AdamD、RAdam 等）预埋伏笔。

**Sec 2 的收尾**：原文说明，为了对算法演化做结构化分析（Fig. 4–7），他们**统一了数学记号系统（Tab. 1）**——该框架"结合通用基础符号与带上标的算法专用记号，以凸显各种方法之间的核心差异"，并且**明确勾勒出每个算法相对于其前驱所做的具体改进**。

> 🎯 **这句话点明了本文与普通综述的差别，也是它的阅读方法。** 它不满足于罗列算法，而是试图对每个算法回答"你比你的前驱多做了什么"。所以读 Sec 3 时，**对每个方法都追问一句"它改的是谁、改了什么"**——这正是我在第十二节的精读卡片模板里放"它和谁本质等价 / 谁是谁的特例"那一栏的原因。

#### Sec 2 里四张演化图（Fig. 4–7）

| 图 | 覆盖范围 | 内容 |
| --- | --- | --- |
| **Fig. 4**（p8） | 基础方法 | GD → SGD → Momentum → NAG，以及 AdaGrad / RMSprop / Adadelta 的过渡机制。标注了每个阶段的**核心数学更新规则与关键转换机制** |
| **Fig. 5**（p11） | 一阶方法 | 按三类分组：自适应策略（Adam / AdamW / Lion）、加速（Nadam / MADGRAD / Adan）、曲率近似（Shampoo / 4bit-Shampoo / ASGO）。图中给出了各方法的关键过渡机制标签，如 AdamW 的 "Decoupled"（权重衰减项 $\lambda$ 从梯度更新中解耦）、Lion 的 "Sign"（**完全移除二阶矩，只保留符号信息**）、MADGRAD 的 "Dual"（**用动量化对偶平均替代 Nesterov 动量**）、Adan 的 "Revise"（**重新表述 Nesterov 前瞻机制**）、ASGO 的 "Light"（**用单边预条件子降低显存**） |
| **Fig. 6** | 二阶方法 | 演化路径 |
| **Fig. 7** | 零阶方法 | 演化路径 |

> 📌 **Fig. 5 值得单独打印。** 它用九个方法把一阶方法的三条技术路线画清楚了：**改造自适应统计量（Adam→AdamW→Lion）、改造加速机制（Nadam→MADGRAD→Adan）、改造曲率近似（Shampoo→4bit-Shampoo→ASGO）**。这三个方向恰好对应 Sec 3.2 的八个主题——你能看出论文的组织逻辑是从这张图来的。

---

---

#### Sec 3.1 Unified Mathematical Perspective（p6–7）——全文最重要的一节

**这一节要解决什么问题：** 原文开篇说得很直白——"为了给比较现代优化器提供一个统一基础，**我们脱离传统的分类方式**，构造一个广义约束优化框架"。也就是说，它认为既有的"一阶/二阶/自适应/动量"这类标签不足以揭示方法之间的真实联系。

**统一框架。** 本综述讨论的**大多数方法**都可以写成如下离散时间动力系统的具体实例：

$$\tilde g_t = \mathcal E(f, \theta_t, \xi_t)$$

$$\hat g_t = \mathcal T_{\text{scenario}}(\tilde g_t)$$

$$m_t = \phi(m_{t-1}, \hat g_t)$$

$$\theta_{t+1} = P_\Theta\!\left(\theta_t - \eta_t M_t^{-1} m_t - \eta_t \lambda \theta_t\right)$$

其中 $\xi_t$ 表示随机数据批，$\eta_t$ 是第 $t$ 次迭代的步长，$\lambda$ 表示权重衰减。**以上四行合起来即原文 Eq. (7)。**

原文说这个统一表述"把优化过程解耦为**四个不同的维度**"，从而可以系统地归类现有方法的演化轨迹。

> 📌 **一个术语上的细节，值得留意：** 原文说"四个维度"，但实际出现的算子是**五个**——$\mathcal E$、$\mathcal T_{\text{scenario}}$、$\phi$、$M_t$、$P_\Theta$（$\lambda$ 是超参数不是算子）。再往下读会发现，论文自己的小节标题是"Gradient estimator E(·)""Preconditioner M_t""Scenario-aware transformation""Structural projection P_Θ(·)"，**偏偏没有单独一节讲动量 $\phi$**——$\phi$ 只在第二节 3.2.1 里作为"动量框架"被讨论。
> 所以我把它理解为：**论文的"四个"可能指 $\mathcal E / M_t / \mathcal T / P_\Theta$ 这四个结构性算子，而 $\phi$ 是第四行更新式内部的时间维组件。** 这个不一致原文没有澄清。$\Rightarrow$ 这不影响使用，但你在向别人介绍这个框架时最好说清你数的是哪几个，避免被问住。

#### 五个组成部分逐个拆解

**① 梯度估计器 $\mathcal E(\cdot)$ —— "目标景观信息如何获取"**

原文的定义是："该算子决定客观景观信息如何被获取。"

| 类型 | $\mathcal E$ 的形式 |
| --- | --- |
| **一阶** | $\mathcal E$ 给出标准随机梯度 $g_t$ |
| **零阶** | **绕过反向传播**，$\mathcal E$ 通过有限差分前向查询构造梯度。使用随机方向 $u_i \sim \mathcal N(0,I)$ 与平滑半径 $\mu$，通常形式化为 $\tilde g_t^{ZO} = \frac1q \sum_{i=1}^{q}\frac{f(\theta_t+\mu u_i)-f(\theta_t-\mu u_i)}{2\mu}u_i$ |

**② 预条件子 $M_t$ —— "在病态空间中扭曲梯度以加速收敛"**

原文定义："该矩阵扭曲梯度向量，以在病态条件空间中加速收敛。"

**一阶适应（First-order adaptation）**：$M_t$ 是一个**仅由一阶信息导出的算子或变换矩阵**，例如历史梯度统计量，或**梯度矩阵自身的固有几何结构**。

这里出现了全文最关键的一句判据：

> **"关键的是，使用经验 Fisher 信息矩阵（Empirical Fisher Information Matrix, EFIM, Gupta et al. 2018）的方法被归入这个一阶类别，因为它们本质上捕捉的是梯度方差，而非显式逼近真实的二阶曲率（Kunstner et al. 2019）。"**

这个范围**从严格的对角矩阵**（例如 Adam 中的 $M_t = \mathrm{diag}(\sqrt{v_t}+\epsilon)$）**一直到特定算子范数下的矩阵级结构归一化**（例如 Muon 中的谱正交化）——**全部都在不计算真实 Hessian 或 FIM 的前提下运作**。

**二阶算法**：$M_t$ **显式纳入高阶几何**，典型形式为 $M_t = \hat H_t + \gamma I$，其中 $\hat H_t \in \{\nabla^2 f(\theta_t), F_t\}$ 是 Hessian 或 FIM 的近似，$\gamma$ 是阻尼因子。

**③ 场景感知变换 $\mathcal T_{\text{scenario}}(\cdot)$ —— 把环境约束直接注入梯度流**

原文强调这是一个**非线性算子**，它"把环境约束直接引入梯度流，作为现代部署挑战的核心机制"：

| 场景 | $\mathcal T$ 的具体形式 | 含义 |
| --- | --- | --- |
| **隐私保护** | $\mathcal T(\tilde g_t) = \mathrm{Clip}(\tilde g_t, C) + \mathcal N(0, \sigma^2 C^2 I)$ | 通过**范数有界 + 高斯噪声注入**保证差分隐私 |
| **分布式优化** | $\mathcal T(\tilde g_t) = \mathcal C(\tilde g_t)$ | $\mathcal C(\cdot)$ 表示**有损压缩算子**（如量化、稀疏化），以缓解通信瓶颈 |

**④ 动量 $\phi(\cdot)$**：$m_t = \phi(m_{t-1}, \hat g_t)$。原文在 Eq. (7) 里给出，但把详细讨论放在 Sec 3.2.1。

**⑤ 结构投影 $P_\Theta(\cdot)$ —— 把更新映回结构流形**

原文："在高度受约束的场景中，更新必须被投影到特定的**结构流形**（structural manifolds）上。对显存高效优化器（例如低秩训练），$P_\Theta$ **把更新限制在低维子空间**，以规避稠密优化器状态的存储开销。"

#### 框架的定位声明（这段很重要，别误读）

原文主动加了限制：

> **"作为一个理论框架，Eq. (7) 并非意在成为适用于所有现有算法的通用收敛性证明框架，而是一种模块化解耦的视角。"**

它进一步说明：通过把复杂的优化轨迹抽象为四个结构算子 $(\mathcal E, \mathcal T, \phi, P)$，该框架**揭示了不同优化范式之间的结构等价性**；而识别这些等价性的同时，**也暴露了设计空间中的理论缺口**。研究者可以用这些洞察直觉地重组数学原语，指导下一代优化器的算法设计。

> 💡 **核心洞察（Sec 3.1）：这个框架的产出不是定理，而是"等价性 + 缺口"。**
> 请特别注意原文的自我限定——**它不声称能证收敛**。那么它的价值在哪？在于两件事：
> 1. **告诉你两个看起来毫不相干的方法其实是同一个算子的不同实例**（结构等价性）；
> 2. **告诉你哪些算子组合还没有人试过**（设计空间缺口）。
> $\Rightarrow$ **这是一种"坐标变换"式的贡献，不是"新定理"式的贡献。** 对数学背景的人来说，这个区分极其重要：这份综述的 Sec 3.1 不是一篇分析论文，它是**一张把算法空间参数化的地图**。你在这张地图上能做的是"找没人去过的格子"，而不是"证明格子里的事情"。
> 而它自己给出的那个最有价值的格子，就在下一段。

#### 一个具体的结构等价性：显存高效 ZO 与一阶单边预条件

原文明确点出了一处非平凡等价：

> **"考察结构投影 $P_\Theta$ 与预条件子 $M_t^{-1}$，会凸显出显存高效 ZO 方法（例如 LOZO（Chen et al. 2025b），它把 $P_\Theta$ 限制到低秩子空间）与一阶单边预条件方法（例如 ASGO（An et al. 2025），其 $M_t$ 使用低秩梯度分解）之间的联系。"**

**在这个框架下，两种方法执行的是同一个数学操作：它们在 $\mathbb R^d$ 空间上施加一个降维映射，以抵消对梯度估计器 $\mathcal E$ 或场景变换 $\mathcal T_{\text{scenario}}$ 的计算约束。**

原文由此给出一个方法论上的判断：**"与其把显存高效优化器当作孤立的启发式技巧，这个视角系统地表明它们是在 $M_t^{-1}$ 与 $P_\Theta$ 之间做出的联合折衷（joint compromises）。"**

> 💡 **核心洞察：LOZO 和 ASGO 是"同一个东西"——这句话值得停下来想一分钟。**
> - **LOZO** 是零阶方法：它没有梯度，靠有限差分估计，然后把更新限制在**低秩子空间**里（省显存）。
> - **ASGO** 是一阶方法：它有梯度，但预条件子 $M_t$ 用**低秩梯度分解**构造（也省显存）。
> 表面上：一个是"没有梯度的算法"，一个是"有梯度但预条件子便宜的算法"。看起来毫无关系。
> 但在这个框架的语言里，两者都在做**同一件事**：给 $\mathbb R^d$ 上的更新施加一个**降维映射**，从而绕开资源约束。
> $\Rightarrow$ **这就是"结构等价性"的实际含义，也是这份综述最实用的产出。** 它给你的不是"哪个算法好"，而是**"哪些算法其实是同一个想法的不同包装"**。掌握了这个，你在读一篇新论文时能立刻判断："哦，这不过是把 ASGO 的思路搬到了 ZO 域"，或者反过来"这个 ZO 方法其实可以用一阶预条件的成熟理论来分析"。
> **迁移性（transferability）就是从这种等价性里来的。**

#### 框架暴露出的理论缺口（全篇最像"可做的题目"的一段）

原文用框架推出了一个明确的研究方向：

> **"例如，尽管像 Shampoo（Gupta et al. 2018）这样的 Kronecker 分解方法在计算上是高效的，但它们对经验 Fisher 信息的依赖，限制了其预条件子 $M_t$ 只能捕捉梯度方差，而非真实的几何曲率。所提出的表述表明，这个限制可以通过重新设计梯度估计器与预条件子之间的交互来解决。"**

**具体的补救方向**（原文原话）：

> **"一个实践方向涉及把显存高效的梯度估计器用于 $\mathcal E$（例如那些利用 model-distribution sampling 来捕捉真实 Fisher 信息的方法），与结构化的或局部化的预条件子用于 $M_t$ 结合起来。研究这样的混合配置，提供了一条清晰的路径，去超越经验方法的方差适应极限，从而在严格显存约束下为大语言模型实现真正的曲率感知优化。"**

> 🎯 **把这段话拆成"缺口—原因—补法"三行，它就是一份选题提案：**
>
> | | 内容 |
> | --- | --- |
> | **现状** | Kronecker 类方法（Shampoo 等）计算高效，被广泛使用 |
> | **缺口** | 它们用的是**经验** Fisher，所以 $M_t$ 只能捕捉**梯度方差**，不是**真曲率** |
> | **框架的提示** | 问题出在 $\mathcal E$ 与 $M_t$ 的**耦合方式**，而不是各自单独不够好 |
> | **补法** | 用 model-distribution sampling 做**真 Fisher 的显存高效估计**（改 $\mathcal E$），配**结构化/局部化预条件子**（改 $M_t$） |
> | **意义** | 在显存受限的前提下，把"方差适应"升级为"真曲率感知" |
>
> $\Rightarrow$ **这是一个把 Sec 3.1（框架）、Sec 3.3（二阶）、Sec 3.7（显存高效）三块串起来的明确 gap。** 它满足一个好题目的几个条件：有明确的技术障碍（经验 vs 真 Fisher）、有框架指出的切入角度（改耦合而非改组件）、有实际动机（大模型显存受限）、有可验证的目标（在同等显存下达到更好的曲率近似）。
> **注意它还附带了一个理论问题**：真 Fisher 与经验 Fisher 的差距到底有多大？Kunstner et al. (2019) 那篇被反复引用的工作正是在讨论这件事。这个差距的定量刻画本身就是一个数学问题。

#### 框架的方法论转向

原文最后总结了这套视角的意义：

> **"通过 Eq. (7) 的透镜来审视现代优化器，我们把叙事从孤立的算法技巧转变为整体的工程协同设计（holistic engineering co-design）。后续各节将展开研究者如何系统地工程化 $\mathcal E$、$M_t$、$\mathcal T$、$P$，以在理论收敛性、硬件效率与系统约束之间的复杂权衡中导航。"**

---

#### Sec 3.2 First-Order Algorithms（p7–22）——一阶方法的八个主题

**原文对一阶方法的定义（严格照抄）：**

> **"我们把一阶（FO）方法定义为：仅利用一阶梯度信息（特别包括经验 FIM，它捕捉的是历史梯度方差而非显式逼近真实二阶曲率）来构造自适应变换或预条件算子 $M_t$ 的参数更新方案。"**

**动机。** 原文指出，尽管被广泛采用，**vanilla SGD 存在若干根本局限，包括对学习率选择的敏感性与噪声梯度下的不稳定性**。为应对这些挑战，研究社区发展出一个丰富的 FO 变体生态，从多个互补维度增强基础更新规则。本节把这些进展系统性地组织为**八个核心主题**，"每个主题代表一条独特的演化轨迹，但它们共同构成一个连贯的叙事：FO 算法如何演化以应对当代深度学习架构不断升级的需求"。

原文同时说明它做了两件事：追溯代表性算法的演化轨迹（Fig. 5），并系统评估其优势与局限（**Tab. 2**）。

#### 3.2.1 Accelerating Convergence Rate（p8–10）——动量的六个改进维度

**这一节的起点是一个自我批判。** 原文说：虽然动量技术显著加速了优化器收敛，**但传统动量框架在复杂优化景观中表现出固有局限**。

**动量方法的通用数学框架**（原文给出的标准形式）：

$$m_t = \beta m_{t-1} + \gamma g_t, \qquad \theta_t = \theta_{t-1} - \eta m_t$$

**为了超越这个标准表述的边界**，近期研究探索了几个核心改进维度。原文明确列举了这四个维度：

1. 动量重置与加权的调整
2. 计算前瞻与方向对齐的增强
3. 对偶动量机制的引入
4. **来自频域与动力系统的新分析视角**

下面按原文的六个小标题逐一展开。

**（1）定程动量重置（Scheduled momentum reset）**

**要解决的问题：** 在非平稳目标下，vanilla 动量方法存在**更新偏差**或**对嵌入层过拟合**的问题。原文指出这些问题的根源是"历史信息的不当累积"。

**统一的形式化方法：** 主动重置或选择性应用动量状态，概念上把更新修改为 $m_t = \gamma_t m_{t-1} + g_t$，**其中 $\gamma_t$ 充当二元或连续的衰减触发器**。

| 方法 | 核心机制 |
| --- | --- |
| **SRSGD**（Wang et al. 2022） | 通过**线性或指数调度**重置动量缓冲区，防止随机条件下 Nesterov 加速梯度的误差累积，从而实现简洁与高效 |
| **Adam-Rel**（Ellis et al. 2024） | 针对强化学习中的非平稳性，**重置时间步以平衡动量估计**，避免梯度突变导致的大幅更新 |
| **LazyOptimizer**（Su 2019） | 通过**零梯度检查**选择性地只更新活跃参数 |

**优势**：通过自适应动量重置增强优化器在动态任务中的适应性。
**固有局限**：**依赖启发式的重置触发器**，可能打断有效历史梯度的连续累积，并导致需要精细的超参数调优。

**（2）加速动量（Accelerated momentum）**

**统一推导视角：** 这类方法**在推断出的未来位置估计梯度**，记为 $g_t(\theta_{t-1} - \eta\beta m_{t-1})$，或者对标准表述施加**显式的偏差校正**。

| 方法 | 核心机制 |
| --- | --- |
| **adaNAPG**（Zhu et al. 2025a） | 把 Nesterov 加速梯度与**自适应采样**结合，以最优复杂度处理随机复合优化 |
| **SGDO**（Kopal et al. 2025） | **直接在"未来权重"处计算梯度**来改进更新方向，实现**零额外显存开销** |
| **RSGDM**（Qin et al. 2024） | 对**梯度**和**梯度的微分**同时施加指数移动平均，主动校正 SGDM 中的偏差与滞后 |
| **SQuARM-SGD**（Singh et al. 2021） | 把 Nesterov 动量与分布式学习中的局部随机梯度下降结合，**事件触发的通信充当对已通信梯度的条件校正** |
| **MADGRAD**（Defazio & Jelassi 2022） | 其动量实现**回避了梯度的指数移动平均**，使之非常适合稀疏模型 |

**优势**：通过前瞻估计获得更高阶的收敛性能。
**固有局限**：**每步计算复杂度增加**，且当未来梯度近似包含高方差时**可能不稳定**。

**（3）对偶动量机制（Double momentum mechanism）**

**要解决的问题：** 单一动量配置在复杂优化场景中的局限。

**统一的形式化推导：** 构造**次级动量缓冲区或递归估计器**，概念上形式化为 $m_t^{(2)} = f(m_t^{(1)}, g_t)$，以系统性地降低方差与偏差。

| 方法 | 核心机制 |
| --- | --- |
| **$\mu^2$-SGD**（Dahan & Levy 2025） | 把 **anytime averaging 与修正动量**结合，达到最优收敛率，并**支持固定学习率** |
| **AdEMAMix**（Pagliardini et al. 2025） | 使用**快慢两组指数移动平均的混合**，通过调度器自适应平衡梯度相关性，缓解训练中的模型遗忘 |
| **YOGI**（Zaheer et al. 2018） | 对二阶矩累加器采用**带符号且有界的增量式自适应更新**，防止有效学习率突变 |
| **MARS**（Yuan et al. 2025） | 通过**缩放随机递归动量**把方差缩减与自适应方法统一起来 |

**优势**：显著提升优化鲁棒性与梯度估计精度。
**固有局限**：**引入额外显存开销**来存储多个动量状态，且**平衡快慢移动平均之间的交互**是个难题。

**（4）动量-梯度对齐（Momentum-gradient alignment）**

**要解决的问题：** vanilla 动量方法中持续存在的**锯齿效应（zig-zag effect）**与次优收敛路径。

**形式化机制：** 依据**历史动量 $m_{t-1}$ 与当前梯度 $g_t$ 之间的余弦相似度或角度度量**来调节步长或更新向量。

| 方法 | 核心机制 |
| --- | --- |
| **AngularGrad**（Roy et al. 2021） | 利用**连续梯度角度**控制步长，从而减少锯齿效应、保证平滑收敛路径 |
| **Cautious Optimizer**（Liang et al. 2024） | 修改动量方法，**严格对齐更新方向与梯度**，保持收敛保证，且**可在一行代码内实现** |

**优势**：通过防止历史更新偏离当前优化趋势，有效改善收敛平滑度与速度。
**固有局限（这条很有洞察力）**：**强制严格对齐可能丢弃历史动量在狭窄峡谷（narrow ravines）中提供的有益加速，可能减慢从浅层局部极小值中逃逸的速度。**

> 💡 **核心洞察（动量-梯度对齐的局限）：这是一个经典的"稳健性与探索能力"权衡。**
> 对齐的直觉是好的：如果当前梯度说"往东"，而动量说"往西"，那动量可能在拖后腿。所以强制对齐 = 减少震荡。
> 但原文指出代价：**在狭窄峡谷型景观中，恰恰是那个"方向不一致"的动量分量，提供了沿峡谷快速前进所需的加速度。** 把它掐掉，收敛轨迹变平滑了，但也**更容易卡在浅层局部极小值里出不来**。
> $\Rightarrow$ **平滑 ≠ 更好。** 这是深度学习优化里反复出现的模式：**任何用来抑制噪声/震荡的机制，同时也在抑制探索。** 梯度裁剪（Sec 3.2.4）、对齐（这里）、小学习率（Sec 4.3.1）都有这个双重性。

**（5）动态动量权重（Dynamic momentum weight）**

**要解决的问题：** 固定动量系数的约束。

**统一推导：** 把静态系数替换为**动态函数**，状态按 $m_t = \beta_t m_{t-1} + (1-\beta_t) g_t$ 计算，其中 **$\beta_t$ 严格适应当前梯度变化**。

| 方法 | 核心机制 |
| --- | --- |
| **DEAM**（Bai et al. 2020） | 用**历史动量与当前梯度之间的判别角**计算自适应权重，采用**回溯项**限制冗余更新，且相比 Adam 的机制**减少了超参数** |
| **SGDF**（Yao et al. 2023b） | 借助 **Wiener 滤波器**原理动态调整梯度估计，在**噪声抑制与信号保留之间**最小化均方误差 |

**优势**：通过量身定制的动量调整增强收敛与泛化。
**固有局限**：**每次迭代评估动态权重函数带来计算开销**，且存在**系数过度衰减导致动量消失**的风险。

**（6）频域动量分析（Frequency domain momentum analysis）——最有数学味道的一类**

**核心思想：** 把动量操作概念化为频域中的**时变滤波器 $H(\omega, t)$**，由此获得解释与优化动量方法的新视角。

**原文引用的关键结论（Li et al. 2025b）：**

> **"高频成分在优化的后期阶段变得有害，而低频信号需要被放大。"**

**FSGDM**（Li et al. 2025b）据此动态调整动量滤波特性。

**优势**：这一分析范式能够**系统地隔离并抑制有害的噪声频率**。
**固有局限**：在高度非线性的深度神经网络中，**把时域梯度映射到频域滤波器在理论上是复杂的**，使实际硬件实现变得困难。

> 💡 **核心洞察（频域视角）：动量就是一个低通滤波器。**
> 这个视角值得单独记下来，因为它把一个"工程技巧"翻译成了你熟悉的信号处理语言：
> - **动量累积 = 低通滤波。** 指数移动平均 $m_t = \beta m_{t-1} + (1-\beta) g_t$ 在频域上就是传递函数 $H(\omega) = (1-\beta)/(1-\beta e^{-i\omega})$——一个低通滤波器。$\beta$ 越接近 1，截止频率越低，滤得越狠。
> - **原文的结论**"高频有害、低频需要放大"翻译过来就是：**训练后期的梯度里，高频成分主要是噪声（mini-batch 采样噪声、非平稳震荡），低频成分才是真实的下降信号。所以应该加强低通。**
> - $\beta$ 于是有了物理意义：**它就是滤波器的截止频率参数。** 所谓"动态动量权重"（第 5 类）就是在做**自适应滤波**；所谓"动量重置"（第 1 类）就是**周期性重启滤波器以清除积累的相位滞后**。
> $\Rightarrow$ **这个映射把一个有 6 个改进维度的杂乱领域，压缩成一个滤波器设计问题。** 这是一个值得你认真对待的视角——它可能是这份综述里最容易被数学背景的人直接接手的入口，因为**滤波器设计是成熟的数学**。
>
> 顺带一提，这条思路与你感兴趣的方向有交集：频域分析天然关心**梯度的谱结构**，而"对称性与表示学习""特征学习动力学"关注的也是谱。这两条线在"损失景观的谱性质"这个点上是可以接起来的。

**（7）动量阻尼机制（Momentum damping mechanism）**

**要解决的问题：** 大规模机器学习任务中的收敛不稳定与过冲。

**核心思路：** 把优化过程建模为**受控的物理或动力系统**。统一的形式化方法是引入阻尼因子或反馈控制器，概念上把标准离散更新改造为稳健的微分方程表示：

$$\ddot\theta + \gamma\dot\theta + \nabla f(\theta) = 0$$

| 方法 | 核心机制 |
| --- | --- |
| **PIDAO**（Chen et al. 2024）与 PID 类方法（An et al. 2018） | 把传统 SGDM 视为**受控的重球系统**，引入由**梯度历史驱动的 PID 控制器**来抑制过冲 |
| **FANoS**（Dhiman 2025） | 把动量更新表述为一个**离散化二阶动力系统**，并引入**恒温器（thermostat）作为积分控制器** |
| **VRAdam**（Vaidhyanathan et al. 2025） | 通过**动态学习率**抑制过大的速度 |
| **AdamP**（Heo et al. 2021） | 从几何上**移除造成不合理权重范数增长的径向动量分量** |
| **SNGM**（Zhao et al. 2024c） | 在动量累积**之前**对梯度做归一化，从而**消除对 batch size 的依赖** |

**优势**：对参数演化起到关键的稳定作用。
**固有局限**：**引入复杂的控制动力学，可能需要精确调节阻尼系数以防止过早收敛。**

> 📌 **FANoS 值得多看一眼，因为它的类比很彻底。** 把 SGD 看成重球在势能面 $f$ 上滚动的物理系统，$\gamma$ 是摩擦/阻尼系数。那么：
> - 欠阻尼（$\gamma$ 太小）→ 在峡谷里来回震荡（overshoot）；
> - 过阻尼（$\gamma$ 太大）→ 爬得慢，且容易陷在局部极小值。
> 而 **PID 控制器**正是工程上解决"如何把系统稳定在目标附近"的标准工具。FANoS 更进一步引入"恒温器"——这暗示了一个更有趣的图景：**把训练过程看成一个热力学系统，学习率类似于温度，退火（annealing）就是降温。** 这与你清单里"流形上的最速下降"那组苏剑林博客在精神上是相通的。

**（8）这一类方法的统一收敛理论与它的失效边界**

原文在这一节末尾给出了一个**贯穿全部改进维度的理论总结**：

> **"关于理论收敛分析，横跨这些多样维度的统一数学框架通常通过严格界定动量估计器的方差，保证最优收敛复杂度——例如对非凸目标的 $O(1/\sqrt T)$。"**

但紧接着给出关键的限制：

> **"然而，这些方法的理论边界与局限往往假设梯度有界或特定的光滑性条件。这些数学假设在现代深度学习架构的高度非凸损失景观中频繁被违反，从而限制了理论保证完美转化为实证成功。"**

> 💡 **核心洞察（理论保证为何经常失效）：请注意原文的因果结构。**
> 它的论证不是"理论错了"，而是"**理论是对的，但它的前提在真实场景中不成立**"。
> 具体是哪些前提？**梯度有界性**和**光滑性（Lipschitz 梯度）**。而在现代架构上：
> - **梯度有界性**：注意力机制里梯度可能剧烈尖峰，"有界"这个假设的界可能极大，使得 $O(1/\sqrt T)$ 里的常数完全失去意义；
> - **光滑性**：$L$-光滑要求 Hessian 谱有上界，而 Transformer 的损失景观在不同层、不同方向上尺度差异巨大（这正是 Sec 4.3.4 说的"高度各向异性"）。
> $\Rightarrow$ **把"常数"和"谱结构"带进理论分析，是这个领域真正的开放问题。** 现有的 $O(1/\sqrt T)$ 类结论是**最坏情况**保证，而最坏情况在实践中从不发生——训练实际走的路径比理论允许的好得多。**"为什么实践中远比最坏情况好"本身就是一个未被充分回答的问题。**

**（9）这一节的核心 gap（原文明确点出）**

> **"尽管有这些全面的进展，现有研究的核心缺口仍然是：缺乏一个通用的、无需超参数的动量框架（a universal, hyperparameter-free momentum framework）。当前的方法论仍然难以在不引入难以承受的显存开销或计算成本的前提下，自动识别并适应损失景观的谱性质（spectral properties）。未来的轨迹必须弥合严格的连续时间动力系统模型与经验优化的离散、高度随机本质之间的鸿沟。"**

**原文归纳的范式转移：**

> **"在核心设计范式与演化逻辑上，该领域见证了一次里程碑式的范式转移：从静态的、启发式的动量累积，转向动态的、反馈驱动的动量调制。这条演化轨迹从基础的标量调整，过渡到复杂的几何、物理与频域干预。"**

> 🎯 **把这段 gap 加上前面的频域视角，一个完整的选题就浮现了：**
> 1. 频域分析（Li et al. 2025b）已经指出"高频有害、低频有益"，并暗示最优滤波器应该随训练阶段变化；
> 2. 但原文说，**目前没有"无需超参数、能自动适应损失景观谱性质"的框架**；
> 3. 而频域/谱的方法目前的障碍是"时域梯度到频域滤波器的映射在理论上是复杂的"。
> $\Rightarrow$ **这三条合起来就是一个具体的数学问题：能否为损失景观的谱演化建立一个可分析的模型，并从中导出最优的（或自适应确定的）动量滤波策略？** 这需要的是随机过程 + 谱分析 + 非凸优化的混合工具——**正好是数学系的强项区**。而且它自带一个"无需超参数"的强动机，不是又一个调参技巧。

#### 3.2.2 Adaptive Step-Size Control（p10–14）——分母的军备竞赛

**这一节要解决的问题：** 原文开篇说明——自适应学习率被广泛用于**降低手工超参数调优的成本**。

**通用更新框架：**

$$\theta_{t+1} = \theta_t - \eta_t \frac{\phi(g_{1:t})}{\psi(g_{1:t})}$$

**其中 $\phi(\cdot)$ 表示一阶动量，$\psi(\cdot)$ 表示二阶预条件子。**

**原文对 Adam 的评价（这是本节全部后续工作的出发点）：**

> **"虽然像 Adam 这样的经典优化器为这个范式奠定了基础，但它在极端步长和复杂景观中的不收敛方面表现出局限。"**

**后续进展的组织方式：** 原文说后续进展"系统性地沿结构维度演化"——四个方向：

1. **精化时间矩估计（refining temporal moment estimation）**
2. **扩展空间适应粒度（expanding spatial adaptation granularity）**
3. **解耦架构组件（decoupling architectural components）**
4. **最小化状态开销（minimizing state overhead）**

**共同目标**（原文原话）：**"这些发展共享一个统一目标：在非凸设定下界定梯度方差，同时确保理论收敛。"** 为解决梯度序列的**时间动态**，优化主要针对 $\phi(\cdot)$ 与 $\psi(\cdot)$ 的内部构造。

下面按原文的七个具体做法展开。**请注意这一节的写法——它不是在介绍七个算法，而是在介绍对同一个公式的攻击面。**

**（1）偏差校正规则适应（Bias correction rules adaptation）**

**目标：** 通过修改早期阶段的估计器，缓解初始训练的不稳定性。

| 方法 | 机制 | 代价 |
| --- | --- | --- |
| **AdamD**（John 2021） | **移除一阶矩估计上的偏差校正**，产生更小的初始更新，从而稳定 warm-up 阶段 | 增强早期与梯度几何的对齐，但**对后期收敛影响边际** |

**（2）二阶矩适应（Second-order moment adaptation）**

**目标：** 界定极端步长，解决方差估计的不收敛问题。**大量策略重新表述 $\psi(\cdot)$ 的指数移动平均。**

| 方法 | 对分母的具体改造 |
| --- | --- |
| **NosAdam**（Huang et al. 2019a） | **提高历史梯度的权重** |
| **ADOPT**（Taniguchi et al. 2024） | **消除对衰减率 $\beta_2$ 的依赖** |
| **AdamNX**（Zhu et al. 2025c） | **自适应调整指数衰减率** |
| **SET-Adam**（Zhang 2024） | **校准矩以防止无穷小的步长** |
| **HomeAdam**（Huang et al. 2026a） | **去掉二阶矩中的平方根**，以界定不稳定的步长并改善泛化 |
| **DP-AdamBC**（Tang et al. 2024） | 纳入差分隐私校正 |
| **HAdam**（Jiang et al. 2019） | 引入高阶矩 |
| **AdamPower**（Wang et al. 2025d） | 符号-幂变换 |
| **MADAM**（Zhu et al. 2021） | 动态加权 |
| **M-SVAG**（Balles & Hennig 2018） | 直接方差适应 |
| **VSGD**（Kuzina et al. 2025） | 概率化泛化 |
| **AdaLomo**（Lv et al. 2023） | 显存高效分解 |

**优势**：严格稳定了收敛轨迹。
**固有局限**：**本质上引入了对辅助超参数的更高敏感性。**

**（3）动态 epsilon 调整（Dynamic epsilon adjustment）**

**目标：** 在不引入辅助开销的前提下进一步精化预条件子——**动态调整稳定项 $\epsilon$**。

| 方法 | 机制 |
| --- | --- |
| **EAdam**（Yuan & Gao 2020） | **把加性项直接施加到未校正的累加器上**，从而隐式地减小小矩的步长，在多种领域中优于 Adam / RAdam / LOZO 等基线 |
| **FAdam**（Hwang 2024） | 基于**黎曼几何**的自然梯度校正来整合自适应调整 |

**这类方法的共同点**：有效防止除零异常；**但它们的理论边界往往依赖经验近似。**

**（4）预测偏差适应（Prediction deviation adaptation）**

**核心思想：** **用观测梯度与预测梯度之间的差异来替代标准的梯度方差。**

| 方法 | 机制 |
| --- | --- |
| **AdaBelief**（Zhuang et al. 2020） | 用 $g_t - m_t$ 的平方（"信念"偏差）替代 $g_t^2$，缩放更新方向，实现快速收敛与强泛化 |
| **Aida**（Zhang et al. 2023a） | 扩展这种基于信念的缩放，**利用一阶动量投影来估计二阶动量**，严格抑制步长范围 |

**框架意义**：**这个框架最优地把学习率与梯度不确定性耦合起来**，但它**仍然对高噪声随机批敏感**。

**（5）基于动量的适应（Momentum-based adaptation）**

**核心思想：** 整合动量机制来改造 $\phi(\cdot)$，通过**相移累积（phase-shifted accumulations）**加速收敛。

| 方法 | 机制 |
| --- | --- |
| **Nadam**（Dozat 2016）、**Adan**（Xie et al. 2024b） | 纳入 Nesterov 动量 |
| **RAdam**（Liu et al. 2020a） | 校正早期更新的方差 |
| **Adam+**（Liu et al. 2020b） | 使用外推的一阶矩 |
| **QHAdam**（Ma & Yarats 2019） | 拟双曲项 |
| **MoMo-Adam**（Schaipp et al. 2024） | 自适应速率模型 |
| **Lion**（Chen et al. 2023） | **隔离动量的符号拓扑，从而消除二阶依赖** |
| **AdaInject**（Dubey et al. 2022） | 二阶注入 |
| **EAGLE**（Fujimoto & Nishi 2025） | 交替曲率阈值 |
| **MARS**（Yuan et al. 2025） | 递归缩放 |
| **ROOT**（He et al. 2025b） | Newton-Schulz 软阈值 |
| **INNAprop**（Shin et al. 2025b） | 把结构信息与 RMSprop 融合 |

**优势**：显著加速优化轨迹。
**固有局限（关键）**：**历史动量的积累可能与当前梯度场动态不一致，从而在复杂或非平稳损失景观中引入更新偏差。**

**（6）空间粒度的扩张（Spatial granularity）**

原文在这里有一个明确的转折句：**"除时间累积之外，适应的空间粒度也从标量参数级更新转向更广的结构几何。"** 也就是适应的**对象从时间维度转到了空间维度**。分三层：

**① 逐层适应（Layer-wise adaptation）**

**要解决的问题：** **统一学习率无法适应深度网络的结构异质性与各向异性缩放。**

| 方法 | 机制 |
| --- | --- |
| **LARS**（You et al. 2017）、**LAMB**（You et al. 2020） | 通过**逐层归一化**缩放更新，以支持超大 batch 训练，**并提供非凸收敛保证**（在 AlexNet、ResNet-50、BERT 上验证） |
| **NovoGrad**（Ginsburg et al. 2019） | 通过**逐层范数**把显存需求减半，同时解耦权重衰减 |
| **AdaL**（Zhang et al. 2021） | 在累积**之前**变换原始梯度 |
| **Coupled Adam**（Stollenwerk & Stollenwerk 2025） | 通过强制统一二阶矩，解决 Adam 的各向异性嵌入问题 |
| **Muon**（Jordan et al. 2024） | **转入矩阵级正交化：使用 Newton-Schulz 迭代来标准化各层更新的谱范数** |
| **CaAdam**（Genet & Inzirillo 2024） | 通过多策略缩放进一步整合结构深度与连通性 |

**优势**：有效解决异构架构中的梯度消失问题。
**固有局限（这条很关键）**：**"它们从根本上假设单层内的梯度分布是一致有界的。"**

> 💡 **核心洞察（逐层适应的隐含假设）：这是你的追踪清单里"逐层梯度异质性"那个开放问题的原文出处之一。**
> 原文自己承认：LARS/LAMB 这一整类逐层归一化方法，**底层假设是"同一层内部的梯度分布是一致的"**。
> 但真实情况是什么？**同一层内部（比如一个大的权重矩阵）的梯度分布本身可能高度异质**——不同输出通道、不同输入通道的有效学习率需求可能差几个数量级。
> $\Rightarrow$ **"逐层"这个粒度可能根本就不够细。** 这就自然导向下一层：
>
> **② 神经元级适应（Neuron-level adaptation）**
>
> **AdaAct**（Seung et al. 2024a）：**根据激活方差的平方根反比调整梯度更新。** 关键设计是——**它使用激活方差的指数移动平均，而不是梯度方差**。这样做的效果是：**处理相同输入特征的参数共享学习率。**
> **优势**：在激活层面隔离方差，以最大化输出稳定性。
> **固有局限**：**对宽度很大的网络，所需显存开销巨大。**
>
> **③ 解耦学习率与适应性（Decoupled learning rate and adaptability）**
>
> 原文的诊断：**"标准优化器经常把全局步长与局部适应性混为一谈（conflate）。"**
> **AvaGrad**（Savarese et al. 2021）**显式解耦这两个组件**，以在多种模态上优化性能，提供更强的任务特定调优能力。
> **优势**：这种隔离防止了**全局收敛率的退化**。
> **固有局限**：**本质上扩张了超参数搜索空间。**
>
> $\Rightarrow$ **把这三层放在一起看，就是一条清晰的"粒度扩张"路线：标量（全局）→ 逐层 → 神经元 → 参数。** 而原文对每一层都给了同一个代价结构：**粒度越细，表达力越强，但显存/超参数开销越大。** 这条张力线在 Sec 3.7（显存高效）里会以另一种形式再次出现——那里要解决的就是"粒度细带来的开销"。

**（7）混合适应策略（Hybrid adaptive strategy）**

**核心思想：** 通过融合不同的适应准则，建立广义优化框架。

| 方法 | 机制 |
| --- | --- |
| **AdaSGD**（Wang & Wiens 2020） | 把 SGD 的统一轨迹与自适应缩放融合 |
| **EXAdam**（Adly 2024） | 引入矩相互作用的去偏项 |
| **APOLLO**（Zhu et al. 2025b） | 通过**结构化投影**降低显存 |
| **S3**（Peng et al. 2025） | 用 $p$ 阶动量与 Nesterov 加速强化理论边界 |
| **ACProp**（Zhuang et al. 2021） | 异步二阶矩中心化 |
| **DiffGrad**（Dubey et al. 2019） | 梯度差分步长 |
| **AdamW**（Loshchilov & Hutter 2019） | **隔离参数正则化**（把权重衰减从梯度更新中解耦） |
| **LaProp**（Ziyin et al. 2020） | 把自适应性与符号方法插值 |
| **AdaMuon**（Si et al. 2025） | **把方差适应性纳入正交更新** |
| **AdaFamily**（Fassold 2022） | 通过超参数混合建立**连续算法谱** |

**优势**：同时缓解多个失效模式。
**固有局限**：**由此产生的计算复杂度，以及理论收敛分析的不可处理性（intractability）。**

**（8）无状态适应（Stateless adaptation）**

原文的过渡句：**"随着模型维度指数级增长，维护历史状态的显存开销使得最优近似策略成为必需。"**

| 方法 | 机制 |
| --- | --- |
| **AlphaGrad**（Sane 2025） | 采用 **L2 归一化与双曲变换**，配以非凸证明 |
| **AutoDrop**（Wang et al. 2024c） | 利用**角速度饱和**来调整速率，无需额外超参数 |
| **AdamS**（Zhang et al. 2025a） | **用"动量-当前梯度"分母替代时序方差**，从而**达到 SGD 的显存效率，同时继承 AdamW 的配置** |
| **AEGDM**（Liu & Tian 2022） | 把动量与变换后的梯度和融合，确保相对基线的能量稳定性 |

**优势**：这些设计**逼近一阶方法的显存极限，同时保留自适应性**。
**固有局限**：**严格依赖"局部平稳梯度分布"的假设。**

**（9）Kalman 滤波类方法**

**RLEKF**（Hu et al. 2023）：重新组织网络层以适应 **Kalman 滤波的协方差追踪**，**用稀疏对角块结构近似稠密误差矩阵**。这高效利用了二阶协方差信息；**但稀疏近似的截断误差会随深度架构不可避免地累积。**

> 📌 **RLEKF 值得注意，因为它换了一套数学语言。** Adam 的 $v_t$ 可以看成对梯度方差的粗略估计；而 Kalman 滤波给出的是**协方差的最优递推估计**（在系统模型正确的前提下）。所以 RLEKF 的野心是：**把优化问题形式化为状态估计问题，从而用 Kalman 滤波的成熟理论来做预条件。** 它的瓶颈也很清楚——**Kalman 滤波的协方差矩阵是 $d \times d$ 的，对现代模型完全不可承受**，所以必须稀疏化，而稀疏化带来截断误差累积（且随深度放大）。这个"精度 vs 可行性"的矛盾是二阶方法的老问题。

**（10）这一节的范式总结（原文的收束判断）**

> **"自适应步长控制的演化展示了一个明确的范式转移：从时间标量校正，到空间结构优化，最终走向显存高效的几何近似。早期方法论主要把优化景观概念化为一系列独立的历史梯度，专注于通过一阶和二阶矩的修正来界定方差。随着网络架构……"**

> 💡 **核心洞察（Sec 3.2.2 的三段式）：把这条总结记住，它就本节全部内容的压缩。**
>
> | 阶段 | 优化对象 | 代表 | 隐含的景观模型 |
> | --- | --- | --- | --- |
> | **① 时间标量校正** | 分母 $\psi$ 这个标量统计量 | AdaGrad → RMSprop → Adam → NosAdam/ADOPT/AdamNX | 景观被看作**一维时间序列**：一系列独立的历史梯度 |
> | **② 空间结构优化** | 逐层 / 神经元 / 矩阵结构 | LARS / LAMB / AdaAct / Muon | 景观被看作**有空间结构的对象**：不同层、不同神经元、不同矩阵方向尺度不同 |
> | **③ 显存高效几何近似** | 在压缩状态下近似几何 | AdamS / AlphaGrad / APOLLO / 低秩类 | 承认**真实几何不可承受**，转而求"够用的近似" |
> $\Rightarrow$ **三个阶段对应三种越来越精细的"景观模型"。** 这是本节最有价值的框架——它把 §3.2.2 里那几十个方法名压缩成了一条三行的演化逻辑。你不需要记住所有方法名，但**必须记住这三个阶段和它们各自的隐含假设**。
>
> 顺带说：**阶段③ 的"够用的近似"正是 Sec 3.1 那个 gap 的另一面。** 综述说"改为真 Fisher 的显存高效估计 + 结构预条件"，本质上就是想在阶段③ 的可行性约束下，拿回阶段② 的几何表达力。**两个缺口是同一个缺口。**

**（11）本节的隐含 open problem**

原文虽未用"open problem"字样收束本节，但从它反复标注的固有局限可以提炼出三个：

1. **超参数敏感性转移而非消失**：几乎每个"改进"方法都被标注"引入对辅助超参数的更高敏感性"。**自适应方法的初衷是减少调参，但演化出的新方法往往调参更难。**
2. **历史动量与当前梯度场的动态不一致**（第 5 类）——在非平稳景观中引入更新偏差。
3. **逐层假设的"层内一致有界"**（第 6 类）——与 Sec 4.3.4 的"高度各向异性"直接冲突。

---

---

> 说明：本页为双栏排版 PDF 的抽取文本，原始抽取结果左右栏交错拼接。以下讲解已按语义重建阅读顺序，把被割裂的句子接回原栏；个别公式符号在原抽取中损坏（如下标错行、花体字母丢失），已按上下文给出可读写法并标注。

---

#### 3.2.3 方差适应（Variance Adaptation）

**要解决的问题。** 经典一阶（first-order, FO）算法把参数拉平成向量，因而**丢弃了参数自身的结构信息**：一个 $m \times n$ 的权重矩阵与长度 $mn$ 的向量被同等对待。本节的预条件（preconditioning）方法正是为了把结构信息找回来。

**通用数学框架。** 论文把预条件更新统一写成 $\theta_{t+1} = \theta_t - \eta P_L \nabla J(\theta_t) P_R$，即梯度被左右两个矩阵各乘一次。作者指出该式可纳入全文统一框架 Eq. (7)：在那里预条件子 $M_t^{-1}$ 被实例化为左右乘算子 $P_L \cdot P_R$。

**关键的概念转变。** 传统上这类方法被解释为「通过近似 Hessian 做曲率适应」。但论文援引 Kunstner et al. (2019) 指出，经验 Fisher 信息矩阵（EFIM）主要刻画的是梯度的**非中心二阶矩**。因此预条件技术应被更准确地理解为在做**方差适应**：在随机优化中抑制梯度噪声，同时保留参数结构。由此得出四条核心改进维度：高效构造预条件矩阵、压低计算开销、在优化效率与潜在泛化性之间取平衡。全节演化逻辑是**从复杂的分维度（dimension-wise）预条件走向高效的单度量近似，以缓解严重的资源瓶颈**。

**双度量（two metrics）预条件子。** 最早的设计范式用两个度量，典型做法是分别近似**行协方差与列协方差矩阵**（原文所说「Kronecker 分解的分维度预条件」即在此语境）。

- **Shampoo**（Gupta et al. (2018)）：开创性的在线结构感知算法，为每个维度分别维护由二阶梯度统计量构造的预条件子，收敛快于传统优化器而性能相当。局限是内存占用高。
- **4-bit Shampoo**（Wang et al. (2024f)）：采用**特征向量矩阵量化**技术降低存储成本。
- **PSGD**（Li (2017)）：提出对预条件子估计的**噪声鲁棒判据**，使预条件后梯度的扰动与参数的扰动相均衡。
- **SPlus**（Frans et al. (2025)）：增强 Shampoo 框架的稳定性，机制包括 **instant-sign normalization（瞬时符号归一化）**、**iterate averaging（迭代平均）**、适应网络宽度的**形状感知缩放**，并把**有界更新**与**历史特征基（historical eigenbases）**结合，使较低频率的矩阵求逆下仍然稳定。

理论上这类双度量预条件子能高效近似自然梯度下降，在高度非凸景观中加速收敛。**固有局限**：为大规模模型存储并求逆协方差矩阵的内存开销，直接限定了其适用边界。

**单度量（single metric）预条件子。** 为应对上述资源需求，后续研究转向统一使用单个结构代理来调整更新步，以平衡效率与性能。

- **ASGO**（An et al. (2025)）：使用**单侧预条件子**并保留矩阵结构，利用**低秩梯度**降低计算开销，同时保持与 Gupta et al. (2018) 相当的收敛性质。
- **NYSACT**（Seung et al. (2024b)）：对协方差估计采用**特征值平移的 Nyström 近似**以实现可扩展估计；通过提高估计精度、降低资源消耗，弥合 FO 与二阶（SO）算法之间的差距。
- **Hessian-aware scaling**（Smee et al. (2025)）：依据**局部曲率**调整梯度以保证单位步长。理论分析表明这些方法改善了优化景观的**条件数**，在凸假设下以显著更低的内存实现更快收敛。

**固有局限**：单度量方法**捕捉维度间复杂关联的能力下降**，无法完整表达双度量设计所具备的相关性。

**小结与 gap。** 曲率近似的设计范式已从 Kronecker 分解的分维度预条件，回到精简的单矩阵预条件，驱动力是现代架构对极端可扩展性的需求。双度量长于结构感知、短于内存；单度量长于轻量、短于曲率表达。论文承认的核心缺口是：**缺少既完全动态、又对硬件友好的预条件子**——它应达到双度量的表达能力，同时保持单度量的算法效率，既不依赖复杂超参数调优，也不在激进量化下出现精度退化。

> **抽取受损提示**：本节右栏在页 13/14 交界处丢失少量文字——关于预条件矩阵构造与每步运行时开销的过渡句，以及双度量与单度量对比中「其固有局限是内存占用高／单度量的优势是计算门槛低、内存高效」之间的衔接句；「ASGO」前的引入语亦不完整。这些位置的语义可由上下文唯一确定，但原句措辞无法逐字复原。

#### 3.2.4 提升训练稳定性（Enhancing Training Stability）

**要解决的问题。** 针对梯度爆炸、方差失衡等与梯度相关的优化难题，当代稳定化方法可统一在一个框架下：**通过函数变换调制梯度更新步**。这些变换的改进维度按**处理粒度**与**统计自适应方式**划分，演进路线是**从静态约束走向动态的、分布感知的校准**。

**Basic fixed gradient clipping（基础固定梯度裁剪）。** 限制梯度幅度是在重尾噪声下稳定收敛、并满足隐私等约束的基本手段。

- **Clipped-SGD**（Gorbunov et al. (2020)）：加速型随机方法，用**固定梯度裁剪**抑制重尾噪声。
- **DP-SGD**（Yang et al. (2022)）：把梯度裁剪与**高斯噪声注入**结合，用于差分隐私的非凸优化。

二者的共同特征是使用**预先给定的标量阈值**限制梯度范数。**优点**：噪声鲁棒性强、隐私界严格。**固有局限**：依赖人工调参的静态阈值，常引入梯度偏置，妨碍最优收敛。

**DP-enhanced gradient clipping（差分隐私增强裁剪）。** 为修正刚性截断造成的**有偏优化轨迹**，此法在传统裁剪上叠加**误差修正**框架。

- **DiceSGD**（Zhang et al. (2024b)）：引入**误差反馈（error feedback）**机制消除差分隐私随机梯度下降中的裁剪偏置；允许裁剪阈值与具体问题参数解耦，并通过更新的收敛分析保持严格的效用与隐私保证。**优点**是把隐私阈值与优化效用做了理论解耦；**固有局限**是持续跟踪并累积误差反馈状态带来的额外计算开销。

**Dynamic gradient clipping（动态梯度裁剪）。** 从静态阈值转向自适应阈值，依据**历史梯度统计量**调整约束，以适应不同参数敏感度与突发尖峰。

- **Stable-SPAM**（Huang et al. (2025a)）：在 Adam 上增加 **AdaClip**，用指数滑动平均导出**逐元素（entry-wise）裁剪**。
- **AdaGC**（Wang et al. (2025b)）：采用**逐参数**滑动平均阈值，与既有优化器广泛兼容。

两者共享同一推导：用历史动量决定**局部裁剪边界**。**优点**：跨架构、跨模态普遍适用。**固有局限**：损失景观发生快速拓扑变化时，阈值调整可能**滞后**。

**Spike-aware gradient clipping（尖峰感知裁剪）。** 面向标准动态调整无法响应极端异常的非凸场景，专门针对**突变**。

- **SPAM**（Huang et al. (2025b)）：把尖峰感知裁剪直接集成进 Adam，缓解梯度尖峰的冲击。**优点**：针对稀疏异常定向增强大规模训练稳定性。**固有局限**：适用面窄，主要只在梯度方差极端的区间有效。

**Element-wise gradient scaling（逐元素梯度缩放）。** 越过幅度截断，**逐个调整梯度元素**，解决统一缩放的不足。

- **SPAM**（Huang et al. (2025b)）：把**动量重置、尖峰感知裁剪、稀疏动量**一并集成进 Adam，既缓解尖峰又优化内存。
- **AdaNorm**（Dubey et al. (2023)）：用**过去范数的指数滑动平均**校正单个梯度，保证优化一致性。
- **pbSGD**（Zhou et al. (2020)）：用 **powerball 函数**实现灵活缩放，并给出非凸目标下的理论收敛保证。

**优点**：单个参数更新获得最大灵活性。**固有局限**：为每个参数维护独立统计状态，内存开销可观。

**Layer-wise gradient normalization（逐层梯度归一化）。** 在**宏观分组层面**归一化梯度，以平衡稳定性与内存效率。

- **Stable-SPAM**（Huang et al. (2025a)）：引入 **AdaGN** 计算**逐层梯度范数**的滑动平均，用「历史均值与二阶矩根之比」重缩放当前梯度以压制范数爆炸。
- **MultiAdam**（Yao et al. (2023a)）：把**损失项分成不同组**、各自维护二阶动量，平衡多目标，改善物理信息神经网络等系统的收敛。
- **AuON**（Maity (2025)）：通过归一化**整个动量矩阵**自动抑制不稳定更新。

**优点**：在专门的多目标场景中有效改善收敛并降低内存。**固有局限**：假设**同层内梯度行为一致**，可能无意中压制异质特征的学习。

**Mean-removal normalization（去均值归一化）。** **统计中心化**通过平移梯度或相应的矩来稳定更新，针对梯度噪声与非光滑轨迹。

- **GCSAM**（Hassan et al. (2025)）：把**梯度中心化**并入 sharpness-aware minimization（SAM）的上升步，在降低噪声与计算开销的同时提升泛化。
- **AdamMC**（Sadu et al. (2023)）：对自适应优化器的**一阶矩**做归一化，得到更短、更平滑的优化路径。

二者共享「去均值」核心，但作用于不同的解析对象。**优点**：训练路径更平滑、泛化更好。**固有局限**：假设梯度噪声**对称分布**，在严重偏斜的随机分布下性能下降。

**Noise-robust normalization（噪声鲁棒归一化）。** 整合**更完整的历史统计量**，保证非典型梯度条件下跨模态的收敛一致性。

- **AdaGC**（Wang et al. (2025b)）：用指数滑动平均自适应约束参数，同时保持与 Adam 相当的非凸收敛率。
- **AdaNorm**（Dubey et al. (2023)）：通过历史范数均值校正梯度，构造对抗非典型更新的变体。
- **NIGT**（Cutkosky and Mehta (2020)）：修改动量形式，在**二阶光滑目标**上加速收敛，从而有效摆脱对**大批量（large batch）**的依赖。

**优点**：缓解批量大小约束，在任务间普遍起稳定作用。**固有局限**：理论收敛分析复杂，往往需要对**底层景观光滑性**做严格假设。

**gap。** 稳定性技术的演进呈现清晰迁移：从刚性约束到细粒度重缩放，最终到自适应统计校准。但根本缺口在于**局部梯度修正与全局拓扑保证之间尚未打通**：当前方法基本都是基于历史观测的**被动（reactive）机制**；缺失的环节是一个**主动的、统一的数学框架**，把稳定性约束与损失景观的内在几何耦合起来，从而摆脱对辅助调参和启发式统计跟踪的依赖。

#### 3.2.5 学习率调度（Learning Rate Scheduling）

**要解决的问题。** 学习率调度动态调整优化器步长，以在不同优化阶段之间平衡**收敛速度**与**训练稳定性**。

**通用数学框架。** 论文统一写成 $\theta_{t+1} = \theta_t - \eta_t \Phi(g_{1:t}, H_t)$，其中 $\Phi$ 是以历史梯度与结构先验为条件的变换函数。该式与 Eq. (7) 衔接：把动量与预条件操作一并吸收进 $\Phi$，并特化到「投影为单位映射、无显式权重衰减」的情形。近期研究聚焦四个维度：**调度依据、自适应粒度、人工依赖的减少、理论稳定性的增强**。

**Lightweight scaling and batch-aware scheduling（轻量缩放与批量感知调度）。**

- **SGD-SaI**（Xu et al. (2024)）：基于梯度的**信噪比**对初始学习率施加缩放。它无需复杂自适应机制，只持续调整信噪比，因此**内存消耗比先前方法减半**而性能仍有竞争力。
- **LAMB**（You et al. (2020)）：使用**逐层自适应学习率**应对「为加速训练而放大批量」这一关键难题。这种批量感知调度阻止了「既要快速收敛又要高精度」的任务中性能的退化，从而克服了高效大规模部署的一个关键障碍。

**优点**：内存效率与计算简洁性突出。**固有局限**：依赖特定经验初值，可能需要仔细调参。

**Element adaptation and gradient angle scheduling（元素适应与梯度角度调度）。**

- **AdaBound**（Luo et al. (2019)）：对**逐元素学习率**施加动态上下界，缓解极端步长的不利影响；平衡了自适应方法初期训练快与 SGD 泛化好的优点，并有严格收敛证明支持。
- **ACMo**（Huang et al. (2021)）：**只用一阶矩、不用二阶矩估计**，用**角度校准**确保下降方向与当前及历史梯度都成锐角，收敛率可与 Adam 家族相当。
- **HGM**（Sarkar (2025)）：引入**事后（hindsight）机制**评估当前梯度与累积动量的余弦相似度，在方向一致区域**提高**学习率、震荡区域**降低**学习率，效率接近 Adam。
- **AdaBFE**（Cao (2022)）：扩展 binary forward exploration，利用**前向损失函数**信息**逐参数维度**自适应学习率。

**优点**：能纠正次优更新方向。**固有局限**：持续的角度或度量评估增加计算开销。

**Loss-sensitive and stability-aware scheduling（损失敏感与稳定性感知调度）。**

- **DecGD**（Shao et al. (2025)）：依损失函数把梯度分解为**代理梯度与向量**，按**损失信息**而非平方梯度调整学习率，实现快速收敛与更好泛化。
- **LyAm**（Mirzabeigi et al. (2025)）：把 Adam 的自适应矩与基于 **Lyapunov 函数**的稳定性机制结合，动态调整学习率，增强抗噪能力并为非凸优化提供保证。
- **Multistage SGDM**（Liu et al. (2020d)）：同样用 Lyapunov 函数**管控动量偏差**，允许较大的初始步长与较小的后续步长，同时匹配 SGD 的收敛率。
- **大学习率 SGD 的分析**（Andriushchenko et al. (2023)）：用**随机微分方程**建模动力学，揭示由**乘性噪声**产生的**损失稳定化**与**隐式稀疏性**。

**优点**：理论严谨、稳定性增强。**固有局限**：依赖严格理论假设，未必与高度随机的真实数据分布吻合。

**Scheduler-free adaptation（无调度器自适应）。** 最新范式迁移聚焦**彻底消除人工学习率调度**，改用**数据驱动指标**与统一理论模型。

- **Schedule-Free**（Defazio et al. (2024)）：把调度与**动量插值**统一起来。
- **AutoDrop**（Wang et al. (2024c)）：利用**角速度饱和**，无需额外调参。
- **AdaS**（Hosseini and Plataniotis (2020)）：利用**每块（per block）的知识增益**。
- **SGD-G2**（Ayadi and Turinici (2021)）：把 **Runge-Kutta 方法**与自适应 SGD 结合。
- **AutoSGD**（Surjanovic et al. (2025)）：评估**邻近学习率**。
- **Step-Tuned SGD**（Castera et al. (2022)）：**直接从梯度导出步长**。
- **Amos**（Tian and Parikh (2022)）：把**模型尺度**纳入考虑以减少内存占用。
- **Adam++**（Tao et al. (2024)）：同样是**无参数（parameter-free）**变体。

论文把上述工作分为**理论组**（Defazio et al. (2024)、Ayadi and Turinici (2021)、Castera et al. (2022)、Tao et al. (2024)）与**经验组**（Wang et al. (2024c)、Hosseini and Plataniotis (2020)、Surjanovic et al. (2025)、Tian and Parikh (2022)）。**优点**：根除昂贵的超参数搜索。**固有局限**：可能丧失高度专门化架构所需的细粒度控制。

**gap。** 学习率调度的演化呈现从人工启发式走向自动化、有理论依据、按维度动态校准的趋势。这些算法的形式化收敛分析高度依赖非凸设定下对期望下降量的界，常用 Lyapunov 函数或保角约束保证梯度噪声的累积不发散。缺口在于：**当前的全自动调度方法往往难以兼顾**——既要有稳定性感知方法那样严格的理论保证，又要满足超大规模基础模型所要求的低计算复杂度。**把无调度器机制的理论性质与超大批量下的经验噪声特性衔接起来，是未来研究的主线。**

#### 3.2.6 提升泛化能力（Enhancing Generalization Ability）

**要解决的问题。** 传统优化器经常收敛到**尖锐极小值（sharp minima）**，显著损害泛化。大量方法因此**主动寻求平坦极小值（flat minima）**，把问题表述为**极小极大（minimax）优化**。

**通用数学框架（由 SAM 开创）。** 目标是在局部邻域内最小化最大损失：$\min_w \max_{\lVert \epsilon \rVert \le \rho} J(w + \epsilon)$。该目标可在 Eq. (7) 内实现：定义梯度估计量 $E$ 使之包含最坏情况扰动，即 $\hat{g}_t = \nabla J(\theta_t + \epsilon_t ; \xi_t)$，其中 $\epsilon_t = \arg\max_{\lVert \epsilon \rVert \le \rho} J(\theta_t + \epsilon; \xi_t)$，并用**一步梯度上升**近似求解。后续研究沿若干改进维度推进：**步长增强、梯度归一化、曲率引导、动量适应、噪声利用、平均策略**，共同构成在复杂非凸景观中平衡**收敛率**与**泛化界**的统一范式。

**Fundamental framework of SAM。** 原始 SAM（Foret et al. (2021)）的主旨是**同时优化损失值与损失的尖锐程度**；由于标准优化器常在尖锐区域过拟合，原始框架用极小极大问题提升对标签噪声的鲁棒性并抬高基准性能。为缓解内层最大化步的高开销，后续变体聚焦效率：

- **ESAM**（Du et al. (2022)）：用**随机权重扰动**与基于尖锐度的**数据选择**降低计算负担。
- **AsyncSAM**（Jo et al. (2025)）：通过**利用先前梯度的异步后台扰动**实现近乎零的额外开销。
- **AE-SAM**（Jiang et al. (2023)）：在 SAM 与标准经验风险最小化之间**自适应切换**，降低扰动的更新频率，同时保持理论收敛率。
- **SAMPa**（Xie et al. (2024a)）与 **LightSAM**（Cheng et al. (2025)）：利用**自适应优化器**调整半径与速率，且与具体参数无关。

**优点**：直接优化泛化界。**固有局限**：仍要面对**精确尖锐度估计与计算效率之间的根本权衡**。

**Renormalized gradient norm adaptation（梯度范数重归一化）。** 针对扰动尺度不受约束造成的不稳定，用**梯度范数重归一化**改进 SAM 的基本表述。

- **SSAM**（Tan et al. (2025a)）：引入轻量重归一化步骤，把**内层上升步的梯度**重缩放以**严格匹配下降步的梯度范数**。该策略既避免过大扰动，又不引入额外超参数或显著计算成本，同时保留优化过程**基础的两步结构**；在严格理论界支持下提升训练稳定性。**优点**：在高非凸区域稳定优化轨迹。**固有局限**：当**下降梯度范数极小**时，可能导致对损失景观的**探索不足**。

**Multi-step Ascent Optimization（多步上升优化）。** 针对原始 SAM **最大化不充分**的缺点，引入多次迭代增强稳健性。

- **Lookbehind-SAM**（Mordido et al. (2024)）：执行**多次上升步**以更精确地近似局部极大值，并用**线性插值**聚合各步梯度以稳定后续最小化阶段，提高对噪声权重的鲁棒性。**优点**：景观探索精度高。**固有局限**：计算复杂度随内部上升步数**线性增长**。

**Curvature-guided Landscape Exploration（曲率引导的景观探索）。**

- **GAM**（Zhang et al. (2023c)）：整合**一阶平坦度（first-order flatness）**改善泛化。
- **MIAdam**（Jin et al. (2025b)）：在标准 Adam 中引入**多重积分项**，动态过滤尖锐极小值，在保持收敛速度的同时改善泛化。
- **DEO**（Hu et al. (2025)）：改造 **Dimer 方法**，用梯度差分估计 Hessian 的**最小特征向量**，把更新正交投影到**最小曲率方向**上以高效逃离鞍点。
- **SKA-SGD**（Thomas (2025)）：用**流式 Gauss-Seidel 迭代**把梯度投影到 **Chebyshev 基 Krylov 子空间**，免除完整 Gram 矩阵计算，降低运算复杂度。

**优点**：在病态问题中理论收敛稳健。**固有局限**：高维空间中精确估计高阶几何始终困难。

**Momentum Landscape Adaptation（动量景观适应）。** 为化解「优化性能 vs 计算开销」的严格权衡，动量类方法改动景观的**遍历方式**。

- **MSAM**（Becker et al. (2024)）：沿**累积动量向量**扰动参数，而非为上升步计算精确梯度——把动量当作尖锐度计算的近似，因而**免除额外的前向与反向传播**。
- **SCSAdamW**（Zhang and Zhang (2025)）：把**随机共轭次梯度**与 Loshchilov and Hutter (2019) 的技术结合，用自适应采样与样本复杂度分析**动态调整训练批量大小**。

**优点**：额外计算开销为次线性甚至接近零。**固有局限**：依赖延迟的历史近似，导致最坏情况理论界**退化**。

**Noise Injection Enhancement（噪声注入增强）。** 利用**随机梯度噪声**削减精确扰动的计算负担。

- **F-SAM**（Li et al. (2024b)）：通过**历史随机梯度的指数滑动平均**过滤完整梯度分量，利用**残余噪声**缓解次优优化轨迹。
- **FGSAM**（Luo et al. (2024b)）：用**图神经网络**生成 SAM 所需的扰动，用**多层感知机**高效执行最小化。

**优点**：效率的内在改善与隐式正则化。**固有局限**：收敛后期可能引入不稳定。

**Weight Averaging Strategies（权重平均策略）。** 作为显式扰动的互补范式，作用在优化过程的**宏观轨迹**上。

- **Lookaround**（Zhang et al. (2023b)）：一种**迭代式权重平均**策略，在整个训练阶段**持续**部署以平衡状态多样性与局部收敛。其推导分两个阶段：先用**多样化数据增强**并行训练多个网络实例，再对这些网络的权重取**算术平均**形成更新后的中心模型。该范式聚合多样的局部最优，自然逼近**更平坦的全局极小值**。**优点**：不改变内层优化步骤即可持续改善泛化。**固有局限**：维护并同步多个网络状态需要**巨大内存**。

**gap。** 演化逻辑清晰：从极小极大的直接表述，到梯度范数稳定化；经多步与曲率引导技术走向高精度景观探索；为弥合理论严谨性与实际部署的落差，又转向利用动量、噪声注入与宏观权重平均的高效近似。但**一个关键理论缺口仍未解决：当动态、低秩或异步近似被部署在高度非凸、大规模分布式训练环境中时，现有文献缺少能严格界定泛化误差与收敛率的统一数学框架。**

#### 3.2.7 混合方法（Hybrid Methods）

**要解决的问题。** 混合优化方法整合互补技术，克服单一策略的固有局限，同时平衡收敛速度、稳定性、泛化性与资源效率。

**通用数学框架。** 论文抽象为 $\theta_{t+1} = P_X(\theta_t - \eta_t H(\nabla J(\theta_t), m_t, v_t))$，其中 $H$ 是融合动量 $m_t$ 与方差 $v_t$ 的统一混合算子，$P_X$ 表示**约束投影**或**空间平滑算子**。通过选择性组合不同机制的长处，这些方法建立了系统性应对各类算法瓶颈的统一推导形式。

**SGD-Adam hybrid。** 混合设计的基础一步是打通随机梯度下降与自适应方法。

- **AdaSGD**（Wang and Wiens (2020)）：引入修改形式的 SGD，**自适应一个全局学习率**，从而把**全局适应**与**参数特定调整**隔离。它对超参数稳健，并缩小了标准方法与自适应算法之间的性能差距。
- **AGD**（Yue et al. (2023)）：用**一个特定超参数**控制自适应步长的启用程度，在 SGD 与自适应优化之间实现**平滑过渡**。

**优点**：统一处理收敛速度与泛化。**固有局限**：依赖精确的超参数调优来控制**过渡边界**。

**Gradient smoothing hybrid。** 为穿越高度非凸景观、避免陷入次优局部极小，强调超越原始局部梯度估计的**平滑机制**。

- **AGS-GD**（Starnes et al. (2024)）：把**各向异性高斯平滑**引入传统技术以显式平滑目标景观，用**非局部梯度**替代原始局部梯度，其协方差矩阵自适应地与函数的拓扑性质对齐。
- **NOVAK**（Kavun (2026)）：针对同一挑战走**时间轨迹平滑与方差校正**路线，不改变空间上的梯度取值点，而通过**内存高效的 lookahead 机制**隐式平滑优化路径。

二者共同体现从纯点估计下降走向**空间或时间平滑动力学**的范式转变。**优点**：稳健避开尖锐极小值。**固有局限**：协方差估计或 lookahead 轨迹带来显著计算开销。

**Gradient filtering hybrid。**

- **ADASS**（Zhao et al. (2019)）：利用 **Lipschitz 常数**自适应选择训练子集。
- **FESS-GDA**（Shen et al. (2024)）：把平滑技术应用于**联邦极小极大优化**，取得更好收敛率。
- **VR-SGD**（Shang et al. (2018)）：引入**快照（snapshot）启动策略**，对光滑与非光滑目标分别使用不同规则，提升算法灵活性。

**优点**：降低梯度噪声与计算成本。**固有局限**：对**预先设定的过滤阈值**敏感，且可能丢失**信息量大的少数梯度**。

**Projection gradient hybrid。** 把投影机制与自适应策略或动量策略**深度耦合**，而非把约束当作朴素的后处理步骤。

- **Cayley SGD**（Li et al. (2020)）：通过**可逆变换**在 **Stiefel 流形**上运算。
- **NAMO**（Zhang et al. (2026)）：把**正交化动量**单独分离，并用**基于范数的自适应标量**使其对随机噪声稳定。
- **AdamP**（Heo et al. (2021)）与 **HVAdam**（Zhang et al. (2025c)）：用**方向投影**消除径向增长，迭代逼近稳定的下降方向。
- **PadamP**（Li and Zhang (2025)）、**LDAdam**（Robert et al. (2025)）、**AdaDiag**（Nguyen et al. (2025)）、**ADAGB2**（Bellavia et al. (2025)）：把投影感知的更新与**结构化预条件子、误差反馈或二阶跟踪**融合，使约束优化在计算上可行。

这些方法说明**把流形约束与方差适应紧密融合**对高效收敛是必要的。**优点**：严格满足几何约束。**固有局限**：流形收缩（manifold retraction）或复杂投影操作带来高昂计算成本。

**Multi-objective hybrid。** 综合性混合方法同时平衡多个优化目标。多项工作把自适应步长与动量计算合并：**FAdam**（Hwang (2024)）通过**自然梯度联系**修正 Adam 的轨迹；**Grams**（Cao et al. (2025)）把**梯度方向**与**动量幅度**解耦；**MADGRAD**（Defazio and Jelassi (2022)）把动量与**对偶平均（dual averaging）**结合；**SET-Adam**（Zhang (2024)）调整 Adam 的二阶动量以**模仿 SGD**；**CAdam**（Wang et al. (2024e)）**省去方向不一致的梯度—动量更新**；**ZetA**（BC (2025)）为 Adam 增加**缩放因子**。自适应策略也常与**加速技术**配对：**Accelerated GRAAL**（Borodich and Kovalev (2025)）把 Nesterov 式加速与基于曲率的步长结合，**GDA-AM**（He et al. (2022)）把 **Anderson 混合**用于极小极大问题，**Lookahead**（Zhang et al. (2019)）用**快权重与慢权重**获得稳定性。效率导向的混合中，**BAdam**（Luo et al. (2024a)）为 LLM 把**块坐标下降**与 Adam 结合，**BADM**（Wang et al. (2024d)）用**数据切分**实现并行，**ADAMBS**（Liu et al. (2020c)）把自适应方法与 **bandit 采样**结合用于样本优先级排序；专门方法如 **AdaSAM**（Sun et al. (2024)）整合**平坦度感知技术**与自适应学习率，**NIRMAL**（Gaud et al. (2025)）把**五种互补策略**纳入同一框架。**优点是跨任务的高度通用性；固有局限是超参数交互关系极端复杂，且难以分离各组成部分的贡献。**

**gap。** 混合优化的演化体现了从孤立启发式设计走向统一数学框架的迁移，轨迹为**度量混合 → 空间平滑 → 几何约束投影 → 系统性多目标整合**。缺口在于：**多算子整合往往导致「超参数化的优化系统（over-parameterized optimization systems）」，其理论假设与实际实现严重脱节**。因此未来研究的核心挑战是开发**自主的混合框架**：既摆脱对人工超参数调优的依赖，又能在高度异质的损失景观中保持严格的理论收敛界。

#### 3.2.8 面向大模型训练（Towards LLMs Training）

> **范围说明**：本节标题恰在所给页面范围（PDF pp. 15–22，即正文页 13–20）的最后一页页首，正文主体延伸至紧接着的下一页（正文页 21–22）。为保持讲解连贯，此处依与该范围直接相邻的正文一并补出；Sec 3.2.3–3.2.7 全部取自指定范围。

**要解决的问题。** 模型规模迅速膨胀，传统优化器频繁遭遇**硬件内存瓶颈**与**计算效率低下**。近期方法从四个维度**重新设计优化动力学**：**梯度变换、维度缩减、参数管理、计算流程**。

**通用数学框架。** 论文抽象为 $\theta_{t+1} = \theta_t - \eta_t M_t(T_t(\nabla^i_t J(\theta_t)))$，其中 $\nabla^i_t$ 表示**块级或随机梯度估计**，$T_t$ 表示**内存高效的预条件或投影算子**，$M_t$ 表示**硬件感知的内存管理策略**（如算子融合 operator fusion）。

**Gradient preconditioning mechanism（梯度预条件机制）。** 与传统依赖厚重历史统计量的方法相反，这类机制只关注**瞬时谱性质**以压缩内存。

- **SWAN**（Ma et al. (2025)）：把**按行均方根归一化**与**梯度白化（gradient whitening）**结合来预条件梯度，并借助**对角替换启发式**降低白化操作的计算复杂度，同时保持有效学习率。它在不背负历史动量状态的前提下通过自适应预条件增强标准 SGD。**优点**：优化器状态内存显著减少。**固有局限**：可能丢失滑动平均通常提供的**长期方向稳定性**。

**Gradient projection mechanism（梯度投影机制）。** 从预条件进一步走向**结构性降维**，把优化动力学约束在**降维子空间**内。

- **APOLLO**（Zhu et al. (2025b)）及变体 **APOLLO-Mini**：用**随机投影**在低秩或秩一（rank-1）辅助子空间中近似梯度缩放。
- **SSO**（Xie et al. (2026)）：超越纯计算动机，把投影范式**重新用于施加严格的几何稳定性**——不计算完整谱，只**分离顶部奇异分量**，把参数更新投影到某个**谱流形的切空间**上。

核心哲学是**用低秩谱近似驯服代价高昂的高维参数更新**。**优点**：每步计算复杂度有严格界。**固有局限**：丢弃低秩梯度信息引入**近似误差**，可能损害细粒度收敛。

**Block-wise computation（分块计算）。** 不改动梯度向量，而通过**参数空间的时间分段**应对内存约束。

- **BAdam**（Luo et al. (2024a)）：把**块坐标下降**集成进自适应矩估计算法，将参数划分成可控的块并**顺序而非同时**更新，把活跃优化状态限制在单个块上，从而绕开全参数更新的内存瓶颈。**优点**：能在内存极度受限的硬件上运行复杂优化器。**固有局限**：顺序处理导致**整体训练速度显著下降**，并可能出现**块间坐标错配**。

**Real-time computation（实时计算）。** 直接针对**执行流程**，把传统上严格分离的「梯度累积」与「参数更新」融合起来。

- **LOMO**（Lv et al. (2024)）：采用**融合计算策略**最小化整体内存占用，把梯度计算与参数更新直接整合并配合**混合精度训练**，从而在反向传播中**无需存储完整梯度状态**，在受限硬件上实现大模型高效训练。**优点**：达到基于梯度训练的理论最低内存消耗。**固有局限**：**缺乏灵活性**——更新步之前无法施加复杂梯度变换或全局梯度裁剪。

**gap。** LLM 优化的演化体现了从「无约束优化」到「硬件感知算法设计」的范式迁移，轨迹为：预条件矩阵的算法简化 → 通过低秩投影实现更新维度的数学降维 → 参数更新的空间分段 → 计算图的硬件级融合。缺口在于：**现存内存高效方法通常为适配硬件约束而牺牲全局信息或时间动力学，形成「内存占用 vs 收敛速度」的根本权衡。未来研究的核心挑战是设计这样的优化框架：能从局部化、低内存的近似中用数学方式重建全局梯度动力学，而不必付出完整状态物化（full-state materialization）的开销。**

（本节之后，论文以一段总结收束整个 3.2 节，归纳 FO 优化的八个维度——动量、自适应、曲率感知、稳定性机制、调度、泛化技术、混合方法、内存高效变体——并指出其共同追求是「自动适应损失景观几何、同时保持计算效率与理论保证」的算法；由于一阶方法本质上是欧氏空间中的线性近似，它们对参数化高度敏感、在高条件数的峡谷中容易锯齿式震荡，这些未解难题自然引出了下一节的二阶方法。）

---

### Sec 3.3 Second-Order Algorithms（p22–29）——二阶方法的四个子主题

#### 3.3.1 Hessian 近似与估计

**要解决什么问题。** 一阶（FO）方法一般无法利用优化景观的几何结构；经典的 **Newton 法（Newton's Method, Moré and Sorensen 1982）** 虽然用精确曲率确定更新方向，但完整 Hessian 的计算代价过高，在大规模场景下难以实用。**Gauss-Newton 方法（Wedderburn 1974）** 是最早的破局者：它开创性地**用一阶导数间接构造二阶曲率信息**，显著降低了计算复杂度。

**通用数学框架。** 后续研究建立了统一的推导形式：**用近似代理矩阵 $\tilde{H}$ 替换真实 Hessian**，借助结构化近似、随机采样与梯度估计提取曲率信息，从而在**不显式计算完整 Hessian** 的前提下加速收敛。理论上，这类方法通常在标准凸性或光滑性假设下保证**次线性或线性收敛率**，前提是近似曲率矩阵满足特定的谱界（spectral bounds）。但论文直说其共同软肋：**近似精度与计算开销之间的边界始终存在**——越激进的简化，在高度非凸的景观上越容易导致性能退化。

**块 Hessian 近似（Block Hessian approximation）。** 为平衡性能与效率，这类方法假设不同参数组相互独立，利用结构化的块状曲率信息：把 Hessian 划分为**块对角矩阵 $\tilde{H}_{\text{block}}$，丢弃全部非对角（块间）交互**。**Athena（Wang et al. 2024g）** 用二阶矩阵导数信息指导分块量化，按列与行对参数分组并迭代优化量化过程；**Q-Newton（Li et al. 2024a）** 采用混合调度，依据实时的矩阵性质在**量子处理器与经典处理器之间动态分配矩阵求逆任务**，并用代价感知的调度器在有利时卸载任务。优点是保留了参数组内丰富的局部曲率；固有局限是**完全丢失跨块参数依赖**，在高度耦合的网络中可能拖慢收敛。

**对角 Hessian 近似（Diagonal Hessian approximation）。** 这是结构简化走到极端的一步，针对"曲率感知的收敛收益"与"完整矩阵运算的过高代价"之间的权衡。其推导把代理矩阵限制为 $\tilde{H}_{\text{diag}} = \mathrm{diag}(H)$，实现高效的曲率引导逐元素更新。**AdaHessian（Yao et al. 2021）** 用 **Hutchinson 方法**做对角估计；**HesScale（Elsayed et al. 2024）** 以可忽略的额外开销改进近似质量，用于强化学习中的步长缩放；**Sophia（Liu et al. 2024a）** 采用**懒惰更新（lazy-updated）的对角估计配合梯度裁剪**，代价与 Adam 相当但收敛更快；**Fed-Sophia（Elbakary et al. 2024）** 通过加权平均与裁剪把 Sophia 搬到联邦学习；**OptiQ（Agarwal et al. 2024）** 用常微分方程刻画优化轨迹，利用静息（quiescence）启用自适应大步长，并施加**部分 Hessian 求逆**提升效率；**CRNAS（Wu et al. 2024）** 把三次正则化与仿射缩放结合用于约束问题；**SASSHA（Shin et al. 2025a）** 把锐度最小化与稳定的懒惰更新结合以提升泛化。优点是极高的计算效率与极小的内存占用；根本局限是**完全忽略非对角曲率**，在变量交互占主导的高度病态峡谷中难以行进。

**随机 Hessian 采样（Stochastic Hessian sampling）。** 这一族从确定性结构简化转向概率性估计：通过随机数据子集评估构造**低秩代理 $\tilde{H}_{\text{sample}}$**。**SketchySGD（Frangella et al. 2024）** 用**随机化 Nyström 近似**从 mini-batch 估计 Hessian，特点是自动学习率选择与不频繁的预条件子更新；**SGN（Gargiani et al. 2020）** 采用 **Gauss-Newton 与共轭梯度法配合自动微分**确定搜索方向；**STDE（Shi et al. 2024）** 用自动微分的 Taylor 模式把随机估计推广到任意导数阶。优势是能适应不同数据规模并捕捉全局曲率趋势；主要局限是给曲率估计**引入显著方差**，必须靠复杂的方差缩减技术才能支撑理论收敛。

**梯度差分估计（Gradient difference estimation）。** 这一族跳出显式矩阵构造，改用向量运算隐式地估计曲率。**SGDHess（Tran and Cutkosky 2022）** 是基于随机梯度下降的算法，其核心公式通过 **Hessian-向量乘积 $\nabla^{2} L v$ 对梯度差分做高阶估计**，在不实体化矩阵的前提下**修正动量中的系统性偏差**，以 Hessian 引导的动量修正提升收敛效率。优点是回避直接矩阵存储，开销随模型规模线性增长；固有局限是**依赖精确的有限差分近似**，后者对数值不稳定性和随机梯度噪声高度敏感。

**本节自认的 open problem。** 这条演进被总结为：从精确稠密矩阵计算，到空间上的块化简与对角化简，再到经由随机采样与梯度差分的概率化、隐式估计。但其**持续存在的 gap** 是：**缺乏能在训练过程中于这些范式之间动态切换的自适应算法**——现有方法通常在整个优化过程中强制一种静态的近似结构，无法适配深度网络不同训练阶段不断变化的曲率特征。

#### 3.3.2 Fisher 信息矩阵的应用

**要解决什么问题。** 二阶优化技术（如自然梯度下降 Natural Gradient Descent, Amari 1998）的实际部署，根本上依赖对 Fisher 信息矩阵（FIM）的高效近似。在通用框架下，**参数更新由 Fisher 信息矩阵的逆乘以目标函数的梯度所支配**，但该矩阵的精确计算与求逆对大规模模型代价高到不可接受。因此近期研究的核心改进维度是：在**对角化、块 Kronecker 分解、数值约束、结构定制**这四条路线上，平衡"计算可处理性"与"关键几何曲率的保留"。

**【关键区分：经验 Fisher 与真正的 Fisher。】** 真正的 Fisher 是**对数似然关于参数的期望二阶导（等价地，得分函数的外积期望）**，$F = \mathbb{E}\left[\nabla_\theta \ell \nabla_\theta \ell^{\top}\right]$，期望取在**模型自身的预测分布**之下；自然梯度用它才能得到参数化不变、黎曼意义上的最速下降。而**经验 Fisher（empirical Fisher）把期望换成对训练数据或当前 mini-batch 的平均，并把得分中的标签分布换成模型当前的预测**，即 $\tilde{F} = \frac{1}{n}\sum_i \nabla_\theta \ell(x_i, \hat{y}_i) \nabla_\theta \ell(x_i, \hat{y}_i)^{\top}$。它不再是任何分布的真实 Fisher，**衡量的实际上是梯度的二阶矩，也就是梯度方差，而不是曲率**；只有在平方误差损失且模型与数据良好匹配（广义线性模型、Gauss-Newton 型近似）等理想情形下，它才在期望意义上与 Hessian 重合。

**为什么这决定了 Kronecker 类方法的性质。** K-FAC、T-KFAC、AdaFisher 及更广义的 Shampoo 类方法都依赖"层内激活协方差 $\otimes$ 预激活梯度协方差"的 Kronecker 分解，而这些因子在实践中都是用 mini-batch 上的外积平均（即经验 Fisher 式统计量）估出来的。因此论文的立场是：**这类方法捕捉到的是梯度方差而非真正的曲率**。后果有二：其一，这种预条件本质上更接近"自适应的方差归一化"（Adam 一族的推广），而不是"用真实 Hessian 的逆去扭曲峡谷"；其二，Kronecker 结构还额外假设**层内激活与预激活梯度的统计独立性**，这在深网络中并不严格成立。因此各族方法都回到同一主题：**结构性近似换来可计算性，代价是曲率保真度的系统性损失。**

**对角 Fisher 近似（Diagonal Fisher approximation）。** 迈向可扩展性最根本的范式转变：把稠密矩阵压成对角结构，其统一形式是**把参数视为彼此独立**，因而获得**线性时间复杂度**。**SOAA（Vo 2024）** 通过直接的对角近似降低算法复杂度；**OCAR（Urettini and Carta 2025）** 通过施加 KL 散度约束，把该框架适配到持续学习（continual learning）环境；**RACS（Gong et al. 2025）** 引入结构化的对角近似方案。优点是极高的计算效率；固有局限是**严重丢失结构化曲率信息**。

**块对角 Kronecker 近似（Block-diagonal Kronecker approximation）。** 为解决纯对角方法的缺陷，演进逻辑推进到**建模层内相关性（intra-layer correlations）**。其统一推导是把 FIM 近似为两个较小矩阵的 Kronecker 积，它们通常分别代表**激活协方差**与**预激活梯度协方差**。**K-FAC（Martens and Grosse 2015）** 奠定了这一方向的基础，使高效矩阵求逆成为可能并加速收敛；在此之上，**AdaFisher（GOMES et al. 2025）** 是一个自适应优化器，**在"对角结构"与"块 Kronecker 结构"之间架桥**，平衡二阶方法的收敛收益与一阶更新的效率。优点是能较准确捕捉层内曲率，但**本质上无法建模层间依赖（inter-layer dependencies）**。

**迹保持的 Fisher 近似（Trace-preserving Fisher approximation）。** 在标准 Kronecker 分解之上，后续方法转向**数值校准**。**T-KFAC（Gao et al. 2021）** 把每个块分解为受**迹约束系数**约束的 Kronecker 积，该推导保证**近似矩阵的对角元素之和与真实 FIM 相等**，从而修正了原版 K-FAC（Martens and Grosse 2015）的缩放不准确问题。优点是提高近似精度并在不同架构上带来谱稳定性；局限是**迹估计带来的额外计算开销**。

**曲率感知的定制近似（Curvature-aware approximation）。** 最新范式是**把近似裁剪到特定架构的性质上**。**MAC（Seung et al. 2025）** 利用均值激活（mean activations）降低估计曲率的计算成本：它专门把 Kronecker 分解施加到现代架构的**注意力层**，并把注意力分数整合进预条件化过程。优点是针对特定模型高度优化了性能；固有局限是**对非注意力架构缺乏普适性**。论文还特别强调，上述近似技术都保证替代矩阵**保持正定（positive definite）**，这是**约束条件数、并保证目标函数稳定下降**的关键要求。

**本节自认的 open problem。** 尽管有这些理论保证，比较各方法的优缺点仍揭示出一条显著边界：**核心 gap 在于这些近似的"静态性"**——**缺少一个普适的自适应框架**，能依据损失景观实时的曲率需求，**在对角、块状与迹保持结构之间动态切换，而无需人工推导**。

#### 3.3.3 拟牛顿法

**要解决什么问题。** 拟牛顿法（以 **BFGS（Byrd et al. 1995）** 为最典型代表）的**核心数学框架是：通过迭代更新动态逼近损失函数的曲率**，从而**绕开对昂贵二阶导数的直接计算**。

**通用数学框架（割线条件）。** 形式化地，Hessian 的近似记为 $B_t$，它满足**割线条件（secant condition）** $B_{t+1} s_t = y_t$，其中 $s_t$ 为**参数差**，$y_t$ 为**梯度差**。它不要求 $B_t$ 等于真实 Hessian，只要求它在新旧两点之间把梯度变化"解释对"。

**三个改进维度。** 为适配深度学习，后续方法沿三条核心维度强化优化过程：**资源约束、对噪声的算法稳定性、系统可扩展性**——**从削减内存占用，到缓解随机方差，最终走向把计算分布到大规模架构上。**

**低内存拟牛顿（Low-memory quasi-Newton）。** 标准框架的首要瓶颈是**存储稠密近似矩阵的二次内存开销**；低内存变体通过维护**稀疏或隐式表示**解决它。面向深度网络训练的方法引入**因子化策略**以保持近似矩阵的特征值有界，**K-BFGS（Goldfarb et al. 2020）** 系列即为示范：这类算法**只利用近期一段受限的梯度与参数历史**计算搜索方向。这种结构化近似显著提升了可扩展性，同时保持收敛稳定性，是深度学习适配的**奠基性一步**。

**随机拟牛顿（Stochastic quasi-Newton）。** 当目标是随机的时，**传统割线条件会因梯度方差而变得不稳定**，因此随机化改造需把**概率建模与方差缩减技术**整合进来。**S-BFGS（Carlon et al. 2025）** 用**贝叶斯推断**吸收噪声梯度，同时控制曲率更新（文中的 noise-aware Bayesian inverse Hessian update）；**SpiderSQN（Wills et al. 2020）** 集成特定的方差缩减框架，以在非凸随机设定下达到**最优复杂度**；**FUSE-PV（Jiang et al. 2025b）** 把 **L-BFGS（Nocedal 1980）** 的形式与小批量计算相适配。这些工作在数学上**把二阶精度与随机效率统一起来**。

**分布式拟牛顿（Distributed quasi-Newton）。** 为应对海量数据集的计算负载，分布式范式**把曲率估计切分开**。**mL-BFGS（Niu et al. 2023）** 采用 **Hessian 的块级（block-wise）近似**，把内存需求分散到各计算节点；为在不依赖昂贵方差缩减的前提下加速收敛，它**把动量方案引入 L-BFGS（Nocedal 1980）迭代**以缓解随机噪声。**块状分布 + 动量平滑**的组合，为把拟牛顿优化器扩展到大型分布式系统提供了稳健的统一推导。

**理论边界。** 这些方法的收敛分析确立了**非凸优化下的最优次线性速率**，前提是**近似 Hessian 的特征值界被严格维持**。但这些保证往往**假设梯度方差有界**，而这在实际深度学习景观中并不普遍成立。此外，**整个框架的固有局限是曲率信息的"陈旧性"（staleness）**——当梯度在相继的 mini-batch 间剧烈波动时，上一轮构造的曲率近似已经过时。

**三类范式的优缺点。** 该领域的里程碑式转移是**从确定性的精确矩阵构造，走向随机的、结构受限的近似**。低内存范式：优点是**存储复杂度的线性下降**，局限是**历史曲率信息的丢失**；随机范式：优点是**对批量噪声的数学稳健性**，局限是**依赖复杂超参数调节来做方差缩减**；分布式范式：优点是**并行处理能力**，局限是**同步块级更新的通信开销**。

**本节自认的 open problem。** 核心 gap 是**缺少一个"通用拟牛顿优化器"，能同时做到：精确的曲率匹配、零通信开销，以及在高度非凸的深度网络架构中对任意随机噪声保持韧性（resilience）。**

#### 3.3.4 二阶矩融合

**要解决什么问题。** 二阶矩融合（Second-Order Moment Fusion）处理的是二阶方法**收敛速度与稳定性之间的关键权衡**：单用曲率信息在随机环境下噪声敏感，单用动量又无法感知几何。

**通用数学框架。** 论文给出的统一形式是：这类方法**把局部曲率信息融合进动量序列**，典型地定义更新规则为 $\theta_{t+1} = \theta_t - \eta P_t^{-1} m_t$，其中 $P_t$ 是**由 Hessian 矩阵导出的预条件子**，$m_t$ 是动量项。论文指出这一形式与全文 Eq. (7) 的统一视角**一致**：只要把 $P_t^{-1}$ 解释为那里的预条件算子 $M_t^{-1}$，把 $m_t$ 解释为动量更新 $\phi(\cdot)$ 的输出即可。关键机制是：**用近似曲率矩阵去缩放累积的历史梯度**，从而**依据损失景观的几何性质，为每个参数分别调整步长**。

**理论收益。** 收敛性分析表明，这种融合能**加速穿越平坦区域、抑制陡峭方向上的震荡**，进而提升整体收敛率。该框架的**边界**主要受两件事约束：**曲率估计的精度**，以及**二阶信息对随机噪声的敏感性**。

**动量-曲率融合（Momentum-curvature fusion）。** 这一范式的演进取自"把曲率信息直接整合进梯度累积过程"。**SGDHess（Tran and Cutkosky 2022）** 是典型例子：它把动量的历史梯度累积与曲率估计结合以缓解优化偏差，**用曲率数据精化动量向量**，从而在不需要大批量的前提下促成快速收敛。这种融合借助二阶方法的几何感知引导动量轨迹，在特定场景中同时提升收敛效率与适应性。

**抗噪的二阶动量（Noise-robust second-order momentum）。** 在基础曲率整合之上，演进逻辑继续推进，去解决**完整二阶方法在随机环境中固有的噪声敏感性**：准确的 Hessian 估计常被噪声梯度与深度学习的非凸目标破坏。为此，方法采用**高效的对角近似配合降噪技术**。**AdaHessian（Yao et al. 2021）** 利用**空间平均（spatial averaging）** 平滑空间变化，并引入 **Hessian 动量**抑制估计噪声；**Sophia（Liu et al. 2024a）** 则用**逐坐标裁剪**防止不准确的曲率估计造成破坏。两者都优先采用高效对角近似与噪声缓解策略，在**回避完整 Hessian 计算负担的同时保留了二阶优化的根本收益**。

**本节自认的 open problem。** 论文把二阶矩融合的轨迹概括为：**从朴素的曲率整合，走向抗噪且计算可行的近似**。动量-曲率融合的优点是**在低噪声设定下显著加速收敛**，局限是**易受随机 mini-batch 导致的 Hessian 估计不准所害**；抗噪二阶动量的优点是**在高随机性环境下优化稳定**，局限是**因依赖对角近似而造成结构信息损失**。最终，**核心 gap 是缺少一种自适应机制**，它能**依据实时的噪声水平动态校准曲率整合的程度**，从而在深度网络的不同训练阶段都保证一阶动量与二阶几何的最优融合。

（说明：所给抽取文本在 PDF 第 27 页末尾、即本节中途结束；本子节余下的段落与本节小结位于其后一页，上文的完整讲解同样依据该份抽取文本、按同一双栏阅读顺序重建，未添加原文之外的内容。）

---

### Sec 3.4 Zeroth-Order Algorithms（p27–33）——零阶方法的六个子主题

论文把零阶（ZO, zeroth-order）优化定位为一次范式转移而非二阶（SO, second-order）方法的数学升级：**放弃精确几何信息，换取物理上的可实现性**。此前的 SO 乃至部分 FO 方法要求完整白盒访问、严格二阶可微，以及难以承受的显存去显式构造曲率矩阵；当模型规模冲向十亿参数 LLM、并进入不可微黑盒环境时，这些要求撞上了"不可逾越的显存与适用性之墙"。

ZO 的破局方式是**完全绕开反向传播**，只靠对**随机扰动参数的函数值查询**估计梯度方向。这抹除了激活值的巨量辅助显存开销，使训练显存与推理显存解耦，理论上可与推理显存精确匹配（如 MeZO）。本节的六个维度，就是围绕"保留无梯度优化优势、同时缩小 ZO 与梯度类算法的差距"组织的递进努力（另见 Fig. 7 与 Tab. 2）。用 Sec 3.1 的四算子视角 Eq. (7) 定位，ZO 的改动主要落在**梯度估计器** $E$ 上。

**统一数学框架。** 论文取随机方向 $u_i \sim \mathcal{N}(0, I)$ 与**平滑半径** $\mu$（smoothing radius），定义

$\tilde g_t^{ZO} = \frac{1}{q}\sum_{i=1}^{q}\frac{f(\theta_t+\mu u_i)-f(\theta_t-\mu u_i)}{2\mu}\,u_i$

其中 $f$ 是目标函数，$\theta_t$ 是第 $t$ 步参数，$\mu$ 是差分步长（在多大尺度上探测损失面），$q$ 是每步**查询数**（在 $\theta_t$ 邻域做几组前向评估）。$q=1$ 时每步只需两次前向评估，即**两点估计**（two-point estimation）；多取几个 $u_i$ 求平均可降方差，但前向评估次数成比例上升。

这也是全部麻烦的根源：有限差分带来 $O(\mu)$ 的偏差，而方差又大致**正比于问题维度**（Sec 3.4.5 明确指出）。六个子节，本质上是六种对付"高方差 + 高维度 + 昂贵查询"的思路。

---

#### 3.4.1 自适应方法（Adaptive Methods）

**动机。** 为弥合一阶与零阶的差距，把 FO 中成熟的自适应机制搬到 ZO。论文称这类方法操纵时间序列、参数空间与几何结构，演化逻辑是**先抑制时间维度噪声，再限制搜索空间，最后压缩更新表示**。

**通用框架。** 以上面的 $\tilde{g}_t^{ZO}$ 充当"梯度"，用**历史指数滑动平均**更新一阶矩 $m_t$ 与二阶矩 $v_t$，再以 $m_t/(\sqrt{v_t}+\epsilon)$ 构造方向：噪声靠时间维度平均压制，且**矩的更新不需额外函数评估**。

**具体方法。**

- **ZO-AdaMM**（Chen et al., 2019）：把一阶的自适应动量机制平移到零阶体制，确立"动量 + 自适应步长"在仅有函数值时的有效性，是本类奠基工作。
- **ZO-SAH**（Kim et al., 2025）：基于投影的自适应。在**随机二维子空间**内用**二次拟合**估计 Hessian，并用**周期性切换**复用旧的评估值，同时保证近似矩阵正定。它针对动量方法的维度瓶颈，使单次迭代代价大幅下降；**局限是被选子空间之外的梯度信息会丢失，可能落入次优局部极小**。
- **DiZO**（Tan et al., 2025b）：在 **anchor 方向**上投影以逐层调整更新，代表低维子空间/结构化投影路线（其调度视角见 Sec 3.4.4）。
- **LORENZA**（Refael et al., 2025a）：**低秩自适应**路线的综合代表，核心是 **AdaZo-SAM**——把 **Adam** 与 **SAM**（Sharpness-Aware Minimization，锐度感知最小化）的更新规则整合，**每次迭代只用一次梯度近似**，再结合**随机化奇异值分解**动态生成自适应子空间投影。它同时追求省显存与寻找平坦极小以提升鲁棒性；**代价是维护子空间需频繁做 SVD，带来额外计算开销**。

**论文承认的 gap。** 该方向已从"朴素高维随机采样"走到"历史感知 + 结构受限近似"，但**在不产生 prohibitive 采样成本的前提下，仍难以在无结构、超大参数空间中达到与一阶可比的收敛速率**。

---

#### 3.4.2 扰动优化（Perturbation Optimization）

**动机。** ZO 的根基是有限差分，依赖对扰动参数的前向评估，而**传统各向同性高斯扰动常遭遇高估计方差、慢收敛与显著计算开销**。既然估计式不能改，本节就直接优化**扰动机制本身**，演进为三步：**简化噪声分布 → 限制扰动空间 → 精化采样的时间动力学**。

**通用框架。** 仍用 $\tilde{g}_t^{ZO}$ 那一式，改的是 $u_i$ 的**分布**（如把高斯换成 Rademacher 向量）、**支撑集**（只扰动稀疏子集或某些层）与**时间结构**（把动量混入当前扰动）。论文提到，这类方法把扰动向量限制为**离散符号值**以高效逼近梯度，并把两点估计**仅施加在一个动态选择的稀疏 mask 上**。

**具体方法。**

- **FZOO**（Dang et al., 2025）：随机符号扰动的代表。用 **Rademacher 随机向量**替代 MeZO 等所用的高斯扰动，把扰动限制为 $\pm 1$，以降低生成扰动向量的开销：保持收敛性的同时**大幅削减前向传播复杂度**；**局限是离散符号难捕捉细粒度方向信息，在病态损失面上可能限制可达精度**。
- **Sparse-MeZO**（Liu et al., 2024b）：用**逐层幅值阈值**动态选参数子集，只在其上做 SPSA 风格两点估计，以消除辅助显存。**局限是硬阈值可能丢弃跨层梯度交互，给轨迹引入偏差**。
- **LeZO**（Wang et al., 2024b）：同样稀疏，但粒度为**整层**——把 **SPSA**（Maryak and Chin, 2001）与逐层稀疏性结合，只扰动若干完整层且不引入额外开销，局限同上。
- **QZO**（Shang et al., 2025）：面向**量化网络**微调，**只扰动连续的量化尺度（quantization scales），不动离散权重**。优势是显存高效、结构稳定；**局限是更新被限制在缩放因子上，严重削弱梯度近似的表达能力，难以应付需要大幅权重变换的任务**。
- **ZO-AdaMU**（Jiang et al., 2024）：用**混合历史动量与当前随机性**的自适应扰动替代标准随机向量，并在 SPSA 框架内构造**成对扰动**，得到更平滑、收敛更快的估计。**局限是须存储历史动量状态，部分抵消了零阶最核心的显存优势**。

**论文承认的 gap。** 当前方法**依赖启发式空间选择或静态分布替换，缺乏在局部损失面上动态调整扰动几何、又不引入高阶估计那种 prohibitive 成本的能力**。

---

#### 3.4.3 零阶-一阶混合（Zeroth-First Order Hybrid）

**动机。** 目标是构造**同时最小化显存与梯度方差**的估计器：纯 ZO 省显存但方差大、慢；纯 FO 准但要存激活、要反传。核心改进维度是**确定 ZO 估计与精确一阶梯度之间的最优整合机制**，演进为**全局混合 → 空间分解 → 统计校正**。

**通用框架。** 论文给出的统一形式是两方向的**凸组合**（convex combination）：把 ZO 与 FO 估计按比例线性相加，比例由**混合权重参数** $\alpha$ 调制（论文只说明是 convex combination 并给出符号 $\alpha$，未在此展开完整上下标）。可概念地写作 $g^{\text{hybrid}} = \alpha\, \tilde{g}^{ZO} + (1-\alpha)\, g^{FO}$：$\alpha$ 越大越省显存，越小梯度越准。

**具体方法。**

- **Addax**（Li et al., 2025c）：在**每一步**都做动态融合，通过 $\alpha$ 调制贡献比，从而连续细粒度地权衡显存效率与梯度精度；优势是易于实现，**固有局限是每步仍需计算一阶信息，无法实现显存占用的绝对下降**。
- **ElasticZO**（Sugiura and Matsutani, 2025）：改用**空间分解**而非全局融合，把显存高效的零阶优化**限制在重的浅层**，把**精确反向传播保留给最后几层**（依据是深层对精度更敏感、浅层参数多激活大）。它既压低激活显存，又以尾部层的定向一阶更新保住精度；**内在局限是浅层零阶估计的高方差会沿网络传播**。
- **VAMO**（Chen and Ma, 2025）：它认为前两者只是线性混合、存在**统计缺陷**，故引入一阶信息做**统计校正**：在零阶估计上叠加**可配置系数加权的方差缩减项**，其推导依赖在**参考点（reference points）**处取得精确一阶估计来稳定含噪的零阶方向。优势是理论收敛显著加快；**局限是维护与评估参考点带来额外计算开销**。（VAMO 还以"快照式方差缩减"身份出现在 Sec 3.4.5。）

**论文承认的 gap。** 轨迹显示从启发式参数混合走向严格方差缩减，但**缺少一个依据损失面局部地形动态调度一阶/零阶整合比例的自适应框架，且不依赖静态结构启发式或昂贵的周期性参考点**。

---

#### 3.4.4 显存高效方法（Memory-Efficient Methods）

**动机。** 从**存储管理、结构利用、稀疏化、量化、调度**多个维度做显存优化，统一思想是把优化显存形式化为**以推理前向所需显存为上界的约束**，靠精心设计的优化策略在维持模型性能的前提下砍掉辅助显存。

**通用框架。** 即约束"优化器辅助显存不超过推理所需显存"。各方法在此约束下以不同策略改进 ZO 估计（论文用 $\hat{g}$）：低秩方法把扰动**投影到低秩子空间** $W = AB$（**内在秩远小于原始维度**，故降低了有效优化维度）；稀疏方法施加动态稀疏子集；量化方法在**降精度表示** $Q(W)$ 上直接操作权重与扰动。

**具体方法（五条路线）。**

1. **推理级显存 ZO**：**MeZO**（Malladi et al., 2023）是奠基框架，把 **ZO-SGD** 改造为**原地（in-place）**运行以满足推理所需的 $O(1)$ 辅助显存（含用**同步随机种子**实现原地扰动）；**ZO-AdaMU**（Jiang et al., 2024）把**动量计算搬进 SPSA 扰动**，用**不确定性调度**配合动量的二阶近似加速收敛；**KerZOO**（Mi et al., 2025）引入**基于核（kernel）的 ZO 框架**提升收敛速度；**ZO2**（Wang et al., 2025c）用 **CPU offloading** 把不活跃参数移到系统内存，GPU 只留当前计算所需模块。优势是优化显存与推理显存精确匹配；**固有局限是高维空间中估计方差大、收敛缓慢**。
2. **低秩 ZO 微调**：**LOZO**（Chen et al., 2025b）用 **lazy sampling**（惰性采样）维持低秩子空间；**TeZO**（Sun et al., 2025b）用 **CPD** 捕捉**跨维度低秩结构**并**动态调整各层的秩**以最优利用显存；**SuZero**（Yu et al., 2024）把**逐层低秩扰动**与 ZO **深度耦合**，借低秩结构降梯度方差。优势是理论上同时降低梯度方差与参数量足迹；**局限是建模高度复杂目标分布时可能损失表达能力**。
3. **稀疏参数**：**Sparse-MeZO**（Liu et al., 2024b）在**动态子集**上按 SPSA 风格做两点 ZO 估计（逐层幅值阈值选点）；**LeZO**（Wang et al., 2024b）把 SPSA 与逐层稀疏性结合且不增额外开销；**MaZO**（Zhang et al., 2025e）用**权重重要性度量**（面向多任务学习）识别关键参数以降低维度。优势是以最小活跃显存做高度定向优化；**局限是对参数选择准则过于敏感，忽略关键权重会致次优收敛**。
4. **量化 ZO 微调**：**QuZO**（Zhou et al., 2025）用**随机量化扰动**微调 LLM；**QZO**（Shang et al., 2025）扰动**量化尺度**并施加**方向导数裁剪**以保证稳定；**ZOQO**（Bar and Giryes, 2025）把 **ZO-Sign SGD** 适配到量化噪声，用符号更新配学习率调整维持量化状态。三者都在 $Q(W)$ 上直接做无梯度优化，差别在扰动目标与稳定化策略。优势是显存被极端压缩；**主要局限是量化噪声严重加剧 ZO 估计器的固有方差，使收敛的理论保证复杂化**。
5. **逐层 ZO 调度**：**DiZO**（Tan et al., 2025b）把全局优化分解为**顺序局部更新**以复用显存，用 **anchor 投影**调整各层更新。优势是**峰值显存大幅下降**；**局限是时间复杂度上升、更新在网络中的传播被延迟**。

**论文承认的 gap。** 领域已从"纯算法层面的显存匹配"走到"深度结构利用"、再到"硬件感知的数据表示与调度"，但**缺少一个能把混合精度量化、动态稀疏、低秩自适应同时整合、并给出严格收敛性数学保证的统一框架**。

---

#### 3.4.5 方差缩减（Variance Reduction）

**动机。** 直击根本痛点：**标准零阶梯度估计器固有地带有正比于问题维度的方差，严重限制收敛速率**。已有思路分布在**统计性质、系统架构、时间基准**三个维度。

**通用框架。** 引入一个**辅助控制变量**（auxiliary control variable）抵消随机噪声，**同时不改变梯度的期望**（without altering the expectation of the gradient），即构造 $\tilde{g}^{\text{VR}} = \tilde{g}^{ZO} - c + \mathbb{E}[c]$ 一类估计器：$c$ 与 $\tilde{g}^{ZO}$ 高度相关，相减消掉大部分方差，期望再加回以保证无偏。代价是 $c$ 通常要在"参考点"上算出较准（多为一阶）的估计，带来额外记忆或计算开销。演进逻辑为**粗糙梯度平滑 → 结构化方差控制 → 快照式方差缩减**。

**具体方法。**

- **R-AdaZO**（Shu et al., 2025）：通过**一阶矩方差缩减与二阶矩精化的协同设计**平滑梯度，其理论分析表明"方差感知的矩精化"带来更平滑的轨迹与更快收敛。优势是**不增加显存开销即提升稳定性**；**局限是依赖历史矩，在高度非凸损失面上可能引入估计偏差**。（它亦作为动量自适应方法出现在 Sec 3.4.1。）
- **FZOO**（Dang et al., 2025）：以**结构化扰动**加**并行化工程实现**建立完整方差控制系统，通过**约束扰动空间**缓解常规零阶方法的方差问题，为理论收敛提供严格保证。优势是并行结构显著提升计算效率；**局限是严重依赖特定系统架构，缺乏跨硬件通用性**。（与 Sec 3.4.2 的随机符号扰动是同一算法的两个侧面。）
- **VAMO**（Chen and Ma, 2025）：**用零阶估计替代完整的一阶 checkpoint 梯度**，同时保持**无偏的方差修正**。
- **VR-SZD**（Rando et al., 2025）：把**随机零阶估计与控制变量结合**，以对齐优化轨迹。
- **MeZO-SVRG**（Gautam et al., 2024）：借鉴 SVRG 思想，**结合全批次与小批次信息构造低方差估计器**（用周期性全批次快照作锚点、每步小批次做增量修正）。

三者的快照范式优势是**严格降低理论方差上界**；**局限是存储 checkpoint 数据需要额外显存**。

**论文承认的 gap。** 该方向呈现从"孤立统计平滑"到"结构化扰动约束"、再到"时间维度控制变量"的转移，但**大多数算法都面临降低方差与消耗显存之间无法回避的权衡**；论文把"**设计在严格约束显存的同时、对极大规模模型保持线性收敛的最优方差缩减框架**"列为未来重大挑战。

---

#### 3.4.6 分布式零阶优化（Distributed Zero-Order Optimization）

**动机。** 处理**分布在多个网络节点上的全局目标优化，且只能依赖函数评估**——这正是它与 Sec 3.5 分布式一阶优化的分水岭：后者还能传梯度，前者连梯度都没有。

**通用框架。** 论文把它形式化为**共识优化问题**（consensus optimization problem）：**本地节点用零阶 oracle 估计梯度**，在**给定网络拓扑**上通信以逼近全局梯度。具体地，**本地更新依赖高斯平滑的零阶估计器**，这些估计随后**沿拓扑被聚合**来近似全局梯度。两条核心改进维度是：用**分布式扰动采样**克服全局信息不可访问，用**隐私保护机制**强制数据保护；演进逻辑为**从建立基础分布式共识到确保安全通信拓扑**。

**具体方法。**

- **ZoPro**（Wang et al., 2024a）：代表"全局数据或 Hessian 不可访问时如何估计信息"的基础范式。它给 **SoPro** 框架加上**高斯平滑的零阶估计器**来估计梯度块或 Hessian 块，从而把**分布式二阶优化与零阶技术桥接**。优势是无需精确梯度即可估计高阶信息；**固有局限是通信开销增加，且依赖随机扰动的方差**。
- **TOP-DP**（Guo et al., 2021）：在共识框架上引入**隐私保护**。采用**拓扑感知（topology-aware）降噪**：复用邻居噪声估计，经**加性分解**（additive decomposition）算出降方差高斯噪声以满足**差分隐私**约束。优势是对隐私有稳健理论保证；**固有局限是注入噪声规模与收敛速率之间存在严重权衡**。论文的收敛分析显示这类方法通常只达到**次线性（sublinear）收敛速率**：拓扑感知噪声与零阶估计器的方差需要严格理论上界，才能保证全局目标的稳定性。

**论文承认的 gap。** 这些方法完成了从"纯分布式信息估计"到"拓扑感知隐私保护"的转变，共同点是**依赖降方差高斯扰动做梯度近似**，区别仅在扰动是用于搜索探索还是严格用于密码学隐私。核心缺口是：**缺少能在零阶查询复杂度、网络通信效率与严格差分隐私上界之间取得最优平衡的统一理论框架，尤其在数据分布高度异质时**。

（说明：本节所在 PDF 页面存在双栏交错与公式符号丢失，个别处如 $f(\theta_t\pm\mu u_i)$ 的上下标、$\hat{g}$ 与 $\tilde{g}$ 的区分，依据 Sec 3.1 的记号表与上下文重建；Tab. 2 的表格内容不在本页范围内，故未展开。）

---

### Sec 3.5 Distributed Optimization（分布式优化：总览与三组根本权衡）

§3.2–3.4 讲优化的数学原语（一阶 FO、二阶 SO、零阶 ZO），§3.5 转向**场景驱动**框架：优化不再只是算法问题，而是受约束的系统工程问题。Transition 段列出四个系统瓶颈，第一个即本节主题——极端通信开销压垮去中心化网络时，如何压缩或同步各节点间的梯度？

通用数学框架：分布式优化最小化**全局目标** $F(x) = \frac{1}{n}\sum_{i=1}^{n} f_i(x)$，它分布在多个计算节点上。该目标可嵌入 §3.1 的统一视角 Eq. (7)：取损失函数 $f$ 为 $F$，再用**场景变换** $T_{\text{scenario}}$ 刻画分布性质——它负责每次迭代中跨节点的梯度聚合、压缩或量化。回忆 Eq. (7)：$\tilde{g}_t = E(f,\theta_t,\xi_t)$，$\hat{g}_t = T_{\text{scenario}}(\tilde{g}_t)$，$m_t = \phi(m_{t-1},\hat{g}_t)$，$\theta_{t+1} = P_\Theta(\theta_t - \eta_t M_t^{-1}m_t - \eta_t\lambda\theta_t)$。分布式优化的位置很清楚：**不改梯度估计器 $E$，专改 $T_{\text{scenario}}$**。

全节归为两条改进维度：**通信压缩技术**与**结构性更新策略**。前者用投影算子（低秩近似、量化、稀疏化）降通信量，常配自适应级别与误差补偿；其统一形式化推导就是把 $g_t$ 换成压缩映射 $C(g_t)$，并要求**压缩误差方差严格有界**以维持最优收敛率。后者含本地更新、去中心化协议、联邦学习与混合框架，其内在边界是**统计效率与硬件利用率的根本权衡**。

全节内容由三组权衡生成：**① 压缩率 vs 收敛速度**——$C$ 把梯度写成更少比特或分量，却引入偏差与方差，方差无界则收敛率退化，故须误差补偿（未传出的部分记在本地下轮补上）或设计误差范数有界的算子；**② 本地步数 vs 客户端漂移（client drift）**——本地多走几步省通信，但数据非独立同分布（non-IID）时各节点局部轨迹朝各自最优点跑偏，$H$ 越大漂移越重；**③ 通信频率 vs 收敛精度**——少通信（事件触发、优先级、延迟容忍）省带宽，但丢弃或延迟的是真实梯度信息。

#### 3.5.1 Gradient Compression & Quantization（梯度压缩与量化）

**动机。** 为系统解决通信瓶颈，相关方法通过统一压缩框架 $C(\cdot)$ 减少梯度传输量。演化从**静态标量削减**走向**动态、带误差补偿、自适应**的机制，即从"一刀切发少一点"变成"看着网络与梯度状态决定发多少、发什么"。

**（1）Quantization compression（量化压缩）** 在**表示层面**动手，把梯度编码成低精度格式。**SIGNSGD**（Bernstein et al., 2018）直接传符号，每维 1 bit，丢掉全部幅值；**signProx**（Xu and Kamilov, 2019）用近端算子（proximal operator）扩展之，处理单比特非凸优化；**0/1 Adam**（Lu et al., 2023）自适应**冻结方差状态**以实现纯单比特压缩；**1-bit Adam**（Tang et al., 2021）用两阶段策略把量化与 Adam 的收敛特性深度耦合，规定**只在预条件子方差稳定后**才对动量做单比特量化——压缩率不能脱离优化器内部状态动力学单独设计；**LQ-SGD**（Li et al., 2025a）把对数量化（log-quantization）集成进 PowerSGD 以最小化精度损失；**DEED-GD**（Ye et al., 2020）在不精确梯度框架内用**双重编码**与**误差递减**；**TAH-QUANT**（He et al., 2025a）引入**分块量化（tile-wise quantization）**做局部精度控制。

**（2）Sparsification compression（稀疏化压缩）**：不统一降精度，而是**只传最重要的梯度分量**，同时保持既有收敛界限。**rTop-k**（Barnes et al., 2020）用参数化统计模型捕捉梯度固有稀疏结构；**SPARQ-SGD**（Singh et al., 2022）用**事件触发**架构，只在参数绝对变化超过预设阈值时传压缩差分。

**（3）Low-rank gradient compression（低秩梯度压缩）**：把高维梯度压到低秩子空间，对扩展到大量 worker 与超高维模型至关重要。**PowerSGD**（Vogels et al., 2019）用迭代幂法快速求梯度低秩近似；**DOME**（Nicolas et al., 2025）用**相关性感知 sketching** 与安全数学聚合把它扩展到去中心化、隐私保护设定；**SketchedAMSGrad**（Xian et al., 2022）把 Adam 型预条件算法与矩阵 sketching 结合，**指数级**降低通信成本。

**（4）Compression error compensation（压缩误差补偿）**：静态压缩算子必然带来信息损失与收敛限制，故须系统追踪并缓解压缩诱导方差的累积。**APM-Squeeze**（Tang et al., 2020）综合 Adam 预条件机制与误差补偿式压缩，实现通信负载显著且稳定的下降；**ADEF**（Gao et al., 2025）把**收缩型压缩算子**直接与数学误差反馈回路结合。其卖点是对内部补偿记忆保持严格理论界，并有完整收敛分析与实验增益。

**（5）Adaptive compression level（自适应压缩级别）**：固定压缩超参数无法适应动态带宽与演化的梯度性质，必然导致次优收敛。**AdaCGD**（Makarenko et al., 2023）把**三点压缩器**框架扩展到稳健支持**双向**动态压缩；**DeCo-SGD**（Lu et al., 2025）用 **Nested Virtual Sequence** 工具分析"同步陈旧度"与"数据压缩"的时间交互，按实时网络状况动态调整两者；**LAGS-SGD**（Shi et al., 2020）把**逐层自适应稀疏化**协议无缝综合进同步梯度下降。

**Gap。** 三方对比几乎就是权衡①的精确表述：**量化**压缩比前所未有，但在复杂优化地形中频繁遭遇**精度瓶颈**；**稀疏化**保持精确幅值，却系统性带来**索引编码的硬件开销**；**低秩投影**能捕捉全局数学结构，但本质上要求为**持续矩阵分解**付出高计算成本。核心 gap 是**非独立同分布数据下混合压缩算子的最优、数学上连续的调度**尚属理论空白，故须建立统一框架动态平衡统计采样效率、本地计算开销与全局网络带宽约束。（原文在量化那一类未单列固有局限，只在总结对比时点出精度瓶颈。）

#### 3.5.2 Local Update Strategies（本地更新策略）

**动机。** 本节处理权衡②。其通用框架**改造同步梯度下降**：允许每个节点在全局聚合前先执行**一串本地梯度步**。核心改进维度是最小化通信开销，同时缓解数据异构的不利影响；演化逻辑从**基本步长延迟**走向**精密方差校正**与**动态调度**，处理"本地计算效率与全局收敛稳定性的根本权衡"。

**（1）Local SGD**：同步前执行多次本地迭代。**SQuARM-SGD**（Singh et al., 2021）只在超过某阈值时才传压缩模型差分，并通过对**加权图求平均**达成共识；**SLOWMO**（Wang et al., 2020c）用**嵌套循环**集成周期性动量更新，把本地步序列与全局同步、动量更新桥接起来。**优势**是通信频率显著下降、本地收敛加速；**固有局限**是在 non-IID 状态下**易受客户端漂移伤害**——未校正的局部轨迹逐步偏离全局最优。这就是权衡②的原始形态。

**（2）Local momentum updates（本地动量更新）**：在更新规则的统一形式推导中引入**时间平滑**。**SQuARM-SGD** 在这一维度把 **Nesterov** 加速（Nesterov, 1983）与多本地步结合；**FAdamGC**（Chen et al., 2025a）把**梯度校正放在动量计算之前**，显式消解客户端模型间的不一致。**优势**是经稳定优化路径提升可扩展性；**局限**是要在所有节点维护动量缓冲，**本地内存占用上升**。

**（3）Variance-reduced local updates（方差缩减的本地更新）**：通过引入**校正项**修改本地更新方程以正面应对漂移。**BVR-L-SGD**（Murata and Suzuki, 2021）使用可与 **SARAH** 类比的方差缩减梯度估计器，**联合**最小化本地迭代随机方差与全局梯度偏差；**SCAFFOLD**（Karimireddy et al., 2020）用**控制变量（control variates）**近似并矫正服务器与客户端间的更新方向。理论分析表明这些策略能在**任意数据分布**下恢复集中式算法的最优收敛率。**优势**是从理论上严格消除数据异构造成的优化偏差；**固有局限**是通信与内存开销大幅放大——框架强制同步**维度与模型参数等价**的辅助控制变量，即每个客户端要多传一整份模型大小的状态。

**（4）Local-global hybrid updates（本地–全局混合更新）**：在结构维度上分解优化过程，本地累积部分参数、全局只同步关键成分。**DeCo-SGD**（Lu et al., 2025）动态协调"本地步延迟"与"全局压缩比"；**HybridSGD**（Devarakonda and Kannan, 2025）在**二维网格拓扑**中沿行维度做本地多步更新、沿列维度做批同步；**Ringleader ASGD**（Maranjyan, 2026）用结构化**两阶段更新机制**界定陈旧梯度。**优势**是绕开全参数同步瓶颈、显著减轻纯本地步的偏差累积；**局限**是实现复杂度上升，限制其在高度非对称拓扑下的适用性。

**（5）Adaptive local steps（自适应本地步数）**：从固定同步间隔转向**动态时间调度**。**FedCET**（Liu and Wang, 2025）用**学习率自适应加权**约束目标漂移并降通信成本；**AbsSADMM**（Jin et al., 2025a）依优化路径的历史轨迹**调节 batch size**；**DP-PASGD**（Hu et al., 2020）把差分隐私约束与**周期性平均**结合，平衡模型精度、计算效率与数据隐私。**优势**是高度异构场景中灵活性与资源利用率更优；**固有局限**是依赖**启发式阈值机制**，难以建立紧的最坏情况收敛保证。

**Gap。** 里程碑式转变是"从启发式减少通信，转向有理论依据的动态漂移校正"。但现有理论收敛分析**严重依赖有界梯度假设（bounded gradient assumptions）**，且缺少统一框架，能在**统计异构与系统资源非对称同时极端**时实现**精确共识**，而不付出辅助变量通信或超参数优化的过高代价。

#### 3.5.3 Decentralized Communication（去中心化通信）

**动机。** 本节处理"中央节点瓶颈"。该策略用于优化**对等（peer-to-peer）交互模式**与全局共识。**通用框架**把全局目标表述为所有节点上聚合局部损失函数的最小化，而**更新规则依赖一个混合矩阵（mixing matrix）支配信息交换**——这是替代中心服务器的数学核心物件。核心改进维度：优化通信图、放松同步约束、提升网络可靠性、集成严格保密保证。

**（1）Neighbor communication topology（邻居通信拓扑）**：调整交互图以平衡本地梯度更新与全局共识。**LD-SGD**（Li et al., 2019）在本地更新与**去中心化一阶步**间引入交替方案，为非凸且 non-IID 环境建立分析框架；**DAT-SGD**（Eisen et al., 2025）经 **anytime stochastic gradient descent** 框架扩展并行度阈值——用**平均查询点**缓解局部不一致、缩小共识距离（consensus distance），改进并行度理论界。

**（2）Distributed consensus optimization（分布式共识优化）**：在保持精确共识前提下优先降通信开销。**LT-ADMM**（Ren et al., 2025）用本地训练降低通信频率，本地更新用标准随机梯度下降（Robbins and Monro, 1951）；**DLAS-R-FTC**（Rikos et al., 2025）通过**有限时间协同**实现自动步长选择。这类方法通过适配底层图结构消除网络异构。

**（3）Asynchronous decentralization（异步去中心化）**：**放松严格全局时钟**。**A(DP)² SGD**（Xu et al., 2021）绕开等待落后者（straggler）的效率瓶颈，经灵活拓扑适配与异步更新策略，在并行度扩展与本地一致性间取得平衡。

**（4）Communication fault robustness（通信故障鲁棒性）**：**DES-LOC**（Iacob et al., 2025）把**模型参数的同步间隔与优化器状态的同步间隔解耦**——优化器状态传得更稀疏，在保持收敛率的同时显著降低通信成本，为高通信开销下的训练提供可扩展、容错的方案。

**（5）Privacy-preserving decentralization（隐私保护去中心化）**：**Interleaved-ShuffleG**（Jiang et al., 2025a）**混合私有与公开数据**，通过代理目标与自适应噪声注入最小化经验超额风险；**FedLAP-DP**（Wang et al., 2023）用**合成样本**近似并聚合本地损失地形为统一全局表示，在不引入额外计算成本下集成**记录级差分隐私**。

**Gap。** 逐类代价很清楚：**同步拓扑方法**理论保证直接，但受落后者瓶颈的固有局限；**共识驱动方法**改善资源利用效率，却受困于延迟梯度与二阶近似的管理复杂度；**异步方法**最大化硬件利用率，代价是引入**梯度陈旧度**；**鲁棒性框架**保护训练免受节点故障影响，但引入显著算法复杂度；**隐私保护协议**防止数据泄漏，却注入固有优化噪声。核心 gap 是缺少统一理论框架，能**同时**达成最优通信复杂度、严格隐私保证与精确共识，且不损害优化方法在**极端异构环境**下的收敛性质。

#### 3.5.4 Federated Learning Optimization（联邦学习优化）

**动机与形式化。** 本节处理跨**分散客户端**的协同训练，形式化最完整：**分布式经验风险最小化** $\min_{\theta}\ \sum_{k=1}^{K} p_k F_k(\theta)$，其中 $p_k$ 是客户端权重，$F_k(\theta)$ 是局部目标函数。该目标可嵌入统一视角 Eq. (7)：把 $f$ 取为加权和 $\sum_k p_k F_k(\theta)$，并把**客户端级交互**（本地更新、聚合、隐私机制）纳入场景变换 $T_{\text{scenario}}$——它作用于**逐客户端梯度估计** $\tilde{g}_t^{(k)}$，产出聚合梯度 $\hat{g}_t$。这个写法把"联邦"的全部特殊性压进一个算子：聚合规则、客户端采样、加不加噪声，全在 $T_{\text{scenario}}$ 里。为应对**统计异构与系统异构**，论文归纳三条改进维度：**一阶动量加速**、**二阶曲率近似**、**零阶或高级结构适配**（含客户端采样与个性化）；目标是协调全局收敛与个性化，同时缓解通信瓶颈与**客户端漂移**。

**（1）First-order methods and unified momentum derivation。** 统一形式化把全局更新纳入历史梯度：$m_t = \beta m_{t-1} + (1-\beta)g_t$。**FEDAC**（Yuan and Ma, 2020）通过**前置梯度校正**及控制变量与动量更新间的动态协调，避免本地动量累积造成的异构偏差；**FedLion**（Tang and Chang, 2024）在客户端用**全局动量初始化本地动量**，经多步本地更新后再上传聚合——符号化梯度（symbolized gradients）降低通信开销，全局动量约束局部异构漂移；**FedMuon**（Liu et al., 2025）引入动量聚合以强制本地与全局更新方向对齐；**AdaFedAdam**（Ju et al., 2024）基于本地更新以**确定性加权**融合局部动量，全局动量按"聚合确定性"自适应调节；**FADAS**（Wang et al., 2024h）通过**延迟自适应学习率**动态调整全局动量权重；**FedRepOpt**（Lau et al., 2024）让本地动量累积与服务器学到的全局最优结构对齐；**FLeNS**（Gupta et al., 2024）把 **Nesterov** 加速（Nesterov, 1983）与 **Hessian sketching** 结合，向二阶技术过渡。

**（2）Federated second-order optimization。** 二阶方法引入**曲率信息**，代表性形式是**近似逆 Hessian 矩阵或 Fisher 信息矩阵（FIM）**来缩放梯度更新。**FAGH**（Sen et al., 2024）用近似的**全局 Hessian 矩阵**，绕开完整 Hessian 存储的必要性以加速收敛；为增强个性化，**pFedSOP**（Sen and Mohan, 2025）基于 **Gompertz 函数**做个性化更新、构造**正则化 FIM**；**BFEL**（Nawaz et al., 2025）把用 FIM 保存客户端特有知识的 **FedCurv** 集成进区块链框架，用于可信医疗应用。

**（3）Zeroth-order and advanced structural adaptations。** **客户端采样**通过改进选择过程缓解通信开销与更新偏差：**FedSTaS**（Slessor et al., 2024）把**基于梯度的分层**与数据采样结合；**FAdamGC**（Chen et al., 2025a）通过**选择性追踪**实现采样优化，核心是把"训练客户端采样"与"控制变量更新的采样"**解耦**；**FedOne**（Wang et al., 2025a）在**黑盒 prompt 学习**中每轮只激活**单个客户端**。**个性化**：**FedIvon**（Pal et al., 2024）在贝叶斯框架下用**高斯分布**近似局部后验；针对**块循环（block-cyclic）数据**，**MM-PSGD** 与 **MC-PSGD**（Ding et al., 2024）通过互补训练策略构造**块特定预测器**。**聚合阶段**：**Kuramoto-FedAvg**（Muhebwa et al., 2025）把聚合**重新表述为同步问题**，借用 **Kuramoto 模型**依各客户端与全局更新的**相位对齐程度**动态调整其更新权重，以降低客户端漂移。

**Gap。** **一阶方法**算法简单但**缺乏几何感知**；**二阶方法**能有效捕捉几何结构，却受**矩阵求逆成本**约束；**结构适配**灵活性最大，但在异步环境下有**不稳定风险**。核心 gap 是缺少统一理论框架，能在**动态、非平稳的异构性**下**同时界定泛化误差与通信复杂度**。

#### 3.5.5 Communication Scheduling & Threshold（通信调度与阈值）

**动机与形式化。** 本节处理权衡③。通信调度与阈值技术编排**数据传输时机与优先级**，依**更新的重要性**与**网络状况**调节信息交换，以缓解带宽拥塞、减轻延迟冲击。论文的抽象很干净：第 $t$ 次迭代的通信过程抽象为算子 $C(\Delta_t, S_t)$，其中 $\Delta_t$ 是参数或梯度更新，$S_t$ 是**环境状态**（如带宽或延迟）。注意与 §3.5.1 的区别：那里的 $C(\cdot)$ 只看更新向量本身（"发什么"），这里有第二个自变量 $S_t$（"何时发、按什么顺序发"）。这些范式可服务于一阶、二阶、零阶各类方法，核心是设计算子 $C$ 使其**自适应地过滤、重排或延迟** $\Delta_t$ 的传输。

**（1）Significance threshold communication（显著性阈值通信）**：侧重更新的空间或时间过滤。有别于固定间隔同步，**SPARQ-SGD**（Singh et al., 2022）采用**事件驱动**机制，通信算子形式化为指示函数 $I(\lVert \Delta_t \rVert > \tau)$，即只有 $H$ 个本地步后参数更新**幅度超过临界阈值 $\tau$** 时才触发通信。它**把降低通信频率置于连续同步之上**，适合带宽受限环境，但**固有地冒着丢失细微梯度信息的风险**。

**（2）Priority communication（优先级通信）**：从"减量"转向"结构优化"。阈值方法靠**丢弃数据**降通信量，而 **DLCP**（Wang et al., 2020b）优化 $\Delta_t$ 的**传输顺序**：算子 $C$ 充当**置换矩阵**，依赋予不同梯度层的**优先级分数**重排传输序列，实现细粒度、**包级（packet-level）**优先级策略，在不丢数据的前提下最大化网络利用率。理论上它**保持全梯度的无偏性**，故相比阈值剪枝能维持**更紧的收敛界**；代价是**计算优先级分数的开销带来额外延迟约束**。

**（3）Delay-aware scheduling（延迟感知调度）**：**DeCo-SGD**（Lu et al., 2025）引入环境自适应性：算子 $C$ **同时动态调节压缩比与陈旧度容忍度**以回应波动的网络状况 $S_t$，解决外部网络方差引起的收敛不稳定。其**理论边界在于有界延迟假设**：收敛率随最大通信延迟**平滑退化**，而非突然发散。

**Gap。** 演化逻辑是"从被动过滤走向主动的环境适配"：从**数据无关的同步**，到 SPARQ-SGD 的**数据感知阈值化**，再到 DLCP 的**结构感知调度**，最终到 DeCo-SGD 的**环境感知动态调制**。优劣对比是本节的权衡浓缩：**阈值方法**带宽最省，但遭受**精度退化**；**优先级方案**保持数学精度，但要为**排序**付出可观计算开销；**延迟感知调度**鲁棒性最优，但需要**复杂的系统级 profiling**。核心 gap 是缺少统一的**协同设计（co-design）框架**，能在严格收敛保证下**联合优化**阈值参数、优先顺序与延迟容忍度，尤其在异构且高度动态的网络拓扑中。

#### 3.5.6 Distributed Hybrid Optimization（分布式混合优化）

**动机与形式化。** 本节是收尾式的"集成"：融合**量化、曲率估计与隐私协议**等多种算法组件，核心改进维度从**基础通信削减**延伸到**高级曲率适配与隐私保护**。前三节各解决一个瓶颈，这一节试图把它们拼起来——但拼起来会带来新的复合代价。统一形式化推导是：在聚合之前，对**累积的本地梯度或模型差分**施加一个数学压缩算子。这与 §3.5.1 的 $C(g_t)$ 略有不同：这里压缩的是"累积的"更新，天然与多步本地更新耦合。

**（1）Compression & local updates。** **SQuARM-SGD**（Singh et al., 2021）把多步本地更新与**任意压缩策略**集成于对等网络；**Qsparse-local-SGD**（Basu et al., 2020）把**量化、稀疏化与本地计算连同误差补偿**统一起来，支持同步与异步两种模式。**优势**是带宽需求显著下降；**固有局限**是**压缩方差的累积**可能导致收敛精度退化——这正是权衡①与②叠加后的表现形式。

**（2）Quantization & low-rank。** **LQ-SGD**（Li et al., 2025a）把**对数量化**引入 PowerSGD 框架，在维持精度的同时进一步降低通信成本；该方法结合**正交压缩技术**平衡通信效率与优化性能。**优势**是结构压缩与精度压缩的**协同效应**；**局限**是每轮通信中**分解低秩矩阵所需的计算开销增加**。

**（3）Second-order & distributed。** 该方向针对两个痛点：完整二阶方法的高成本，以及一阶方法在**病态（ill-conditioned）地形**中的慢收敛。**Fed-Sophia**（Elbakary et al., 2024）用 **Hessian 对角线的轻量估计**，无需计算完整 Hessian 即可纳入曲率信息，兼顾通信与计算效率。**优势**是在复杂地形中加速收敛；**局限**是对**对角预条件子在异构数据分布下的估计方差**敏感。

**（4）Privacy & distributed。** **TOP-DP**（Guo et al., 2021）通过**差分隐私**框架严格验证理论隐私保证，适用于图像分类等视觉任务（它在 §3.4.6 零阶分布式中已出现，本节把它当作"隐私组件"重新纳入混合框架）。**优势**是可证明的信息泄漏界；**局限**是**噪声注入规模与模型最终效用之间的固有权衡**。

**Gap。** 演化是"从启发式的单维压缩，走向有理论保证的多维联合设计"。重大空白依然存在：缺少统一理论框架，能**同时**界定**极端压缩引入的收敛误差**、**二阶曲率的估计不准**、以及**差分隐私的严格约束**——尤其在**严重数据异构**条件下。

---

**三组权衡的收束。** 论文反复回答同一组问题：压缩算子 $C$ 用多大力度（权衡①，§3.5.1 与 §3.5.6 的量化/稀疏/低秩），本地步数 $H$ 走多远（权衡②，§3.5.2 的 Local SGD 到 SCAFFOLD、§3.5.4 的漂移校正），通信如何被调度（权衡③，§3.5.5 的阈值/优先级/延迟感知、§3.5.3 的同步/异步与拓扑）。三个子节末尾的 gap 有共同形状：**每一维单独的最优都已清楚，但三者联合的理论框架尚未建立**——这正是论文为后续章节（§3.6 隐私、§3.7 优化器状态压缩、§3.8 自动化优化器设计）留下的接口。

---

### Sec 3.6 Privacy-Preserving Optimization（p40–43）——隐私保护的五个子主题

论文把这一块分成两半：**基础算法框架（fundamental algorithmic frameworks）**回答"私有化机制本身怎么造"；**权衡管理策略（trade-off management strategies）**回答"隐私机制对收敛的副作用怎么补救"。区分标准就是研究目标落在私有化机制上，还是落在副作用缓解上。

核心范式是差分隐私优化：不新造优化器，而是改造现成的优化器（vanilla optimizer），在加入严格隐私保证的同时处理随之而来的计算低效。私有化更新规则为 $\theta_{t+1} = \theta_t - \eta_t \left( \frac{1}{B} \sum_{i \in B_t} \Pi_C(\nabla J(x_i, \theta_t)) + N(0, \sigma^2 C^2 I) \right)$。其中 $B_t$ 是第 $t$ 步的 mini-batch、$B$ 是其大小，$\Pi_C$ 是**逐样本梯度裁剪算子**（per-example gradient clipping），把每个样本梯度的范数压进阈值 $C$；$\sigma$ 是**噪声乘子**（noise multiplier）。两个容易看漏的细节：噪声标准差写的是 $\sigma C$ 而非 $\sigma$，即噪声强度自动跟随裁剪阈值缩放；且噪声加在"batch 内已裁剪梯度的平均值"之上，**每步只注入一次**，并非给每个样本各加一次。若把"裁剪＋加噪"这一步抽成单个算子，即 $T(\tilde{g}_t) = \mathrm{Clip}(\tilde{g}_t, C) + N(0, \sigma^2 C^2 I)$。

$C$ 与 $\sigma$ 分工明确：$C$ 界定**敏感度**（sensitivity，单个样本最多能把更新推动多少），$\sigma$ 是**隐私-效用旋钮**。本节只用 $\sigma$ 表述，并未写出隐私预算 $\epsilon$；按差分隐私的通用结论（标准背景知识，非原文公式），$\delta$ 固定时 $\sigma$ 越大则 $\epsilon$ 越小、保护越强、效用越差。论文强调该范式可无缝嵌入全文的统一形式 Eq. (7)，各类方法只是它在不同优化阶数上的展开。

总述另点出四条延伸线：**gradient noise injection**（自适应强度、标量注入、noise-robust 设计、让噪声沿梯度方向对齐的几何扰动）；**trade-off management**（动态噪声调度、后处理精修）；**privacy-aware gradient clipping**（全局裁剪、基于梯度统计的自适应裁剪、曲率感知裁剪）；**federated privacy enhancement**（protection protocols 用梯度混洗混合私有与公共数据，structural adaptations 用 SVD 重参数化或 SAM 类优化器抵抗聚合噪声，low-rank adaptations 消除异构下的截断偏差）。

#### 3.6.1 差分隐私优化（Differential Privacy Optimization）

**要解决的问题**：隐私保护训练中"严格数据保护"与"模型保真"天然对立；本节收集化解这对矛盾的算法框架，手段是**自适应机制加误差修正**，以压住噪声注入与梯度偏差的副作用。

**DP-SGD variants** 是主干。vanilla DP-SGD（Yang et al. (2022)）被点名三个缺陷——噪声冲击大、优化器估计有偏、计算低效，修补必须按优化阶数分别下手。一阶：**TOP-DP**（Guo et al. (2021)）用拓扑感知（topology-aware）的噪声复用与时间衰减降低噪声方差；**DPIS**（Wei et al. (2022)）用重要性采样加自适应裁剪换取更低方差与隐私代价；**DP-λCGD**（Kalinin et al. (2026)）引入轻量的迭代噪声相关性。二阶与动量适配：**DP-MicroAdam**（Hudişteanu et al. (2025)）用 top-k 稀疏化滤掉低信噪比更新以降偏差，再用误差反馈（error feedback）累加公式找回丢失信号，代价是额外的稀疏化与累积开销；**Corrected DP-Adam**（Tang and Lécuyer (2023)）修正差分隐私下二阶矩估计的偏差；**DP-AdamW**（Sun et al. (2025a)）用解耦让正则强度不依赖瞬时梯度噪声；**DP-aware AdaLN-Zero**（Huang et al. (2026b)）缓解条件扩散模型中 conditioning 诱发的重尾梯度导致的过度裁剪。零阶：**SPARTA**（Makni et al. (2025)）用结构化扰动换取效率；**DP-SGD-JL**（Bu et al. (2021)）借 Johnson–Lindenstrauss 投影降低梯度范数的计算开销；**DOPPLER**（Zhang et al. (2024a)）用频域滤波提升信噪比；**RaCO-DP**（Yaghini et al. (2025)）把梯度上升改造到 rate-constrained 任务并给出收敛保证。

**Joint differential privacy**：**ANSGD**（Huang et al. (2023)）在随机凸优化中推导 population loss 的理论上界，并用多分类实验验证。**Dynamic noise scheduling**：**TOP-DP** 用噪声复用加时间衰减让噪声随训练递减；**DC-SGD**（Wei et al. (2025)）用梯度范数的 DP 直方图动态设定裁剪阈值。**Privacy-utility balance**：**TOP-DP** 以噪声复用维持效用；**Interleaved-ShuffleG**（Jiang et al. (2025a)）在混合框架里同时使用私有与公共数据。

**Open problem（原文自陈）**：缺少统一理论框架，能在不同优化阶数上最优界定 privacy-utility trade-off，且既不依赖大量超参调优，也不预设数据分布知识。论文也承认固有代价：收敛率与模型效用的下降无法避免，高度非凸的 landscape 上尤甚。

#### 3.6.2 梯度噪声注入（Gradient Noise Injection）

**要解决的问题**：加噪是差分隐私落地的那一步，噪声加多少、往哪个方向加，同时决定隐私是否成立与模型能否收敛。论文的形式化很干净：$\theta_{t+1} = \theta_t - \eta (\mathrm{Clip}(g_t) + N(0, \sigma^2 I))$，即先裁剪有界梯度，再叠加各向同性高斯噪声；与总述那式相比，这里噪声尺度直接由 $\sigma$ 给出，阈值被吸收进 $\mathrm{Clip}$。近期方法分两条线：用自适应与几何机制校准噪声，用二阶、零阶近似增强优化韧性。

**Adaptive noise intensity**：一阶优化传统上只加均匀的向量扰动（Yang et al. (2022)）；**DC-SGD**（Wei et al. (2025)）改用梯度范数的统计分布来调裁剪阈值，优化"计算-隐私"权衡。**Geometric noise injection**：**GeoDP-SGD**（Duan et al. (2025)）把扰动结构化分解，将梯度**方向**与**幅度**分开处理，从而对各向同性噪声有更细粒度的控制，提高结构性效率。**Noise robust optimization**：**DP-Adam**（Tang and Lécuyer (2023)）修正标准 Adam（Kingma (2015)）处理含噪梯度时的二阶矩估计偏差；**DP-FedSAM**（Shi et al. (2023)）集成 SAM（Foret et al. (2021)）去定位更平坦的损失区域，天然抵抗梯度扰动、稳定高噪声下的收敛。**Scalar noise injection**：在通信受限的纵向联邦学习中，**DPZV**（Zhang et al. (2025b)）用标量噪声替代高维向量扰动，放弃显式的 FO 方向导数却在理论上匹配其收敛率，兼顾严格隐私、低通信开销与分布式显存效率。

**Open problem（原文自陈）**：三种代价互相牵制——自适应方法能实时校准，但每步计算复杂度上升；鲁棒优化器稳定收敛，但需严格超参调优，否则欠拟合；零阶方法通信最省，但收敛结论依赖严格凸假设。"同时保证紧的理论收敛、极小显存占用、且在异构非凸 landscape 上仍能几何感知校准的统一框架"仍然缺席。

#### 3.6.3 隐私-效用权衡（Privacy-Utility Tradeoff）

**要解决的问题**：DP 造成的效用损失从哪里补回来。已有工作归为两个维度——**梯度裁剪**与**噪声处理**，即分别在更新的入口与出口做文章。入口一侧是 **DC-SGD**（Wei et al. (2025)）：在裁剪阶段动态调整，用梯度范数的差分隐私直方图自适应设定裁剪阈值，既减轻人工调参负担又提升优化效率。出口一侧是 **DP-LSSGD**（Wang et al. (2020a)）：专注后处理阶段的信号恢复，引入基于拉普拉斯平滑（Laplacian smoothing）的实时去噪机制，抹平被高斯噪声污染的梯度。论文的评语是：二者分别在"输入端的裁剪阈值"与"输出端的含噪梯度"上做优化，都能在维持严格 DP 保证的同时提升模型效用。

**Open problem**：本节篇幅很短，原文没有为它单独列出 gap 段落；与之最贴近的缺口陈述出现在上一节末尾，即缺少跨优化阶数最优界定隐私-效用权衡的统一理论框架。因此这一节更接近两条工程路线的并列展示，而非自带未解问题的独立方向。

#### 3.6.4 联邦隐私增强（Federated Privacy Enhancement）

**要解决的问题**：协作学习中聚合过程本身就是泄露通道——上传的本地更新可被反推，本节用专门协议在聚合阶段加固机密性。形式化框架为 $\theta_{t+1} = \theta_t - \eta \sum_{i=1}^{K} w_i M(\Delta \theta_t^{(i)})$，其中 $M(\cdot)$ 是对本地更新的**随机化隐私机制**，$w_i$ 是聚合权重。它嵌入统一视角 Eq. (7) 的方式是：把本地更新 $\Delta \theta_t^{(i)}$ 解读为客户端 $i$ 的梯度估计 $\tilde{g}_t^{(i)} = E_i(f, \theta_t^{(i)}, \xi_t^{(i)})$，再把隐私机制与聚合一并塞进场景变换 $T_{\mathrm{scenario}}$，于是 $\hat{g}_t = \sum_i w_i M(\tilde{g}_t^{(i)})$。理论上的关键结论是：**只要控制住分布式噪声的方差，算法就能逼近非隐私基线的收敛率**。

方法分两类。**架构协作（Architecture collaboration）**：**Interleaved-Shuffle**（Jiang et al. (2025a)）重构协作架构与传输机制，用加密的梯度传输把噪声校准分摊到客户端与服务器两侧；论文指出这种结构性改造给聚合函数定了敏感度上界，从而在不改变一阶优化基本动力学的前提下降低泄露风险，代价是通信开销高。**优化器层面的鲁棒性（Optimizer-level robustness）**：**DP-FedSAM**（Shi et al. (2023)）不修改传输结构，而是在聚合造成噪声放大时集成客户端侧的 SAM 优化器去识别局部 loss landscape 中的平坦极小值；扩大解的搜索空间换来 landscape 感知的鲁棒性并保留理论收敛保证，代价是需要相当可观的本地算力。

**Open problem（原文自陈）**：还缺一个统一框架，能在严格隐私约束下**同时**最小化通信开销、优化局部 landscape 的平坦度、并适应异构的数据分布。

#### 3.6.5 隐私感知梯度裁剪（Privacy-Aware Gradient Clipping）

**要解决的问题**：给差分隐私与随机优化中的敏感度定上界。裁剪是全部隐私机制的地基——加噪量正比于敏感度，阈值不收紧，噪声就必须开大。形式化为 $\theta_{t+1} = \theta_t - \eta \hat{g}_t$，其中 $\hat{g}_t = \min(1, \frac{C}{\lVert \tilde{g}_t \rVert}) \tilde{g}_t$：当 $\lVert \tilde{g}_t \rVert \le C$ 时原样通过，一旦超过 $C$ 就整体按比例缩放到范数恰为 $C$。改进方向是从静态全局约束，走向动态统计自适应与曲率感知的重参数化。

**Global gradient clipping**：**AClipped-dpSGD**（Jin et al. (2024)）对凸问题用一次性的全局裁剪；**Logit-DP**（Kong et al. (2025)）在计算 loss 之前就裁剪 logit 梯度。二者场景不同，但数学上都依赖**预先设定**的全局阈值来限制敏感度、压低噪声，并据此建立基础收敛界。**Adaptive clipping**：**DC-SGD**（Wei et al. (2025)）构造梯度范数的 DP 直方图去动态逼近真实分布；**Stable-SPAM**（Huang et al. (2025a)）用最大梯度的指数滑动平均做逐元素裁剪；数据驱动使其适应不同尺度的梯度，在隐私效率与实证稳定性上都胜过静态裁剪。**Curvature-aware clipping**：传统做法对所有参数维度施加同一缩放，忽略局部 landscape 几何而次优；**Sophia**（Liu et al. (2024a)）改用曲率估计对裁剪操作做预条件，让阈值在结构上感知参数空间，改善复杂任务中的收敛稳定性与更新效率，代价是昂贵的曲率近似。

**Open problem（原文自陈）**：全局裁剪实现简单，但在异质的训练阶段之间表现次优；自适应裁剪改善了权衡，却引入辅助计算开销；曲率感知裁剪最大化 landscape 利用，却要付出昂贵的曲率近似。真正的缺口是**还没有一个统一的裁剪算子**，能在严格 DP 约束下把零阶的显存效率与二阶的曲率感知无缝整合。

### Sec 3.7 Memory-Efficient Optimization（p43–46）——显存高效的六个子主题

论文分两条路线：**结构性设计策略（structural design strategies）**改写算法本身；**数值压缩技术（numerical compression techniques）**改写数据的表示方式。结构性一侧含低显存优化器设计（无状态化 Sane (2025)、二阶矩近似存储 Shazeer and Stern (2018)、状态量化或分块）、无状态优化（实时梯度统计 Zaheer et al. (2018)、参数驱动更新 He et al. (2025a)、曲率估计 Shang et al. (2018)），以及靠无状态反向传播与选择性更新支撑超大模型的显存高效微调。数值压缩一侧含低秩方法（自适应投影、基于张量分解的低秩梯度存储）与优化器状态压缩（稀疏压缩 Li et al. (2019)、低秩近似、状态共享 Ma et al. (2025)）。

这里必须先补一个"优化器状态到底占多少显存"的背景（原文只作定性描述，未给具体字节数，以下按通行实现口径折算）。设参数量为 $N$：SGD 只需存参数；而 Adam 还要为每个参数各存一份一阶动量 $m$ 与二阶动量 $v$，两者都与参数张量同尺寸，所以**优化器状态本身就有 $2N$ 个标量，等于参数量的两倍**。按常见的混合精度实现折算：fp16/bf16 参数 2 字节、fp16 梯度 2 字节、fp32 主权重 4 字节、fp32 一阶动量 4 字节、fp32 二阶动量 4 字节，合计约 16 字节/参数，其中优化器状态独占 8 字节/参数。也就是说，从 SGD 换到 Adam，显存里多出来的不是零头，而是与参数量成正比的另一整份拷贝。

有了这个坐标，"各种压缩路线分别压的是哪一块"就清楚了：**量化**（4-bit Shampoo、FlashOptim）压的是状态数值的**位宽**；**低秩/子空间**（AdaRankGrad、Adapprox、SUMO、GWT、HELENE、TeZO）压的是状态的**秩**，只存两个小因子而非完整矩阵；**分块与共享**（Adafactor 的行列共享、Adam-mini 的 Hessian 结构分块、AdaAct 的 neuron-wise 共享、SPAM 与 MICROADAM 的稀疏化）压的是**独立状态变量的个数**，让多个参数共用一份状态；**无状态**（AlphaGrad、SWAN、Sophia 的对角 Hessian、SGD-SaI、BFE/AdaBFE）把状态数从 $2N$ 直接降到零或常数级。另有一类（tpSGD、LOMO、Sparse-MeZO、LeZO）压缩的根本不是优化器状态，而是**反向传播的范围与梯度的生命周期**，即激活与梯度的显存。

#### 3.7.1 低显存优化器设计（Low-Memory Optimizer Design）

**要解决的问题**：削减训练过程固有的存储开销，手段是状态量化、状态近似与结构划分，以便在有限硬件上做可扩展的高吞吐学习。这一节的五条子路线正好对应上面那张"压缩什么"的地图。

**Stateless optimizers**：**AlphaGrad**（Sane (2025)）最激进，彻底不存历史梯度，仅靠一个 steepness 参数完成梯度变换，为"不保留历史窗口"的显存下界提供基线。**Approximate second-moment storage**：**Adafactor**（Shazeer and Stern (2018)）不删状态，而用矩阵分解降低二阶矩存储开销，靠**近似**而非**消除**在效率与性能之间架桥。**Quantized optimizer states**：**4-bit Shampoo**（Wang et al. (2024f)）保留预条件器特征向量矩阵的结构但把它们量化到 4 bit；**FlashOptim**（Ortiz et al. (2026)）结合基于 ULP 的权重切分与定制的 companding 函数，对动量与方差状态做 8 bit 量化，证明精度降低后仍能维持高性能的状态管理。**Blocked optimizer states**：**Adam-mini**（Zhang et al. (2025d)）按 Hessian 结构把参数分块，每块只分配一个学习率，以远少于 AdamW 的状态量匹配其性能——它优化的是**参数分组方式**，而不只是压缩单个状态值。**Target projection strategy**：**tpSGD**（Lomnitz et al. (2023)）重新定义训练范式，用随机标签投影生成本地目标以实现逐层训练，从而不再需要全局反向传播；它改写的是**梯度传播的流向**，而非优化器的存储机制。

**Open problem**：本节原文没有给出独立的 gap 段落（3.7 全节的写法是分类叙述，3.7.3 至 3.7.6 同样如此）。就这五种机制而言，核心张力是：无状态路线省得最彻底却丢掉了自适应性，量化路线压得最直接却受数值精度约束，分块与近似路线保住了性能却引入额外的结构假设与实现复杂度，三者不可兼得。

#### 3.7.2 低秩算法（Low-Rank Algorithms）

**要解决的问题**：用自适应投影与随机近似，把梯度或优化器统计量压进低维子空间，同时缓解显存约束与通信开销，在资源效率与近似保真度之间取平衡。**Low-rank projection**：**AdaRankGrad**（Refael et al. (2025c)）对梯度施加低秩投影以削减 Adam 的显存，用随机 SVD 高效近似梯度子空间，并把自适应秩缩减与 Adam 的学习率结合起来——它是"用高效矩阵分解降低显存"的基线（与 GaLore 一类的低秩梯度投影同族，但本文这几页并未直接点名 GaLore）。**Dynamic rank adjustment**：**Adapprox**（Zhao et al. (2024b)）改为针对最耗显存的二阶矩，并区别于静态低秩方法——引入自适应秩选择，让近似的秩在训练中动态调整，更灵活地平衡优化精度与显存开销。**Low-Rank&quantization**：**QuZO**（Zhou et al. (2025)）用随机量化扰动补充低秩适配，专门降低梯度估计偏差；**LQ-SGD**（Li et al. (2025a)）把 Vogels et al. (2019) 的低秩框架（PowerSGD 一脉）扩展为对数量化。论文的结论是：低秩约束与量化结合，可同时降低通信成本与显存占用而不损害模型性能。

**Open problem**：原文未单列 gap；留下的张力是"秩的选取"与"量化的精度损失"这两个近似源叠加时，缺乏统一的误差刻画。

#### 3.7.3 优化器状态压缩（Optimizer State Compression）

**要解决的问题**：用稀疏化、低秩近似与状态共享削减优化变量的显存占用——对照上面的账目，压的就是 $2N$ 那份状态本身。**Sparse state compression**：**SPAM**（Huang et al. (2025b)）把 spike-aware clipping 集成进 Adam，用稀疏动量在维持性能的同时削减显存；**MICROADAM**（Modoranu et al. (2024)）在梯度**进入优化器状态之前**先做压缩，并保持有竞争力的收敛保证。**Low-rank state approximation**：**Adapprox**（Zhao et al. (2024b)）对 Adam 的二阶矩使用随机低秩矩阵近似加自适应秩选择，针对的正是全秩二阶矩估计的高显存与高计算开销。**State sharing**：**Adafactor**（Shazeer and Stern (2018)）对矩阵型参数不再逐元素存状态，而让同一行或同一列的参数共享一份；**AdaAct**（Seung et al. (2024a)）为具有相同输入特征的神经元共享学习率。论文的总结是：跨参数的状态复用提升了优化的效率、稳定性与适应性。

**Open problem**：原文未为 3.7.3 单列 gap。三条路线的共同未解问题是：稀疏与低秩只保留状态的部分信息，共享则强行让参数耦合，三者都缺少异构训练阶段下"何时该保真、何时可压缩"的统一判据。

#### 3.7.4 低秩梯度存储（Low-Rank Gradient Storage）

**要解决的问题**：用压缩的低维表示近似完整的矩阵梯度，手段包括子空间投影、动态秩自适应与张量分解。**Gradient low-rank projection**：两个方法目标相同（把高维梯度投影到低维子空间）而数学根基不同——**SUMO**（Refael et al. (2025b)）走矩阵分解路线，对逐层梯度做截断 SVD 来构造自适应投影子空间；**GWT**（Wen et al. (2025)）走信号处理路线，用小波变换压缩 Adam（Kingma (2015)）的状态；论文把这条对比提炼为**代数分解**与**谱变换**两条可行路径。**Dynamic gradient rank**：**Adapprox**（Zhao et al. (2024b)）是**显式**路线，用自适应选择机制直接调整二阶矩矩阵的存储秩；**HELENE**（Zhao et al. (2024a)）是**隐式**路线，不动秩参数，而靠逐层自适应裁剪与退火来优先保留重要梯度信息；区别在于动态定秩究竟是显式的架构选择，还是梯度正则化带来的涌现性质。**Gradient reconstruction**：**TeZO**（Sun et al. (2025b)）与前两类不同，在零阶框架内做结构化估计，用 canonical polyadic decomposition（CPD）捕捉低秩的时间与跨模型结构来重建梯度，并按层特性动态调整估计器的秩——它压缩的不是"算好的梯度"。

**Open problem**：原文未单列 gap；剩余的问题是截断 SVD、小波变换与 CPD 各自引入的近似误差，如何在同一收敛分析中统一界定。

#### 3.7.5 无状态优化（Stateless Optimization）

**要解决的问题**：摆脱对历史数据累积的依赖，让更新方向直接来自**瞬时计算**与**参数固有属性**，靠即时反馈把显存与延迟压到极低——即总述中"把状态数从 $2N$ 降到零"的那条路线。**Real-time gradient statistics**：**SWAN**（Ma et al. (2025)）用瞬时梯度统计做无状态更新，针对的是自适应优化器中 EMA 的额外开销与偏差，并把核心预处理、效率调整与变体整合起来以灵活权衡。**Parameter characteristic-driven updates**：**AlphaGrad**（Sane (2025)）用单个 steepness 参数做省显存的缩放；**SGD-SaI**（Xu et al. (2024)）用梯度的信噪比调整初始学习率；**BFE**（Cao (2022)）用前向 loss 信息调节学习率，**AdaBFE**（Cao (2022)）把它扩展到高维空间中的逐参数自适应。**Real-time curvature estimation**：**Sophia**（Liu et al. (2024a)）用轻量的对角 Hessian 估计作预条件器，靠实时曲率估计消除传统二阶优化器对大规模存储的依赖，且不增加额外计算负担。

**Open problem**：原文未单列 gap；共同缺口在于无状态意味着放弃动量的方差缩减与历史平滑，如何在完全不存状态的前提下恢复 Adam 级的稳定性，仍无统一答案。

#### 3.7.6 大模型显存高效（Memory-Efficient for Large Models）

**要解决的问题**：全参数训练的资源需求已不可承受，本节从**梯度计算与应用协议**入手绕开它。**ZO fine-tuning**：**KerZOO**（Mi et al. (2025)）是基于核函数的零阶框架，用核平滑缓解低阶偏差以提升收敛速度，从而改善大语言模型零阶微调的效率。**Stateless fine-tuning**：**LOMO**（Lv et al. (2024)）对每个参数算出梯度后立刻更新该参数、随后清空梯度，内存中至多保留一个参数的梯度，把反向传播与参数更新融合，实现无状态微调。**Selective parameter fine-tuning**：**Sparse-MeZO**（Liu et al. (2024b)）通过逐层幅度阈值动态选出稀疏参数子集；**LeZO**（Wang et al. (2024b)）把 SPSA 与逐层稀疏性结合，只选择性地更新部分层，从而在不增加额外显存的前提下完成高效微调。

**Open problem**：原文未单列 gap。共同代价很明显：零阶估计的方差与核平滑的偏差、选择性更新带来的收敛速率损失、无状态更新对超参敏感度的放大，都还缺少统一的理论保证——这也与全节始终没有"进展—缺口"式收束的写法一致。

---

### Sec 3.8 Tailored Optimization Approaches（定制化优化方法）：本节的问题意识与分类

3.7 节讲的是"压低优化器内存开销"这类通用手段，3.8 换了个角度：**当通用优化器在特定场景下失效时怎么办**。论文按"更新规则从哪来"分成两类。**Auto-Designed Optimizers（自动设计优化器）**不依赖人的启发式，而是**自动发现并形式化更新规则**，手段包括**结构化程序空间中的程序搜索**、**神经控制器建模物理输运**、**符号推导**，并借助**演化策略**逃离局部最优、用**元学习**获得随任务动态适应的规则。**Robust Optimization（鲁棒优化）**不改规则来源，而是给训练加装抗打击机制，分两个方向：**gradient resilience（梯度韧性）**保证稳定性，**systemic adaptability（系统性适应）**处理联邦学习的分布漂移与大规模可行性所需的结构感知优化。

**论文承认的 gap：** 本节只按"规则发现 vs. 工程韧性"排列方法，未给出统一评价协议，也未正面回答"自动发现的规则是否真的优于人类启发式"；搜索空间本身仍由人预先设计，这正是"超越人类直觉"这一说法在论文框架内的张力。

#### 3.8.1 Auto-Designed Optimizers（自动设计优化器）

**要解决的问题。** 手工优化器（SGD、Adam 等）受限于研究者的直觉与试错成本，更新规则被固定在少数模板里。自动设计优化器把"设计优化器"本身变成**搜索问题**：通过算法化探索自动发现并形式化更新规则，以找到人类直觉想不到、却在适应性、泛化性与收敛性上更好的机制。论文把这归为汇集**符号推导、演化搜索、元学习、程序化合成**四种生成范式。

**程序化搜索优化器（Programmatic search optimizers）**是本节骨干，由三篇工作层层递进。第一步，**Li and Malik (2017)** 用**程序化策略表示**取代手工设计的优化规则并配以自动化搜索机制；论文评价它"具有重要的启发价值"——第一次把更新规则从数学公式变成可搜索的代码。第二步，**Chen et al. (2023)** 把**算法发现重新表述为一种程序搜索**，搜索空间是**无限的稀疏空间（infinite sparse spaces）**："稀疏"指这片庞大程序空间里真正能有效下降的更新规则极其稀少，"无限"指程序语法可组合出无穷多候选；为在此高效探索，它用**演化技术**引导搜索而非穷举。搜索的直接产物就是 **Lion**——使用**符号动量更新（sign momentum updates）**的优化器。整条链的逻辑是：搜索只拿到一个极简元语言（对梯度/动量做逐元素一元变换，再与动量加权组合），演化搜索负责选出用哪种变换，最终选中的答案是 **sign 函数**，即只用动量的符号、丢弃幅值。收益是双重的：只需维护一个动量变量，不再像 Adam 那样同时维护一阶矩与二阶矩，内存大幅下降；符号化还使核心步长与梯度幅值解耦（4.3.1 会重新用这点解释鲁棒性）。代价同样明确：**梯度幅值中的信息被彻底丢弃**，优化器失去逐维自适应缩放能力，峰值精度因此不占优（表 4 中 Lion 最佳 Top-1 为 76.334，AdamW 为 75.196，Muon 高达 78.252）；更根本的是论文全节**未讨论这次搜索本身消耗多少算力**——程序搜索需要成千上万次短程训练，**搜索成本**是该范式最现实的固定局限。第三步，**Bello et al. (2017)** 用 **RL 训练的控制器**发现更新规则，找到 **PowerSign** 与 **AddSign**；论文总结：这些方法"共享规则的**程序化表示**，但**搜索范式不同**"，统一抽象是"用程序/策略表示更新规则"，分歧点在用什么搜索器去找。

其余四类如下。**神经控制器**：**KO**（Feng et al. 2025）把参数更新建模为**随机碰撞（stochastic collisions）**，用 **Boltzmann 输运方程**描述，靠**硬碰撞与软碰撞**机制调控参数离散程度以提升多样性与泛化；论文称之为"物理启发、理论可证、工程高效"的新范式，代价是碰撞机制的超参数与额外计算。**符号推导**：**AGS-GD**（Starnes et al. 2024）用**高斯平滑的符号数学推导**重建更新规则，把**局部梯度转化为非局部梯度**，从理论根源上解决传统优化器**陷入次优局部极小值**的痛点；局限是非局部梯度需额外采样/平滑开销，论文未给出其在标准基准上的对照数据。**演化算法**：**GADAM**（Zhang and Gouza 2018）把 Adam 与**遗传算法**结合，用**交叉（crossover）**与**变异（mutation）**跳出局部最优，**BGADAM**（Bai et al. 2021）再加 **boosting 策略**生成多样化训练集；代价是**每步维护多个候选种群并做适应度评估**，开销按种群规模成倍放大，这是它难以用于大规模训练的根本原因。**元学习**：**WarpAdam**（Pan et al. 2024）在 Adam 流程中**插入可学习的扭曲矩阵**；**HyperAdam**（Wang et al. 2019）用 **RNN（Elman 1990）框架**自适应调整衰减率与权重并组合不同 Adam 配置的**集成更新**；**MADA**（Ozkara et al. 2024）用**超梯度下降（hyper-gradient descent）**统一多种优化器。论文评价它们"优于固定启发式"，局限有二：元训练与元测试任务分布一旦**分布偏移**，学到的规则未必可靠；元学习的**二阶/超梯度计算代价**远高于普通一阶训练。

**论文承认的 gap：** 3.8.1 末尾未集中总结各范式的 open problem，只以"共享程序化表示、分歧在搜索范式"收束。可确认的空白是：未比较各范式的**搜索成本**；未给出这些自动发现优化器在**语言建模**上的对照证据（4.3.1 的敏感性分析全部在 ViT-S 上完成）；也未讨论自动发现的规则是否具有**跨架构可迁移性**——而这恰是第 4 节要回答的核心问题。

#### 3.8.2 Robust Optimization（鲁棒优化）

**要解决的问题。** 训练过程本身脆弱：**随机梯度噪声**、**硬件量化误差**、**数据分布漂移**这三类系统性不规则因素都会让优化失稳。本节方法通过**自适应裁剪（adaptive clipping）**、**漂移校正（drift correction）**、**结构分解（structural decomposition）**等韧性机制对抗扰动，目标是在波动或异质条件下仍保证可靠收敛与一致的模型性能。

**噪声鲁棒梯度。** **AdaGC**（Wang et al. 2025b）用**指数移动平均为每个参数维护局部裁剪阈值**，按参数自身尺度自适应裁剪，避免固定阈值的粗暴截断；**AdaNorm**（Dubey et al. 2023）用**历史梯度的 EMA 校正梯度范数**，专治"梯度过小"与"梯度值非典型"两类问题。代价是额外维护一份阈值/范数状态，部分抵消了 3.7 省内存手段的收益。

**分布漂移鲁棒性。** 针对**联邦学习**在异质（non-IID）数据下各客户端局部最优彼此偏离（**client drift**）、局部与全局目标不再对齐的问题：**FedCET**（Liu and Wang 2025）用**学习率对客户端信息加权**；**FAdamGC**（Chen et al. 2025a）把**梯度校正**整合进自适应联邦优化；论文称它们增强了对分布漂移的鲁棒性并**降低通信开销**。

**硬件噪声鲁棒性。** **QuZO**（Zhou et al. 2025）通过**随机量化扰动**减小梯度偏差，增强对硬件噪声的鲁棒性，使优化在受限设置下仍稳定。注意 QuZO 在 3.7.2 节同时被列为"低秩 + 量化"的混合压缩方法——量化既是省内存的手段，也是噪声的来源。

**结构感知优化。** **BC-ADMM**（Pan and Wu 2025）把大规模问题**分解为可并行求解的子问题**，从而允许**更大的步长与更快的收敛**，并在全过程中**保持解的可行性（feasibility）**。其适用前提很明确：问题必须具有可利用的分解结构，这对端到端非凸深度网络并不普遍成立。

**论文承认的 gap：** 本节结尾同样只是方法罗列，未给出鲁棒性与收敛速度、峰值精度之间的**权衡分析**，也没有统一的鲁棒性评测指标；两类鲁棒性（面向联邦学习的分布漂移、面向量化训练的硬件噪声）在论文图景中彼此独立，能否统一到同一理论框架，论文没有回答。

### Sec 4.1 Datasets（数据集）

**要解决的问题。** 第 3 节按"受限环境"（分布式、差分隐私训练等）梳理了优化器演进，但论文指出：**公平的经验比较常被硬件与工程实现差异所妨碍**。因此本节提出一个**受控评测协议（controlled evaluation protocol）**，把**算法本身的数学行为**从实现细节中隔离出来。论文强调它不是在评测**峰值系统吞吐量**，而是聚焦**优化器背后的数学机制**——通过超参数敏感性、收敛动力学与跨架构泛化性，判断在视觉任务上表现良好的方法**能否可靠迁移到语言建模**。由此给出核心问题（原文以问句给出）：那些为 CNN 或 ViT 大量精调过的优化器，在面对**因果注意力机制特有的高度各向异性（highly anisotropic）损失地形**时，**是否会灾难性地变差**？

**视觉任务的数据与架构。** 使用标准 **ImageNet-1K**（Deng et al. 2009）：**约 1.28M（128 万）训练图像、50,000 张验证图像、1,000 个类别**；架构选取 **ResNet-50** 与 **ViT-Small（ViT-S）**，分别代表 **CNN** 与**视觉 Transformer**。

**语言任务的数据与架构。** 在 **WikiText-103**（Merity et al. 2016）上**从零训练**一个 **60M 参数的 Llama 模型**。关于这个 60M 配置，论文的自我限定必须完整记录：它**并非被设计用来复现完整规模 LLM 层面的涌现行为（emergent behaviors）**；相反，它充当一个**高度具代表性的现代自回归 Transformer 的架构代理（architectural proxy）**——在**保留 Llama 核心架构特征**的同时**保持计算上可行**，从而为评估优化器的**跨架构可迁移性**建立严格的测试床。

### Sec 4.2 Metrics and Settings（指标与设置）

**超参数设置。** 为保证公平，论文**只优化一个共同的超参数：学习率**。做法是：以各优化器**原始文献建议的默认值**为起点，用**网格搜索**把基准值乘以 **0.1、0.2、1.0、5.0、10.0** 探索其邻域；找到的最优学习率随后被**迁移到 ResNet 上**使用。

**视觉任务：指标与三套训练设置。** 图像分类以 **Top-1 准确率**为主指标。**ViT-S 使用 DeiT 训练设置**；**ResNet-50 分别训练 100 与 300 个 epoch，对应 A3 与 A2 设置**。论文考虑三套常规设置：

1) **DeiT setting**：为 **Transformer 及现代 CNN** 架构（如 ViT-S）设计，使用多种高级增强，包括 **RandAugment**、**Mixup**、**CutMix** 与 **Random Erasing**。

2) **A2/A3 settings**：为 **CNN** 设计，目的是在性能与收敛速度上**对齐 ViT**；相比 DeiT 设置，它们**减弱增强强度**，并把**交叉熵（CE）损失替换为二元交叉熵（BCE）损失**。

**一个重要的诚实性细节。** 论文明确记录：**某些优化器在 ResNet 架构上、用 BCE 损失训练时会出现不稳定或无法收敛（unstable or failed convergence）**，**对这些情况改用了 CE 损失**。此处给出的引用列表包含 Robbins and Monro (1951)、Wang et al. (2025c)、Zhuang et al. (2020)、Shazeer and Stern (2018)、Taniguchi et al. (2024)、Li (2017)、You et al. (2017)、Zhang et al. (2019)、Defazio and Jelassi (2022)，以及另行点名的 **SGDP**（Heo et al. 2021）。关键点在于：这是把"某些优化器无法在 BCE 下稳定训练"当作公开的协议偏离记录在案，而非掩盖；把 CE 用回这些优化器会**轻微削弱 A2/A3 设置内部的可比性**，读表 4 时必须记住。

**视觉训练的超参数（表 3）。** 三套设置的配方如下（"×"表示该成分不使用；原文注明灰色区域表示应按各优化器分别修改）：

| Procedure | DeiT | A2 | A3 |
| --- | --- | --- | --- |
| Dataset | IN-1K | IN-1K | IN-1K |
| Epochs | 300 | 300 | 100 |
| Batch size | 1024 | 1024 | 1024 |
| Optimizer | AdamW | AdamW | AdamW |
| Learning rate | $1 \times 10^{-3}$ | $1 \times 10^{-3}$ | $1 \times 10^{-3}$ |
| Optimizer Momentum | 0.9, 0.999 | 0.9, 0.999 | 0.9, 0.999 |
| Weight decay | 0.05 | 0.05 | 0.05 |
| LR decay | Cosine | Cosine | Cosine |
| Warmup epochs | 5 | 5 | 5 |
| Label smoothing $\epsilon$ | 0.1 | × | × |
| Dropout | × | × | × |
| Stochastic Depth | 0.1 | 0.05 | × |
| Repeated Augmentation | ✓ | ✓ | × |
| Gradient Clip. | × | × | × |
| Horizontal flip | ✓ | ✓ | ✓ |
| RandomResizedCrop | ✓ | ✓ | ✓ |
| Rand Augment | 9/0.5 | 7/0.5 | 6/0.5 |
| Auto Augment | × | × | × |
| Mixup $\alpha$ | 0.8 | 0.1 | 0.1 |
| Cutmix $\alpha$ | 1.0 | 1.0 | 1.0 |
| Erasing probability | 0.25 | × | × |
| ColorJitter | × | × | × |
| EMA | × | × | × |
| CE loss | ✓ | × | × |
| BCE loss | × | ✓ | ✓ |

读法：三套设置共用**相同数据集、batch size 1024、AdamW 基线优化器、学习率 $1 \times 10^{-3}$、weight decay 0.05、余弦衰减与 5 个 warmup epoch**；区分它们的是**训练时长（300/300/100 epoch）**与**增强/正则强度**。自上而下是单调递减的强度梯度：RandAugment 由 9/0.5 降到 7/0.5 再降到 6/0.5，Mixup 的 $\alpha$ 由 0.8 降到 0.1，随机擦除概率 0.25 直接取消，Stochastic Depth 由 0.1 降到 0.05 再取消，Repeated Augmentation 在 A3 中被关掉，标签平滑只在 DeiT 中保留；而 **CE 损失只在 DeiT 中启用，A2/A3 全部改用 BCE**。

**语言任务：指标与训练设置。** 语言建模用**困惑度（PPL）**评价。训练配置**对齐 Semenov et al. (2025)**：**batch size 256、序列长度 512**；但由于计算资源限制，论文使用 **60M 参数的类 Llama（Llama-like）架构**，与参考文献所用模型规模不一致。此外，**训练 token 总数依照 Chinchilla 缩放定律（Hoffmann et al. 2022）确定**。论文随后给出该 60M Llama 架构在 WikiText-103 上的标准训练配置样例（含数据配置、优化设置与正则化技巧），并**以 AdamW 为例**：**峰值学习率 $1 \times 10^{-3}$、weight decay 0.5、优化器 $\epsilon$ 为 $10^{-8}$**；使用**余弦学习率调度器，warmup 阶段 2000 步**。为稳定训练与泛化，**梯度裁剪阈值设为 0.5**。训练在 **8 块 GPU** 上进行，**序列长度 512、每设备 batch size 32**（故**全局 batch size 为 256**）。论文强调，这套语言建模设置的材料与超参数被严格控制，以便对优化性能做基准测试。

**论文承认的 gap：** 4.2 只优化学习率这一个超参数，其余沿用各优化器原始文献的默认值——这使"跨优化器比较"的公平性建立在"各自默认值已经合理"这一较强假设上；60M 规模代理、以及部分优化器被迫从 BCE 退回 CE 这两点，也被论文显式承认为对可比性的让步。

### Sec 4.3 Analysis 与 4.3.1 Learning Rate Sensitivity on ViT-S（ViT-S 上的学习率敏感性）

**4.3 的定位。** 论文在此系统分析各优化器在不同任务上的实际表现并探究其背后机制；这些动力学由 **ViT-S、ResNet-50 与 Llama-60m** 三个模型上的**训练曲线**直观佐证（对应论文图 8、图 9 与图 11）。

**4.3.1 要解决的问题。** 为系统评估学习率敏感性，论文在**一个很宽的学习率谱**上报告 Top-1 准确率，并明确标出各自达到峰值性能的**最优学习率取值**（见**表 4 与图 8**）。图 8 的说明也点明结论：**收敛稳定性与最终 Top-1 准确率高度依赖精确的超参数调优**。

**表 4：各优化器的超参数鲁棒性与学习率设置。** 表头给出 0.1×LR、0.2×LR、1.0×LR、5.0×LR、10.0×LR 五档缩放及 Best LR（最优基准学习率）。下为完整转录，连字符表示该档位未报告结果：

| Optimizer | 0.1×LR | 0.2×LR | 1.0×LR | 5.0×LR | 10.0×LR | Best LR |
| --- | --- | --- | --- | --- | --- | --- |
| SGD | 48.056 | 55.918 | 64.360 | 60.590 | 59.262 | 0.1 |
| RMSprop | 54.254 | 43.416 | 20.512 | 6.1140 | - | 1e-4 |
| Adam | 66.234 | 70.772 | 72.240 | - | - | 1e-3 |
| AdaBelief | 29.248 | 40.210 | 58.562 | 66.528 | 68.642 | 1e-2 |
| Adafactor | 66.294 | 70.834 | 74.630 | 73.344 | - | 1e-3 |
| RAdam | 66.220 | 70.826 | 71.190 | - | - | 1e-3 |
| AdamP | 67.336 | 71.856 | 75.444 | 72.606 | - | 1e-3 |
| SGDP | 49.800 | 57.662 | 66.536 | 61.182 | 60.416 | 0.1 |
| AdamW | 67.082 | 72.076 | 75.196 | 70.136 | 0.2640 | 1e-3 |
| Adan | 74.562 | 74.956 | 75.190 | - | - | 1.25e-2 |
| ADOPT | 62.408 | 66.552 | 64.684 | 38.170 | 29.670 | 2e-4 |
| Kron | 75.878 | 76.432 | 77.304 | - | - | 1e-3 |
| LAMB | 24.346 | 37.750 | 69.100 | 75.858 | 69.956 | 5e-3 |
| LAPROP | 66.834 | 71.834 | 75.050 | 6.3640 | - | 1e-3 |
| LARS | 8.3020 | 12.426 | 32.892 | 53.660 | 58.276 | 1 |
| Lion | 63.192 | 71.092 | 76.334 | 72.254 | 65.988 | 1e-4 |
| Lookahead | 38.492 | 48.896 | 62.898 | 64.320 | 61.962 | 0.5 |
| MADGRAD | 72.218 | 74.150 | 74.634 | 68.946 | 61.526 | 1e-3 |
| MARS | 62.414 | 70.300 | 74.474 | 74.608 | 72.542 | 5e-3 |
| Muon | 73.844 | 75.944 | 78.252 | 75.206 | - | 2e-3 |
| Nadam | 66.594 | 70.742 | 72.180 | - | - | 1e-3 |
| NadamW | 67.332 | 71.872 | 75.278 | 74.982 | - | 1e-3 |
| NovoGrad | 10.290 | 16.686 | 48.892 | 66.514 | 64.258 | 5e-3 |

**高鲁棒性表现。** **Kron**（Li 2017）与 **Adan**（Xie et al. 2024b）在**低档位（0.1× 到 1.0×）表现出极小方差**，但**高学习率下容易发散**。论文解释 Adan 的原因：它**重新表述了 Nesterov 动量**并整合自适应梯度机制，用**有目标的前瞻动量策略**加速收敛；但**动量项与自适应梯度的耦合产生很高的更新惯性**——学习率被放大时，**大梯度幅值叠加强动量惯性**造成**参数更新的剧烈振荡**，从而**抵消自适应分母的稳定效果**。这说明"自适应分母"并不自动保证高学习率下的稳定性。

**Muon**（Jordan et al. 2024）**对超参数最不敏感**：在 **0.2× 到 5.0×** 范围内**保持 > 75% 准确率**，极为好调；原因是它通过**对累积动量矩阵做迭代正交化（iterative orthogonalization）**，把参数更新**约束在一个受限流形（limited manifold）上**，**无论外部学习率如何缩放**都如此，这种**内在正则化（intrinsic regularization）**使它**对超参数变化高度不敏感**。

**高下界稳定性。** 尽管在最优设置之外有明显退化，**Lion**（Chen et al. 2023）、**MADGRAD**（Defazio and Jelassi 2022）与 **MARS**（Yuan et al. 2025）在**整个 0.1× 到 10× 范围内都维持了一个"安全"基线（> 60% 准确率）**。**Lion 的机理**：其更新**只使用动量加权梯度的符号（$\mathrm{sign}(c_t)$），丢弃精确幅值**，因此**无论梯度因学习率缩放而爆炸还是消失，符号更新项始终被限制在 $\{-1, 0, +1\}$ 之内**，使**参数更新的核心步长完全由学习率决定，并且在所有维度上对符号分量是相同的**（原文抽取受损：页 51 末与页 52 首之间该句在"the sign com-"与"ponent."处被切断，重建后语义即如上）——符号更新把"幅值信息"这条不稳定通道整体切断，这既是它在极端学习率下仍有训练下界的原因，也是它峰值精度不高的原因。**MADGRAD 的机理**：它把**基于平均的更新机制与自适应缩放性质结合**，核心是**双重平均（dual averaging）框架**——**不对梯度逐步施加自适应缩放**，而是**用权重平滑累积历史梯度**，再**通过一个取立方根的（cube-rooted）自适应分母归一化**，因此**天然缓冲了极端的梯度冲击**；"取立方根"是关键细节：相比 Adam 的平方根，它对分母极端值的压缩更温和，故对梯度尖峰更钝感。

**大幅学习率敏感性（Adam 家族）。** 这些方法**对激进学习率高度脆弱**：**放大 5× 或 10× 通常导致不收敛或性能急剧下降**。表 4 可直接印证：Adam 在 5× 与 10× 下未报告结果（即无法收敛），Nadam、RAdam、Adafactor、AdamP、LAPROP、Adan、Kron 也都在高档位缺失，而 AdamW 在 10× 下**崩到 0.2640**。这是极有说服力的对照——**同一"家族"在低档位（0.1×–1.0×）几乎都集中在 66–75 区间，高档位却集体失效**。

**各优化器的偏好差异。** 论文把优化器按敏感性分成三类。**只偏好保守学习率的**：**RMSprop**（Tieleman and Hinton 2012）**鲁棒性有限，偏好保守学习率**，表 4 中呈单调崩塌：54.254 → 43.416 → 20.512 → 6.1140。**在小学习率下收敛困难的**：**AdaBelief**（Zhuang et al. 2020）、**LAMB**（You et al. 2020）、**LARS**（You et al. 2017）与 **NovoGrad**（Ginsburg et al. 2019）**在小学习率下遇到收敛困难，因此需要更大的学习率**；这一类曲线反向爬升——AdaBelief 由 29.248 → 40.210 → 58.562 → 66.528 → 68.642（**最优学习率 1e-2，即基准的 10 倍**），LARS 由 8.3020 → 12.426 → 32.892 → 53.660 → 58.276（**最优学习率为 1，比基准大三个数量级**），LAMB 在 0.1× 时仅 24.346、到 5× 才达峰值 75.858。机制是：**LARS 与 LAMB 的核心为 trust ratio（信任比）**——**通过计算参数范数与更新向量范数之比来归一化每一层的更新步长**（即 $\lVert \theta \rVert / \lVert u \rVert$ 形式的逐层比值），以保证**跨层更新幅值稳定**；其**关键弱点**是**若全局基准学习率设得过小，再乘以 trust ratio 就会把实际参数更新步长压缩过甚**，使模型**在训练早期无法逃离初始平坦区域（initial flat regions）**，故这些优化器**需要更大的学习率**。逻辑闭环很清楚：trust ratio 解决了"层间尺度不一致"，却把"全局尺度"的自由度进一步放大。**低倍鲁棒但高倍易崩的**即上一段的 Adam 家族。

**SGD 的韧性。** SGD（Robbins and Monro 1951）**峰值精度落后（64.36%）**，但**对大学习率表现出非凡鲁棒性**，**即使在 10× 缩放下仍然可用，而此时其他优化器已经失效**（表 4 中 SGD 10× 为 59.262，AdamW 仅 0.2640）。论文的完整机理对照是本节最核心的论述：**对 Adam 这类自适应优化器，更新公式是把梯度的一阶矩乘以"梯度二阶矩的平方根加上一个小 $\epsilon$ 值"的倒数，再整体乘以负的学习率**，即形如 $\theta_t = \theta_{t-1} - \eta \cdot \hat{m}_t / (\sqrt{\hat{v}_t} + \epsilon)$ 的更新式；**对于梯度极小的参数，这个平方根也极其微小，于是该除法会产生一个巨大的乘数**，**当基准学习率再被放大 10 倍时，这些参数的实际步长会变得天文数字般地大，瞬间破坏模型权重**。与之相对，**SGD 的更新规则非常直接：只是把负的学习率直接乘以梯度**（即 $\theta_t = \theta_{t-1} - \eta \cdot g_t$），**即使学习率被放大 10 倍，更新步长仍与梯度成正比（线性比例）**；**大学习率确实会让 SGD 在最优点附近剧烈振荡，但它永远不会因"除以一个极小的值"而触发数值爆炸**。换言之，Adam 的失败模式是**结构性**的（分母趋零导致步长爆炸），SGD 的失败模式只是**振荡性**的（步长线性放大，但方向仍在梯度方向上）。

**关于符号更新（sign updates）的论述。** 论文把 **Lion 与 MADGRAD** 归为同一类保护机制：两者都通过把**更新幅值限制在 $\{-1, 0, +1\}$（在梯度归一化后）**的方式，使**核心步长不受梯度幅值爆炸或消失的影响**，"以驱动归一化的更新并产生足够的参数位移"。这正是它们能维持高于 60% 训练下界的根本原因——**步长上限由学习率而非梯度决定**。

**解耦权重衰减修饰项的有效性。** 论文观察到**引入解耦权重衰减（decoupled weight decay）能持续带来可测量的性能提升**，优于其所基于的基础算法：**在 1.0× 学习率下 AdamW（Loshchilov and Hutter 2019）达到 75.196，明显优于标准 Adam**（Adam 在 1.0× 下为 72.240）；**类似提升在 NadamW（Wightman 2019，达到 75.278）与其未加修饰的对应版本 Nadam（72.180）之间同样存在**。这说明"解耦权重衰减"是**正交于优化器主干设计**的改进，可在几乎不增加成本的前提下稳定生效。

**本节论文承认的 gap（及其限制）。** 第一，整个 4.3.1 的敏感性分析**只在 ViT-S 上完成**，ResNet-50 与 Llama-60m 的曲线虽在 4.3 中被引用（图 9、图 11），本节所有结论都局限于视觉 Transformer 这一类架构。第二，**部分优化器在高端档位根本没有报告结果**（表 4 的连字符），论文未区分"未测试"与"测试后不收敛"，读者只能由"5×/10× 通常导致不收敛"推断。第三，本节**未把超参数鲁棒性折算进最终选型建议**——Muon 虽最不敏感，论文并未据此推荐它，也没讨论"鲁棒但更费显存"与"精度最高但极难调参"之间的取舍。第四，原文此处**抽取受损**：页 52 正文在 NadamW 与 Nadam 的对照句之后于 "to its unmodified counterpart" 处中断，后续内容不在本次抽取的页面范围内。

---

> 阅读说明：原文为双栏排版，pdftotext 抽取时左右两栏被逐行交错拼接。下文已按语义重建阅读顺序（先读完左栏再读右栏），并把被割裂的句子接回。图表曲线的像素级数值无法从抽取文本恢复，遇此情况均已注明。

#### 4.3.2 跨架构泛化（Cross-Architecture Generalization）

**实验设计：参数迁移测试。** 论文用"参数迁移测试"（parameter transfer test）评估跨架构泛化，并在 Table 5 中同时报告 Top-1 准确率（ViT-S、ResNet-50）与困惑度 PPL（Llama-60M）。做法是：**把在 ViT-S 上找到的最优学习率直接迁移到 ResNet-50 与 Llama-60M**；在此设定下**核心学习率严格固定**，只允许辅助训练策略与权重衰减按各架构的最佳实践适配。此外论文还在 Llama-60M 上做学习率缩放实验，评估优化器在语言模型架构上的超参鲁棒性。

（读表提示：Table 5 中 Llama-60M 三列列名为 Base LR、$5.0\times$LR、$0.2\times$LR，填入的数值是 PPL 而非准确率；`-` 表示该项缺失。）

**Superior robustness（卓越鲁棒性）。** 论文点名 **Muon** 与 **MARS** 在 ResNet 与 Llama 上都取得极好结果，尤其擅长在 Llama 上**维持低而稳定的 PPL：PPL $\approx$ 12–14，且该区间在极端学习率缩放下依然保持**。可逐项核对：Muon 在 base LR / $5.0\times$LR / $0.2\times$LR 下为 **12.40 / 12.21 / 14.01**，MARS 为 **12.12 / 14.46 / 12.75**，放大与缩小两端都没崩。

**Muon 为何能跨架构？** 原因是其算法设计中的**矩阵级正交化**（matrix-level orthogonalization）。与依赖**标量方差历史**的方法不同，Muon 把更新矩阵**投影到受限流形**（restricted manifolds）上；这种内在的**几何正则化**能**压平被夸大的步长**，而大语言模型的损失景观恰恰**高度各向异性**，压平后极端方向不再爆炸。

**MARS 为何能跨架构？** MARS 引入一个**缩放参数控制方差缩减的强度**，并从当前步梯度中**减去上一组参数处的梯度在当前 batch 上的值**。论文指出，这种**梯度校正在数学上抵消了**由多样网络架构与复杂数据分布引入的**固有随机噪声**。

**High transferability with constraints（带约束的高可迁移性）。** 论文列出整体表现强、迁移性好的一批方法：**Adafactor、AdamP、AdamW、Adan、LAMB、LAPROP、Lion、NadamW、Kron**，它们略微落后于 Muon、MARS。论文用 **Kron** 说明"迁移性好"不等于"安全边际宽"：它在 base LR 下达到**顶级 PPL 12.57**（上限很高），但学习率放大到 $5\times$ 时**塌陷到 PPL 336.2**，其超参**安全边际明显更窄**。

**解耦权重衰减为何有助于跨架构？** 论文认为 AdamW、NadamW 保持竞争力的核心机制是**把自适应梯度更新与权重衰减解耦**：从浅层视觉模型迁移到参数量级完全不同的语言模型时，**传统正则化容易被历史二阶矩的不均匀累积扭曲**；把**惩罚项放到缩放分母之外**，就能保证无论各层参数绝对量级如何，正则化都**一致且忠实**。

**对 LAMB、Lion、Adan、AdamP 的机理解释。**

- **LAMB**：采用**逐层自适应**（layer-wise adaptation）严格约束步长；这在**数学上把随机梯度量级与应用到各层上的更新量解耦**，因此对深度各异的网络都有效。
- **Lion**：使用**基于符号的更新**让**所有参数更新量级统一**，从而使优化器对**复杂注意力机制中常见的梯度尖峰不敏感**。
- **Adan 与 AdamP**：通过**更精细的动量估计与轨迹校正**，**显著改善跨架构泛化**。

论文同时给出解耦权重衰减的具体收益作为对照：$1.0\times$ 学习率下 **AdamW 达到 75.196**，明显优于标准 **Adam**；**NadamW 达到 75.278**，优于未修改的 **Nadam**（72.18）；**AdamP 达到 75.444**，也优于标准 Adam——论文认为这凸显了"精炼参数更新规则"的普遍有效性。

**Architecture-specific instability（架构特有的不稳定性）——完整点名列表。** 论文原文写道："一大群优化器，包括 RMSprop、Adam、MADGRAD 和 Adabelief"，在视觉骨干网上表现尚可，**但在 Llama 上难以收敛**。即完整列表为：**RMSprop、Adam、MADGRAD、AdaBelief**。

**机制解释：梯度离群值。** 论文把根因归结为**大语言模型训练中普遍存在的梯度离群值**。像 Adam 这类自适应方法**依赖高度有偏的标量方差历史**，会遭受**二阶矩污染**：极端梯度把二阶矩估计带偏，使优化器在更新时**错误地缩放逐维步长**，从而在高维非凸空间中**偏离有效优化轨迹**。

**Training collapse（训练崩溃）。** 论文另外点名 **SGD 系列优化器**：它们在视觉架构上可用，迁移到 Llama 时却遭遇**灾难性训练崩溃**（Fig. 9）。原因是大语言模型的**极端深度与复杂注意力机制**造成层间**梯度量级与参数尺度的巨大差异**，而纯一阶动量方法**缺少自适应的逐参数／逐层缩放机制**，无法同时满足所有层的收敛要求；结果是**同一个全局学习率在某些层触发梯度爆炸、在另一些层造成梯度消失**，最终导致**模型表征能力彻底丧失**。Figure 9 图注补充：缺失数据点表示**初始步即梯度爆炸（loss 变 NaN）或训练中断**；图注还点名 **SGD 与 SGDP 迅速发散**，而 Muon 与 MARS 在**所有缩放尺度上保持稳定收敛**。

（相关的学习率敏感性结论：**Adam 家族**放大 $5\times$ 或 $10\times$ 通常**不收敛或性能急剧下降**；**RMSprop** 偏好保守学习率；**AdaBelief、LAMB、LARS、NovoGrad** 在小学习率下**收敛困难**。反之 **SGD** 虽峰值准确率落后（**64.36%**），$10\times$ 缩放仍可用：Adam 类更新要**除以"二阶矩的平方根加一个小 $\epsilon$"**，对梯度极小的参数该平方根也极小，除法**产生巨大乘数**，**瞬间破坏权重**；而 SGD **只是把负学习率直接乘以梯度**，步长与梯度**成线性比例**，只**剧烈震荡，不会因除以极小值而数值爆炸**。）

---

#### 4.3.3 长期训练可扩展性（Long-term Training Scalability）

**实验设计。** 论文把训练协议从 **100 epoch 延长到 300 epoch**，在 **ViT-S 与 ResNet-50** 上同时进行，并在 **Table 6** 给出两个里程碑的 Top-1 准确率及提升量（$\Delta$），用 **Fig. 11** 可视化收敛轨迹、**Fig. 10 雷达图**给出综合性能画像。

**Architecture-specific scaling（架构特有的可扩展性差异）。** **SGD 系列展现出很强的可扩展性**：SGD 在 **ViT-S 上提升 9.41%**（Table 6 精确值 **+9.414**，从 **64.360** 到 **73.774**）。机制上，这类优化器**只依赖一阶动量而不做激进的逐元素方差累积**，这种设计**阻碍了步长的快速衰减**——代价是**初始收敛较慢**，好处是**让模型得以广泛探索损失景观**。

**Consistent scalability（稳定的可扩展性）。** 训练延长到 300 epoch 时，**Muon、Lion、Kron、AdamW** 等**高级优化器**表现出**一致但温和**的增益，**范围为 +1.896% 到 +4.690%**（横跨不同架构）。论文举 **Muon** 为例：它在 100 epoch 时已在 **ViT-S 上取得 78.252 的高基线**，300 epoch 的增益**被封顶在 +2.622%**（对应 80.874）。原因是这些方法借助**复杂预条件、正交化或解耦权重衰减**能够**迅速穿越优化轨迹**，靠**高效利用局部曲率与结构先验**在前 100 epoch 内就**收敛到高质量最优点**，因此**后续迭代收益递减**——它们在早期**已提取了网络表征能力的绝大部分**。

**Performance regression（性能回退）。** 相反，**RMSprop 在 ResNet-50 上对延长训练表现出有害反应，在 100 到 300 epoch 之间出现性能回退**（Table 6：**71.536 → 71.152，即 $-0.384$**）。论文给出的原因是：这**凸显了"依赖未校正的平方梯度指数滑动平均（uncorrected EMA of squared gradients）"在长训练周期下的结构性脆弱**——由于**缺少一阶动量的稳定作用或解耦正则化机制**，**后期梯度噪声的持续累积会不成比例地扭曲逐参数步长**，300 epoch 中这种**失衡缩放打破了局部极小点内部的脆弱平衡**，**迫使优化器逃离最优盆地**，导致**后期表征能力下降**。

（交叉核对细节：Table 6 中 MARS 在 ResNet-50 上 100 epoch 为 77.294、提升记为 **+1.184**；Muon 为 77.996、提升记为 **+1.896**。Muon 的 300 epoch 单元格在抽取中与 MARS 的数值纠缠，**此处原文抽取受损**，故不给出其具体值。）

**"损失差分法"（differencing method）的用途。** 论文分析的是验证损失的**一阶差分而非原始损失值**。理由是：**原始损失轨迹由于网络收敛的总体趋势往往呈现高度共线性（high collinearity）**，各优化器曲线看上去都差不多；而**一阶差分把损失函数的"逐步加速与减速"单独隔离出来**，从而**有效凸显时间上的拐点（temporal inflection points）**——即**某优化器逃离次优区域或进入快速下降阶段的时刻**。用论文的话说：**通过剥离损失的累积尺度，差分方法揭示了底层更新机制的内在节律与步调（intrinsic rhythm and pacing）**。

---

#### 4.3.4 优化器相关性（Correlation of Optimizers）

论文对 ViT-S 与 ResNet-50 上的优化器表现做了相关性分析（Fig. 11），分析对象是**验证损失的一阶差分序列**，据此计算 **Pearson 相关系数矩阵**（Fig. 11 内嵌热图）。

**核心发现：架构的影响有时超过优化器本身。** 论文原句是：**"经验相关性矩阵揭示，模型架构对优化轨迹施加了深刻影响，有时甚至超越了优化器自身的算法设计（occasionally superseding the optimizer's algorithmic design）。"**

**ViT 与 ResNet 的差异及其原因。**

- **ViT：压倒性的同质性（overwhelming homogeneity）。** 相关性矩阵**在全部被评估的优化器之间都显示出压倒性的同质性**，即各优化器轨迹高度相似。论文的解释是：**ViT 高度受限的训练体制主导了损失景观的穿越过程**，该体制**重度依赖长时间的线性学习率 warmup 与严格的正则化**；结果是**调度策略实际上"覆盖"（override）了各优化器独有的步长自适应行为**，把大家抹平成同一种轨迹。
- **ResNet-50：显著的算法分叉（significant algorithmic bifurcation）。** 相反，ResNet-50 上出现明显分叉。原因是 **CNN 固有的归纳偏置（inductive bias）与更平滑的损失景观**赋予了优化器**几何上的自由度去探索彼此不同的下降路径**，因此**优化器之间的相关性差异很大**。

**算法分类与轨迹分叉。** 热图中优化器按 **Fig. 11 所列顺序**排列（横轴自左向右、纵轴自上而下）。在**约束较少的 ResNet-50 环境**中，热图揭示出**基于动态行为、彼此分明的优化器分类簇**：

- **Adaptive family（自适应家族）。** **Adam、RAdam、AdamP、AdamW、Nadam、NadamW** 构成**高度相关的紧密簇**：它们**共同依赖平方梯度的指数滑动平均**，因而**对景观陡峭度做出同步自适应**，使**快速下降阶段在时间上彼此对齐**。
- **Preconditioning and orthogonalization（预条件与正交化）。** **Kron 与 Muon 组内相关性异常高，同时与自适应标量家族明显分叉**；论文认为这**完美对应它们的理论基础**：通过**结构性梯度统计做预条件**或**矩阵正交化**，它们**以结构性矩阵更新而非简单对角缩放的方式导航参数空间**，因而产生**根本不同的损失穿越轨迹**。
- **Mechanistic outliers（机制上的离群者）。** **Lookahead、MADGRAD、SGDP** 与标准方法**持续呈现低相关性**；以 Lookahead 为例，其**双权重插值机制在一阶差分序列中引入时间滞后**，**使拐点相对于基于步长的方法发生位移**。

**Temporal saturation and cross-epoch homogenization（时间饱和与跨 epoch 同质化）。** 对比短期与长期训练可见**清晰的时间平滑效应**：**训练初期**优化器的**探索性差异达到最大**，凸显各自独特的数学特性；而**训练从 100 延长到 300 epoch 时，相关性矩阵整体同质性增强**。原因是**大多数优化器最终收敛进入同一个局部吸引盆地（a localized basin of attraction）**；在**利用（exploitation）阶段**，**学习率衰减到极小值**，**一阶差分序列被接近零的波动主导**，这种**渐近饱和在数学上迫使相关系数收敛**——说明**不同算法路径在足够长的时间尺度上最终导向相似的几何终点**。

---

#### 4.3.5 综合优化器评价（Comprehensive Optimizer Evaluation）

论文在 **Table 7** 中对**二十多个优化器**做了严格的实证比较，采用**多维度的星级评价范式（multifaceted star evaluation paradigm）**。星级越高代表算法效率越强，**星级本身表示在被评估对象中的相对性能百分位（relative performance percentiles）**。五项核心准则是：

1. **Convergence Speed（收敛速度）**
2. **Accuracy（准确率）**
3. **Memory Efficiency（显存/内存效率）**
4. **Generalization（泛化能力）**
5. **Hyperparameter Robustness（超参数鲁棒性）**

**每项评分的定量依据。** 论文明确交代了这些星级的经验基础：

- **Accuracy** 综合了**三种不同架构**上的整体表现（见 Table 5）；
- **Generalization** 由**跨架构泛化实验**体现（见 Sec. 4.3.2）；
- **Hyperparameter Robustness** 由**学习率缩放实验**评估（见 Table 4）；
- 另外，**Convergence Speed 与 Memory Efficiency 分别用"达到目标损失所需的迭代步数"和"峰值 GPU 显存占用"来量化**。

总体结论是：**Muon 取得最佳综合性能**（Table 7 中 Muon 五项分别为 $\star\star\star\star\star$ / $\star\star\star\star\star$ / $\star\star\star\star$ / $\star\star\star\star\star$ / $\star\star\star\star\star$）。作为对照：**SGD** 为 $\star$ / $\star\star$ / $\star\star\star\star\star$ / $\star\star$ / $\star$；**Adam** 为 $\star\star\star$ / $\star\star\star$ / $\star\star\star$ / $\star\star$ / $\star\star\star$；**AdamW** 为 $\star\star\star\star$ / $\star\star\star\star$ / $\star\star\star$ / $\star\star\star\star$ / $\star\star\star$。（Table 7 在抽取文本中星号与行名交错严重，上列星号数为按行对齐后的读取结果。）

---

#### 4.3.6 算力成本披露（Computational Cost Disclosure）

为保证研究结论的**内部有效性（internal validity）与可复现性（reproducibility）**，论文对全部 **23 个被评估优化器**强制实施了**严格的超参数网格搜索协议**。然而，这一严谨方法带来了**计算开销的指数级爆炸**。如 **Table 8** 所示，论文的完整实证框架——涵盖**多尺度学习率扫描**与**延长的 300 epoch 训练周期**——累计产生**超过 1073 个单卡 NVIDIA A100 GPU 小时**的估算成本。

**Table 8 完整转录：综合算力成本估算（GPU Hours）。** 表题说明：该表详列整个实证基准所需的**估算单卡 NVIDIA A100 GPU 小时**。工作量涵盖 **23 个优化器**，包括**跨 5 个学习率尺度**的严格网格搜索与**延长的 300 epoch 训练周期**。巨大的总算力足迹（$\sim$<!-- -->1073 GPU 小时）凸显了本评估的严谨性，也凸显了现代优化器基准测试所面临的**根本性算力壁垒**。

| Benchmark Task | Hardware Type | Hyperparameter Search (5 LR Scales $\times$ 23 Optimizers) | Final Evaluation (Best LR $\times$ 23 Optimizers) | Total GPU Hours |
| --- | --- | --- | --- | --- |
| ResNet-50 (ImageNet-1K) | NVIDIA A100 | - | $\sim$<!-- -->288 Hours (100/300 epochs) | $\sim$<!-- -->288 Hours |
| ViT-S (ImageNet-1K) | NVIDIA A100 | $\sim$<!-- -->544 Hours (100 epochs) | $\sim$<!-- -->213 Hours (300 epochs) | $\sim$<!-- -->757 Hours |
| LlaMA-60M (WikiText-103) | NVIDIA A100 | $\sim$<!-- -->28 Hours (Chinchilla scaling) | - | $\sim$<!-- -->28 Hours |
| Total Aggregated Cost | Equivalent to $\sim$<!-- -->45 Days on a single NVIDIA A100 GPU | | | $\sim$<!-- -->1073 Hours |

几点值得注意的地方：

- **ViT-S 是成本主体**，合计 $\sim$<!-- -->757 小时；其中**超参搜索（5 个学习率尺度 $\times$ 23 个优化器，100 epoch）就要 $\sim$<!-- -->544 小时**，而**最终评估（最佳学习率 $\times$ 23 个优化器，300 epoch）只要 $\sim$<!-- -->213 小时**——网格搜索的成本是最终评估的 **2.5 倍以上**，这正是"算力壁垒"的直接体现。
- **ResNet-50 行**未在搜索列与最终评估列之间做拆分（原文搜索列为 `-`，最终评估列写作"$\sim$<!-- -->288 Hours(100/300 epochs)"），此处照原样保留，不做推断。
- **LlaMA-60M 行**的超参搜索标注为 **Chinchilla scaling**（按 Chinchilla 缩放规律折算），为 $\sim$<!-- -->28 小时，**未单独列出最终评估成本**。
- **总计**：等价于**单卡 NVIDIA A100 上约 45 天**，合计 **$\sim$<!-- -->1073 小时**。

---

### Sec 5 未来展望（Future Prospect）

论文先归纳当前领域面临的挑战，再基于这些挑战与前述实验分析，探讨优化算法可能的**发展（研究）方向**。

### Sec 5.1 当前优化框架的挑战（Challenges in Current Optimization Frameworks）

论文的总纲是：**优化大模型本质上是一个高度受限的多目标权衡（highly constrained, multi-objective trade-off）**。当现有方法追求收敛速度、内存效率或分布式扩展性这类具体指标时，往往要**牺牲泛化能力、引入计算延迟或加剧系统不稳定性**。因此核心挑战是：**在统一的设计框架下打破这些相互耦合的瓶颈，有效平衡稳定性、计算成本与噪声鲁棒性**。

**（1）泛化与稳定性边际（Generalization and stability margins）。** **自适应方法经常把模型困在尖锐极小点（sharp minima）中**，泛化比标准 SGD **更差**；**预条件方法虽用结构近似改善了收敛，但稳定性边际很窄（narrow stability margin）**——**学习率一大就训练发散**。此外，**无状态（stateless）的显存高效设计丢失了二阶矩的历史累积**，**丧失了"平均掉极端梯度冲击"的能力**，在非凸损失景观中**有发散风险**。

**（2）显存与计算开销（Memory and computational overheads）。** 模型规模达到**数十亿参数**时，**稠密优化器状态构成首要显存瓶颈**；**结构近似**试图平衡精度与显存，但**矩阵求逆与结构性开销显著降低全局效率**。而且**某些方法理论上需要更少迭代即可收敛，但每步计算延迟往往抵消这些收益**，限制了大规模应用。

**（3）噪声放大与估计方差（Noise amplification and estimation variance）。** 在**深度注意力网络典型的各向异性损失景观**中，**小批量训练的随机噪声在曲率估计中被严重放大**，**降低更新稳定性**；**用于估计方向梯度的随机扰动也会遭受近似方差**，且该方差**随参数维度迅速增长**。**隐私保护优化器**面临平行的**效用权衡（utility tradeoff）**：**差分隐私所需的噪声注入降低梯度保真度并使收敛不稳定**。

**（4）可扩展性与共识张力（Scalability and consensus tensions）。** **优化轨迹对超参数依然高度敏感**，成功扩展需要**大量人工调参**。**分布式设定**下，**梯度压缩与本地更新结合会加剧客户端漂移（client drift）**，在**通信效率与全局共识之间形成持久张力**。此外，**在低秩子空间中优化以控制高维搜索空间，可能过早约束轨迹，使模型无法抵达全局最优**。

### Sec 5.2 对应的研究方向

**（1）一阶优化（First-order optimization）的三条路径。**

- **① 自动化符号发现（Automated symbolic discovery）。** 方法论重心应从**脆弱的启发式调参**转向**架构专属优化器的自动生成（automated generation of architecture-specific optimizers）**，使模型能够**在没有人工干预的情况下内在地穿越复杂损失景观**。
- **② 预条件与正交化（Preconditioning and orthogonalization）。** 鉴于 **Kron、Muon** 这类方法与自适应标量家族展现出**根本不同的损失穿越轨迹**，未来研究应聚焦于**推进结构性矩阵更新**：通过**利用结构性梯度统计做预条件**或**采用矩阵正交化技术**，这些方法能够**克服简单对角缩放的表征瓶颈**，为参数空间导航**开辟新颖且高效的路径**。
- **③ 动态精度缩放（Dynamic precision scaling）。** 为缓解**稠密优化器状态带来的严重显存瓶颈**，未来研究应**整合自适应低精度算术（adaptive low-precision arithmetic）**，在**不损害收敛稳定性**的前提下**显著降低显存足迹**。

**（2）二阶优化（Second-order optimization）的三条路径。** 论文的定位是：为应对这些**难以承受的瓶颈**，未来二阶框架的发展应强调**三个关键领域的结构性创新**：

- **① 硬件—算法协同设计（Hardware-algorithm co-design）。** 为克服**新出现的显存瓶颈**，研究应转向**通过整合结构感知的自适应与低精度算术来优化全局计算效率**，从而**在计算强度与收敛稳定性之间取得平衡**。
- **② 稳健的曲率估计（Robust curvature estimation）。** 未来设计应发展**数学上可靠的机制来隔离极端梯度离群值（isolate extreme gradient outliers）**，从而**拓宽那个"危险地狭窄"的安全边际，而不膨胀显存足迹**。
- **③ 稀疏预条件（Sparse preconditioning）。** 为缓解**高昂的每步计算延迟**，未来架构应探索**高效的稀疏矩阵求逆技术**，在**保留二阶信息内在优势**的同时，**达到与一阶基线相当的全局效率**。

**（3）零阶优化（Zeroth-order optimization）的三条路径。** 论文指出，要弥合**这一巨大的性能差距**，无梯度框架的未来发展必须聚焦于**噪声管理与空间管理**：

- **① 精确噪声抵消（Exact noise cancellation）。** 未来框架必须**在数学上抵消由多样网络架构引入的固有随机噪声**，可以**从"精确梯度校正机制"中汲取灵感——即减去历史扰动偏差（historical perturbation bias）来稳定高度易变的更新步长**。
- **② 维度鲁棒性（Dimensionality robustness）。** 研究应**发现先进的子空间投影方法**，使其能够**安全地约束搜索空间，而不会过早限制对高质量全局解的访问**。
- **③ 自适应扰动调度（Adaptive perturbation scheduling）。** 与其依赖**静态随机扰动**，未来策略应**根据局部景观几何动态调整盲估计的方差与方向**，从而**避免持续破坏局部极小点内部的脆弱平衡**。

**（4）面向场景的框架（Scenario-oriented frameworks）的三条路径。** 为保证在**关键基础设施**中可扩展且可信的部署，这些专用框架的未来发展必须**从经验性妥协转向严格保证**，覆盖三个关键维度：

- **① 严格的理论界（Rigorous theoretical bounds）。** 分布式与隐私保护框架必须为**通信效率**与**隐私效用**建立**严格的数学下界**。
- **② 状态保真度保持（State fidelity preservation）。** 未来显存高效的架构必须被设计为**恰好保留足够的历史上下文**，以便**平滑地平均掉极端梯度冲击**，同时**既不触及刚性的保真度上限（fidelity ceiling），也不超出显存约束**。
- **③ 异构共识机制（Heterogeneous consensus mechanisms）。** 为解决**通信效率与客户端漂移之间的持久张力**，分布式设置必须引入**拓扑感知的梯度压缩技术（topology-aware gradient compression）**，使其**自然适应不同网络架构之间梯度量级的巨大差异**。

**（5）讨论（Discussion）：总体走向。** 论文的判断是：**未来优化框架很可能超越孤立的算法改进，走向 FO（一阶）、SO（二阶）、ZO（零阶）算法的融合**。为应对显存瓶颈与结构性妥协，**研究重心必须从"最小化迭代复杂度"转向"提升全局墙钟时间效率（wall-clock efficiency）"**，而这**需要硬件—算法协同设计**：**结构性预条件与矩阵正交化是关键**，把它们与**动态低精度算术**整合，就能在**计算成本与收敛稳定性之间取得平衡**，使优化器**克服简单对角缩放的表征极限**。方法论上，**重心应从启发式调参与静态噪声注入转向自动化符号发现与精确噪声抵消**；结合**预条件与正交化的几何校正**，将**促成架构专属优化器**，以**处理高度各向异性的损失景观并抵抗极端梯度离群值**。在**分布式与隐私保护**场景中，框架必须**超越经验性权衡**：**为通信与效用建立理论下界，并配合拓扑感知的共识机制**，才能确保专用架构的**可扩展且可靠的部署**。

---

### Sec 6 结论（Conclusion）

论文的总结性论断可归纳为：

1. **定位与理论基础**：本文对**深度学习优化方法的最新进展**做了**全面而系统**的回顾，并首先通过回顾优化理论的**核心背景概念**建立起**严谨的理论基础**，为理解优化算法的演进提供**统一框架**。
2. **分类与分析**：论文系统**分类并分析了四大领域的技术方法**——**一阶、二阶、零阶优化算法，以及面向专门场景的优化框架**，同时覆盖每一类方法的**结构设计原理**与**功能特性**。
3. **实证评估**：为提供**实用且可操作的洞见**，论文做了一次**严谨、受控的实证评估**，在**多样的任务领域与模型架构**上对**代表性优化器**进行基准测试。
4. **核心结论——关键权衡**：实验分析揭示了**超参数敏感性、跨架构泛化能力、长期训练可扩展性三者之间的关键权衡（critical trade-offs）**，为真实应用中的**算法选择与超参数调优**提供了有价值的指导。
5. **未来方向与总体目标**：论文指出了该领域的**关键开放挑战**并勾勒出**有前景的研究方向**，以引导**下一代高效、稳健且可信的优化技术**；通过把**理论洞见与大量实证证据**结合，本综述**旨在成为一份全面、权威的资源**，服务于**希望推进深度学习优化最前沿的研究者与从业者**。

---

## 11. 阅读路线与自测题 → 已并入学习计划

> 📌 **这两块内容已拆散并入 `学习与追踪清单.md` 的对应位置**，本文件不再重复维护，避免两处分叉。

| 原来在这里的内容 | 现在在哪 |
| --- | --- |
| **三遍读法 + 跳过清单** | 学习计划 **总表第 28b / 28c / 28d 行**（第一遍框架、第二遍口径校准、第三遍缺口清单） |
| **第二遍的五条主线（A 预条件的谱 / B 稳定性与范数 / C 可行性约束 / D 分布与隐私 / E 基准方法学）** | 学习计划 **总表第 28e 行** |
| **第三遍当字典用 + 跳过清单 + 读完验收** | 学习计划 **总表第 28f 行** |
| **30 道自测题** | 按主题拆入对应检查点：Sec 3.1 框架题 → 检查点 4 / 20；ZO 题 → 检查点 1；分布式 $\mathcal T$ → 检查点 5；RMSprop 回退与收益递减 → 检查点 6；LR 安全区间 → 检查点 9；跨架构与排行榜 → 检查点 16；算力成本 → 检查点 13；Muon vs Adam → 检查点 18；综合题 → 检查点 19 终测表 |

**一条留在本文件的提醒：**

> ⚠️ **这份综述不是第一份材料。** 它假设你已经知道 Adam 的更新式、知道反向传播在算什么、知道 KL 散度长什么样。**如果你在 Sec 3.2.2 第一段就卡住，那不是你的问题，是材料选错了。** 正确顺序是先在 `学习与追踪清单.md` 里把 Adam/AdamW/Muon 的基础打通，再回来读这份综述——它的价值会翻好几倍。

---

## 12. 自测题索引（原编号 → 现位置）

> 下面保留**原始编号**只作索引之用，题目正文已移入学习计划。**做题请到 `学习与追踪清单.md` 对应检查点。**

| 原编号 | 主题 | 现在的位置 |
| --- | --- | --- |
| 1–5、10–11 | Eq.(7) 四算子、$\mathcal T$ 的类型与两种实例、ZO 估计式、三堵墙、既有综述三点不足 | 检查点 4（第 7–10 题）、检查点 5（第 9 题）、检查点 1（第 12 题）、检查点 20（第 1–3 题） |
| 6–9 | 预条件子 $M_t$ 的一阶/二阶判据、二阶一般形式、自适应通用分解式 | 检查点 4（第 7–10 题） |
| 12 | Lion 是怎么被发现的（程序搜索） | 检查点 12（第 12–13 题） |
| 13 | 为什么经验 Fisher 方法被归为一阶 | 检查点 4（第 7–8 题）+ 检查点 11（第 9–11 题） |
| 14–15 | Adam 在 10× lr 下为什么崩、sign 更新的位置 | 检查点 10（第 10–12 题）|
| 16 | Kronecker 类方法的缺口与补救方向 | 检查点 20（核心洞察 ④ + 第 6 题）|
| 17 | 二阶矩融合的缺口（该用多少二阶信息） | 检查点 12（第 14 题）|
| 18–19 | RMSprop 回退、复杂预条件收益递减 | 检查点 6（第 8–9 题）|
| 20 | Muon/Kron 的轨迹为何本质不同 | 检查点 12（第 10 题）|
| 21 | 如何反驳"Muon 比 Adam 好" | 检查点 18（第 9 题）|
| 22–23 | architectural proxy 的声明、排行榜为何不适定 | 检查点 16（第 12–13 题）|
| 24 | 算力成本表想论证什么 | 检查点 13（第 9 题）|
| 25–27 | 结构化矩阵更新的理论根据、反驳"只是工程"、综述最大弱点 | 检查点 19 终测表第 17–19 行 |
| 28 | ZO 复兴与显存墙 | 检查点 1（第 12 题）|
| 29 | 设计实验检验"架构影响超过算法" | 检查点 19 动手清单 |
| 30 | 1073 GPU 小时对独立研究者意味着什么 | 检查点 19 终测表第 20 行 |

## 13. 精读笔记模板

读每个算法时按这个模板填，一张卡片一个算法：

```
算法名 / 年份 / 原文文献
─────────────────────────
在 Eq.(7) 中的实例化：
  E        = ?
  T_scen   = ?
  φ        = ?
  M_t      = ?
  P_Θ      = ?
─────────────────────────
它想解决什么问题（对应哪个子节）
核心机制（一句话）
每步额外开销：显存 / 计算 / 通信
对学习率的敏感性
失效场景
它和谁本质等价 / 谁是谁的特例
─────────────────────────
我不懂的地方：
```

> 💡 **模板里最关键的两栏是"在 Eq.(7) 中的实例化"和"它和谁本质等价"。** 前者逼你把新算法塞进统一框架（塞不进去的地方往往就是这个算法的真正创新点，或者综述框架的盲区）；后者逼你建立联系而不是收集名词。**你之前说"没有任何 muon adam adamw 积累"——这个模板的作用就是防止你把学习变成名词收集。**

---

## 14. 一句话总结

> 这份综述的论点可以压缩成一句：**当模型规模、隐私合规与分布式部署成为硬约束之后，"更快的收敛"不再是优化算法的唯一目标函数；现代优化器的演化，是传统数学原语（一阶/二阶/零阶）在可行性危机下被重新架构的过程，而 Eq.(7) 的四算子分解给出了理解这场重构的统一坐标系。**

---

*本文件所有内容基于 arXiv:2604.12968v1 原文提取，章节号、公式编号、表号与实验数字均可回原文核对。*
